---
topic: Global FR to Epic to Story Coverage
updated: 2026-09-03
---

- (event) Created the canonical cross-product coverage model during the approved People Management documentation re-baseline.
- (decision) Product requirements use PM-FR-1..42 exactly once; UM-FR and M-FR identifiers are historical/domain aliases.
- (decision) Platform, User Management, and Mentorship epic files remain unchanged bounded-context slices.
- (decision) Coverage status is independent of sprint status and uses implemented, in-progress, specified, deferred, superseded, or uncovered.
- (open) CONFLICT-UM-01 preserves contradictory 403/404 empty-audience denial semantics pending owner approval.
- (decision) CONFLICT-UM-01 marked resolved at product (401/404/403). Removed as live gate/conflict on PM-FR-4 and PM-FR-12. Notes added for PM-FR-19/32/34/41. Runtime denial still diverges.
- (decision) Removed live OQ-114/OQ-115/CC-05 gates after PM/AD-32/33/28. OQ-PERM-01 remains. DEPARTMENT-EDGE and CC-07 remain implementation gates.
- (decision) H6: Live TIMETRACKER-CONTRACT gates replaced by TT-IDENTITY-01 and/or TT-PMDM-01. TIMETRACKER-CONTRACT retained only as superseded historical record.
- (event) H6 applied: PM-FR-2/14/37 use TT-IDENTITY-01 and/or TT-PMDM-01; PM-FR-36 has no invented live TT gate; TIMETRACKER-CONTRACT historical only.
- (event) RETIRE prior open CONFLICT-UM-01 empty-audience bullet: superseded by later decision resolving product conflict; runtime divergence remains implementation debt. Mentorship G-* / OQ-M* gates removed from coverage; live gates use blockers.yaml IDs only.
- (event) Created resourcing bounded-context slice `resourcing/epics.md` (RS-E*): PM-FR-23..26 decomposed into 2 epics, 7 stories. Status uncovered → specified. RS-E* registered in namespace_rules and source_slices. PRD §0.2 registration still open (same class as PMC-E* gap).
- (event) Created engagement bounded-context slice `engagement/epics.md` (ENG-E*): PM-FR-19, 20, 21, 22, 35 decomposed into 4 epics, 16 stories, 113 acceptance criteria. Status uncovered → specified. ENG-E* registered in namespace_rules and source_slices. PRD §0.2 registration still open (same class as PMC-E* and RS-E*).
- (decision) ENG SD-2: S6, S8, and S14 section support is a slice-level precondition with NO registered blocker. AC-S9-S13 is named for a different range and must not be stretched; a new blocker (suggested AC-S6-S8-S14) is owed by access-control. Recorded as a Step 4 FAIL in the ENG slice and as notes on all five FRs. No gate ID was invented in any `gates:` list.
- (decision) ENG SD-8: a form-campaign audience is frozen at activation as a resolved list of people. This is deliberately the opposite of PMC SD-5 shared-view re-resolution. Both are correct for their object and must not be unified into one rule.
- (decision) ENG SD-3: the requirements §3.3.7 campaign-sender exception is implemented as a fixed campaign-bound projection, never as a permission flag, audience, or tier promotion. ENG-E2-S2.3 and ENG-E2-S2.4 are one release unit — 2.3 grants the exception and 2.4 is the only thing that ends it.
- (decision) ENG SD-5/SD-6: risk has no terminal state; `leaver` is never read, written, or joined against EmploymentStatus `dismissed`; S6 is absent for Self on every surface, verified by endpoint enumeration. ENG-E3-S3.1 and ENG-E3-S3.3 are one release unit.
- (open) SD-9 escalation: S7 management notes carry a §3.2 matrix row, three §3.3 normative rules, and user journey UJ-4, but no owning requirement in PM-FR-1..42. Product-model gap; no FR was invented.
- (finding) blockers.yaml records QUALITY-GATE-AC as `closed` (2026-09-02, gate_status=PASS / p0_status=MET / critical_open=0) while ARCHITECTURE-RATIFICATION.md §7 prose still lists it among open P0s, and §7's counts (18 open / 8 closed) contradict §2.1, §10, §11 and a direct count (16 open / 10 closed / 4 superseded, 30 total). §7 declares blockers.yaml authoritative, so the ENG slice used blockers.yaml. `platform-capabilities/epics.md` carries the stale QUALITY-GATE-AC P0 precondition and should be corrected.
- (event) `RS-E1-S2.1` → `RS-E2-S2.1` corrected in `resourcing/epics.md` (FR coverage map + story heading) and in this model. The story sits under resourcing Epic 2; sprint key `2-1-s15-request-history-on-profile` was already correct and is unchanged. Supersedes the prior "left as found" finding.
- (decision) A requested `action-items/epics.md` slice (`AI-E*`) for PM-FR-19 was NOT created: PM-FR-19 was already decomposed into `engagement/epics.md` ENG-E1 (4 stories, registered here). A second slice would have produced a duplicate decomposition, a second unregistered namespace, and a conflicting `stories:` list for one FR. Patched ENG-E1 in place instead — one owner per FR.
- (event) ENG-E1 AD-14 gap closed: the slice cited PM/AD-5 context creation but never bound the route shape. Added PM/AD-14 to ENG Additional Requirements, a route-contract note on Epic 1, and an ENG-E1-S1.2 acceptance criterion fixing the lifecycle to top-level `POST /action-items/:id/complete|cancel` (shape-2 collection-member actions, never nested under /users, no generic PATCH status write). PM-FR-19 notes updated.
- (event) Created feedback bounded-context slice `feedback/epics.md` (FB-E*): PM-FR-35 decomposed into 2 epics, 3 stories. Carved out of engagement ENG-E4. Coverage re-pointed ENG-E4-S4.* → FB-E1-S1.1, FB-E1-S1.2, FB-E2-S2.1. Gates: OQ-PERM-01, AC-S9-S13. ENG Epic 4 marked superseded. FB-E* registered in namespace_rules and source_slices. Campaign distribution cross-refs ENG-E2; not duplicated.
- (event) PRD §0.2 registration gap CLOSED: §0.2 now carries a slice table (PMC-E*/RS-E*/ENG-E* with file paths and decomposed FR ranges), adds RS-E* and ENG-E* to the namespaced-identifier bullet, and states the one-owning-slice-per-requirement rule that makes a second decomposition of an owned PM-FR a defect rather than an alternative. Extended the same pass to FB-E* and TT-E* as those slices landed, so §0.2 now registers all seven live namespaces and no §0.2 registration gap remains. Any future slice must add its row here as part of its own run.
- (decision) Duplicate placeholder gate IDs converged: ENG SD-2's suggested `AC-S6-S8-S14` is RETIRED in favour of `AC-SECTION-MATRIX-01` (already used by PLAT-E6 for S2-S8 and S14-S16, a strict superset). One registration now closes the gap for both slices. Registration in blockers.yaml stays architect-owned; no gate ID was added to any live `gates:` list.
- (event) Stale QUALITY-GATE-AC P0 precondition removed from `platform-capabilities/epics.md`: both QUALITY-GATE-AC and QUALITY-GATE-AC-NFR now read as closed 2026-09-02 per blockers.yaml, with the NFR closure's resolver-revision pin (f89e034) recorded so an access-control change is known to invalidate it. Supersedes the prior finding. ARCHITECTURE-RATIFICATION.md §7 prose is still stale and remains architect-owned.
- (event) Created timetracker bounded-context slice `timetracker/epics.md` (TT-E*): PM-FR-36, PM-FR-37 decomposed into 2 epics, 8 stories. Status uncovered → specified. TT-E* registered in namespace_rules and source_slices. Gates: none on PM-FR-36; TT-IDENTITY-01 + TT-PMDM-01 on PM-FR-37 only — no TIMETRACKER-CONTRACT. Epic 2 NOT STARTABLE until TT-IDENTITY-01 closes (TT-SD-4). Unblocks PM-FR-2 project-line, PM-FR-14, PMC-E3 (PM-FR-16/17). PRD §0.2 registration still open (TT-E* same class as FB-E*).
- (open) Uncovered after this run: PM-FR-9, PM-FR-16, PM-FR-17, PM-FR-27, PM-FR-30, PM-FR-31 (6 of 42). PM-FR-16/17 remain epic-assigned and story-uncovered by PMC SD-7, blocked on TT-IDENTITY-01 until timetracker Epic 2 lands.
- (event) Platform CE pass (bmad-create-epics-and-stories Steps 1–4) appended PLAT-E4–E7 to existing `platform/epics.md` (append/upgrade only; E2/E3 story bodies frozen). 4 epics, 16 stories, Given/When/Then AC, ID + sprint key + gates. PM-FR-1 gained PLAT-E4-S4.1 (one FR-1 consequence). PM-FR-2 gained PLAT-E4-S4.1–S4.4 and PLAT-E5-S5.1–S5.3. PM-FR-3 gained PLAT-E6-S6.1–S6.6 and PLAT-E7-S7.1–S7.3. Coverage status stays in-progress on all three. No new file. No sprint-status keys. No blockers.yaml edit.
- (decision) Platform SD-1: E2/E3 (ACF-1, ACM-0..ACM-9) are historical evidence; this pass does not restate or re-scope them.
- (decision) Platform SD-2: no ACM-*/ACF-* workboard IDs assigned to E4–E7; stories are PLAT-E{n}-S{m} only.
- (decision) Platform SD-3: AC-SECTION-MATRIX-01 remains proposed/unregistered; not written into any coverage `gates:` list. Closing AC-S9-S13 unblocks S9 and S13 only.
- (decision) Platform SD-4: Epic 5 owns the department/PP walk only; PM/AD-35 schema stays unowned; completing E5 does not close DEPARTMENT-EDGE; PM-FR-42 stays with UM-E4-S4.3.
- (decision) Platform SD-5: Epic 7 owns the §3.2 Shared-link port only. PM-FR-27 stays uncovered — a port is not the capability.
- (decision) Platform SD-6: Epic 7 overlay evaluation only (PM/AD-28, CC-05 design-closed). PM-FR-39 grant lifecycle stays deferred. S7.3 creates no grant/revoke/seed path.
- (decision) Platform SD-7: no E4–E7 story claims PM-FR-4. Decision vs HTTP projection; UMAC-1 remains the adoption owner.
- (decision) Platform SD-8: every E4/E5/E6 production story re-runs ACM-9; QUALITY-GATE-AC-NFR pin at f89e034 is invalidated by any access-control change.
- (decision) Platform SD-9: file-churn split across E4–E7 on src/access-control/** is intentional (identity / department / matrix / overlay risk boundaries), not consolidation-by-default.
- (open) Step 4 FAILs recorded in platform/epics.md: SEC-AUTH-01 P0, UMAC-1 in-progress, TT-IDENTITY-01 (E4 cannot close), DEPARTMENT-EDGE (E5 cannot close), AC-SECTION-MATRIX-01 unregistered, PM-FR-5 blocks S16, CC-07 blocks S7.1 journal evidence, PLAT-E2-S2.1 coverage/sprint-status drift left to Story 1.1.
- (event) `gap-validation-2026-09-02.md` findings triaged 2026-09-03. Already closed before triage: both CRITICALs (risk carve-out complete — PM-FR-21/22 point at `RISK-E1-S1.1`–`S1.4` / `RISK-E2-S2.1`–`S2.3`, ENG Epic 3 marked superseded, `RISK` in `namespace_rules` + `source_slices`) and LOW PMC-ROOT-DUPLICATE (`planning-artifacts/epics.md` is now a symlink, not a byte copy). Closed in this pass: STORY-ID-NOT-IN-EPICS (`M-E1-S1.1`–`S1.6` ID + sprint-key blocks added to `mentorship/epics.md`, FR Coverage Map gained a Story ID column, frontmatter gained `slice`/`id_namespace`; all six IDs now resolve bidirectionally) and both PRD findings (§0.2 gained a Risk row, a CDS draft-only row, `RISK-E*` in the identifier bullet, and a retired-identifier line; §13 postures rewritten against actual yaml statuses with an as-of date and the 5/27/6/4 rollup).
- (open) Left open by design after the 2026-09-03 triage: CDS (HIGH ×3 — `cds/epics.md` is still a template with `{{requirements_coverage_map}}`/`{{epics_list}}`, PM-FR-30/31 stay `uncovered`; needs real decomposition, not a yaml edit) and `AC-SECTION-MATRIX-01` (MEDIUM ×4 — registration is architect-owned per Platform SD-3 and must not be written into any `gates:` list here). The remaining MEDIUM/LOW/INFO rows are recorded accepted gaps.
- (event) CDS slice completed and registered 2026-09-03. The 2026-09-02 file was not a template — analysis (9 scope decisions, preconditions, 6 verified inconsistencies, full requirements inventory, 12 CDS-DR) was finished; only `{{requirements_coverage_map}}` and `{{epics_list}}` were unrendered. Wrote FR Coverage Map, Epic List, and 7 story bodies: CDS-E1 (matrix dictionary, assessment log, IDP + own-completion tick, S12 wall) and CDS-E2 (last-assessment date filter, open-IDP filter, non-inference property). PM-FR-30/31 uncovered → specified. CDS registered in namespace_rules + source_slices as `role: draft-bounded-context-slice` (matching `status: draft`, since PM/AD-5 still lists `cds` pending confirmation — SD-9, no scaffolding story created). PRD §0.2 CDS row flipped from draft-only to live; §13 and the rollup updated. Uncovered now 4 of 42: PM-FR-9, 16, 17, 27.
- (event) FALSE CLAIM corrected in `cds/epics.md` L54: the file asserted `CDS-E*` "is registered ... as part of this run" when it was in neither registry, and described PRD §0.2 as enumerating only four namespaces. Rewritten to state what was actually false, and what is now true. This was the audit's DRAFT-SLICE-CLAIMS-COVERAGE finding.
- (decision) CDS gate asymmetry recorded: only CDS-E1-S1.1 is hard-gated on DEPARTMENT-EDGE, because only the matrix key resolves against the Department entity (SD-2). S1.2/S1.3/S1.4 and all of Epic 2 carry no department dependency, so PM-FR-30 is partial-by-gate on one story rather than blocked whole. PM-FR-31 does not inherit DEPARTMENT-EDGE.
- (open) `AC-SECTION-MATRIX-01` NOT registered — proposal written instead at `architecture/gate-registration-proposal-AC-SECTION-MATRIX-01-2026-09-03.md`, nothing applied, per Platform SD-3 (architect-owned) and the PLAT-E1-S1.1 invariant. Re-check narrowed the defect to 4 requirements over 3 sections (PM-FR-21/22 → S6, PM-FR-26 → S15, PM-FR-35 → S8); the audit's PM-FR-19 entry is WRONG — it carries no AC-S9-S13 gate. Surfaced a contradiction the audit missed: S12 is in AC-S9-S13's id and closure_condition but absent from its blocks:, gated on AC-SECTION-MATRIX-01 by PLAT-E6-S6.5, and absent from every row of platform/epics.md's Gate binding table. Two resolutions offered; (a) keeping S12 under AC-S9-S13 is the smaller change and matches the registered artifact. Reporting hazard: on the day AC-S9-S13 closes, four requirements plus the CDS slice will look unblocked.
- (decision) `AC-SECTION-MATRIX-01` REGISTERED 2026-09-03, resolution (a), approved by the decision owner — reversing the same day's "proposal only, do not apply" position. Entry added to `blockers.yaml` (Access Control, P1 open, `blocks:` S2-S8 and S14-S16 naming Risk S6, Feedback S8, Action items S14, Request history S15; AD-1 closure condition, same evidence bar as AC-S9-S13). `revision:` bumped to 2026-09-03-section-matrix-gate-registration. Registration closed a gap rather than adding a constraint: the facade already returned `none` for these sections, they simply had no citable ID and PLAT-E1-S1.1 forbids an unresolvable `gates:` entry.
- (decision) S12 ownership contest resolved in favour of `AC-S9-S13` (option (a)) — its `id` and `closure_condition` already spanned S9-S13, so `blocks:` was corrected to name *CDS registry and CDS directory filters (S12)* rather than narrowing the closure condition. Consequences applied: `platform/epics.md` Gate binding table gained the S12 row it had never carried in any row; `PLAT-E6-S6.5` now carries BOTH gates because it spans S12 (AC-S9-S13) and S14/S15 (AC-SECTION-MATRIX-01), so closing one gate does not release that story; `cds/epics.md` needed no acceptance-criterion change, having used the registered ID from the start.
- (event) Gate repointing applied: PM-FR-21, PM-FR-22 (S6), PM-FR-26 (S15), PM-FR-35 (S8) moved off the AC-S9-S13 stand-in onto AC-SECTION-MATRIX-01, with their GATE SCOPE DEFECT annotations retired. PM-FR-3 carries both, owning the whole section matrix. PM-FR-29 (S9), PM-FR-30/31 (S12), PM-FR-32/33/34 (S13) correctly keep AC-S9-S13. Slice prose updated in platform (SD-3, gate-gap section, binding table, stories 6.3-6.6, follow-ups, coverage-actions), risk (RSD-3, precondition table, 3 story gate lines, findings), feedback (FSD-3, preconditions, 3 story gate lines, check 7, follow-up 1), engagement (SD-2, check 7) and cds (inconsistencies #2/#3, preconditions, findings, escalations). No "proposed, unregistered" claim survives as a current fact.
- (open) NEW reporting hazard, inverted from the old one: AC-SECTION-MATRIX-01 and AC-S9-S13 are now two open P1 gates over one section matrix, and closing either will look like it releases more than it does. Both entries carry explicit "closing this unblocks only X" notes, and PLAT-E6-S6.5 is the story where the two ranges meet.
- (event) Blocker verification sweep 2026-09-03 — all 31 blockers.yaml entries checked, 17 open, recorded at `architecture/blocker-verification-2026-09-03.md`. **Zero closeable, zero stale premises.** Every factual claim in the 12 code-dependent entries verified true against services/backend@e6049c8: SEC-AUTH-01's `Boolean(userId)` stub is exact AND wired into the production module (user-management.module.ts:36 binds ACCESS_CONTROL_PORT to InterimAccessControlAdapter); the facade's S1/S10/S11-only early return is literal; AccessJournal, UserEvents, EmploymentStatus, Department/UserDepartment and src/mentorship all absent; Relationship type=project has no application writer at all. No blocker status changed — blockers.yaml's own rule is that open blockers are fail-closed and not resolved by ratification, and individual entries repeat that schema/design approval does not close implementation.
- (open) Five blockers close on an APPROVAL, not code, and are the only ones actionable without implementation: OQ-PERM-01 (PO — default role-to-permission matrix), OQ-AC-EDIT (architect + AC — catalog entries or remove the design references), TT-IDENTITY-01 (P0), TT-PMDM-01, OPERATIONAL-ENVELOPE (P0). OPERATIONAL-ENVELOPE is cheapest: its closure condition explicitly accepts "explicitly deferred with a named owner", so it needs assignment, not solutions.
- (open) EVIDENCE DEGRADED: blockers.yaml's evidence_caveat records docs/integrations/timetracker-external-api.json as untracked and absent at the pinned SHA on 2026-09-02, leaving TIMETRACKER-CONTRACT, TT-IDENTITY-01, TT-PMDM-01 and part of OPERATIONAL-ENVELOPE as working-tree observations. As of 2026-09-03 `docs/integrations/` does not exist in the working tree either, so those findings are now unauditable from both index and tree. Two of the four are P0. No status changed.
- (event) Merged origin/main into docs/ux-reconciliation-and-platform-capabilities 2026-09-03 (3 commits: User management WIP #15, post-ratification corrections #13, QUALITY-GATE-AC-NFR closure #14). Two conflicts, both resolved: `implementation-artifacts/platform/sprint-status.yaml` (only `last_updated`, took the newer 09-02 11:20) and the `services/backend` submodule (merged origin's 960ff87 into our 03d54ea inside the submodule — clean, 56 files; superproject gitlink now f091d44).
- (event) The merge INVALIDATED four rows of the same day's blocker sweep; `architecture/blocker-verification-2026-09-03.md` gained a §5 re-sweep and the stale rows are struck through rather than rewritten. SEC-AUTH-01 is HALF resolved — `interim-access-control.adapter.ts` is deleted and ACCESS_CONTROL_PORT now binds the real AccessControlFacadeAdapter (audience resolution for reads, fail-closed on writes), but `interim-session-resolver.adapter.ts` is still wired as SESSION_RESOLVER_PORT and still provisions a `position: 'HR Admin'` user, so the closure condition (interim adapters fail closed OR are removed) still fails. DEPARTMENT-EDGE, CC-08 and CC-09 gained their tables (Department, DepartmentMembership, EmploymentStatus, UserEvent) but stay open: no parentId index, no isHr column, no cycle rejection, no idempotencyKey. CC-07 (AccessJournal), CC-10-MENTORSHIP, ARCH-PROJ-WRITER-01 and both AC gates unchanged. Still 17 open, still 0 closeable.
- (decision) DEPARTMENT CARDINALITY CONTRADICTION found and partly corrected. `docs/project-requirements.md` §4.17 was amended 2026-09-02 to "one or more departments" (was "exactly one"), and the backend implements the amended rule — DepartmentMembership is temporal with a partial unique index on (userId, departmentId), deliberately permitting many current memberships. `cds/epics.md` carried the superseded wording in its 2026-09-03 draft and was CORRECTED in place, with a new [DERIVED] rule on Story 1.1: a multi-department employee resolves one matrix entry per current department and the section renders all of them rather than silently picking a specialisation. NOT edited, because they need one architecture-owned amendment rather than four independent restatements: AD-35's `UserDepartment {userId PK}` shape (the PK *is* the superseded constraint), DEPARTMENT-EDGE's closure_condition naming UserDepartment, platform/epics.md's AD-35 restatements, and platform-capabilities/epics.md:959 whose acceptance criterion asserts "each employee appears under exactly one department" — a wrong assertion, not just stale prose.
- (decision) REVERTED the §4.17 department-cardinality amendment that arrived with `a9c6873 User management WIP (#15)`. That commit edited `docs/project-requirements.md` and `docs/requirements-changelog-v1.2-to-v1.5.md` from "exactly one department" to "one or more departments", self-labelled "(Amended 2026-09-02)". Product Owner decision 2026-09-03: the local version is correct and requirements do not change from an implementation branch — PRD §0.1 makes docs/project-requirements.md v1.5 the upstream normative assignment and grading source. Both files restored to the pre-merge text; nothing else from that commit was touched (its architecture docs, design prototypes and test cases are downstream work at their own altitude).
- (open) DEPARTMENT CARDINALITY — direction corrected. The normative rule is unchanged at "exactly one department" (§4.17, PM/AD-35's `UserDepartment {userId PK}` where the PK *is* the constraint). The SHIPPED SCHEMA diverges: `DepartmentMembership` is temporal with a partial unique index on (userId, departmentId), permitting many current memberships, and the migration cites `docs/architecture/database-schema.md` §"Multi-department membership" as its authority. So an architecture doc and a migration both diverge from the normative source, and the earlier requirements edit would have hidden that divergence instead of exposing it. DEPARTMENT-EDGE is correct as written and stays open on four counts, not three: parentId index, UserDepartment (a different entity with a different cardinality shipped), cycle rejection, isHr. Architect must either bring the schema to §4.17 or raise an amendment at the requirements altitude — not settle it in a migration comment.
- (event) `cds/epics.md` re-reverted to the §4.17 rule: fixed fact back to "exactly one department", the multi-department [DERIVED] matrix rule removed, and the PM/AD-35 note reframed so the divergence is recorded against the implementation rather than absorbed into the slice. SD-2 unchanged — the matrix key resolves against the employee's single department.
- (event) Created profile-sharing bounded-context slice `profile-sharing/epics.md` (PSH-E*): PM-FR-27 decomposed into 2 epics, 7 stories. Status uncovered -> specified; uncovered now 3 of 42 (PM-FR-9, PM-FR-16, PM-FR-17). PSH-E* registered in namespace_rules and source_slices as `role: draft-bounded-context-slice` (PM/AD-5 has no profile-sharing context — PSH SD-9, confirmation is a sprint-entry precondition, no scaffolding story). PRD §0.2 gained a Profile Sharing row and §13 was updated. This closes the model's longest-standing recorded gap: PLAT-E7-S7.1/S7.2 deliver only the §3.2 Shared-link section-policy port counted under PM-FR-3, and platform SD-5's "a port is not the capability" is unchanged — PLAT-E7 is retained in PM-FR-27 epics[] as the consumed port, not as coverage.
- (decision) PSH SD-2: a link is an authorization artifact, never a document. No profile content is snapshotted, cached or frozen at creation — a snapshot would survive revocation, creator-access loss, and a section being switched off, defeating every §4.8 lifecycle rule at once. SD-3: revocation authority is resolved live from the current relationship graph and never copied onto the link row, because a stored revoker list is how a link outlives everyone entitled to kill it.
- (open) CC-07 is a RELEASE BLOCKER for the whole profile-sharing slice, not a deferrable story gate. §4.8's "every access via a link is journaled" is unconditional and §3.4 lists shared-link access among the six events defining the journal's entire scope; a share link is the only path granting profile access outside the relationship graph, so shipping links without shared_link_access journaling creates the one access class nobody can audit. PSH-E2-S2.4 is hard-gated and inside the release unit.
- (open) NORMATIVE GUARANTEE UNMET, no owner (PSH Step 4 check 7 FAIL): §4.8 guarantees "there must never be a link nobody can revoke" and names full-profile holders as the backstop, but PM-FR-39 is deferred with no stories and no owner, and platform SD-6 confirms PLAT-E7-S7.3 creates no grant/revoke/seed path. The backstop class exists in the requirements and nowhere else. PSH-E2-S2.3 delivers the relationship-derived path in full and records the guarantee as unmet rather than asserting it.
- (open) NORMATIVE CONTRADICTION escalated: S5 is listed among the sensitive sections requiring explicit re-enablement on EVERY link (§4.8) and is simultaneously one of the five sections enabled BY DEFAULT on the resourcing evaluation link (§4.7, as RS-E1-S1.4 implements). Both are normative and cannot both hold on one link. PSH-E1-S1.2 implements §4.8 for creator-made links and leaves the resourcing default set with RS-E1-S1.4, so neither story rewrites the other silently.
- (event) Decision package for the five approval-closable blockers written at `architecture/decision-package-five-open-blockers-2026-09-03.md`. Nothing applied — all five are PO/architect-owned. Contains a DRAFTED default role-to-permission matrix for OQ-PERM-01 (the draft §2.3 itself asks the team to produce): 10 of 16 rows derived directly from §2.2's role table, 6 cells left open as the ones §2.3 says the document does not settle — approve/reject candidates (DM only or DM+PM), assign and end mentorships (UM vs PP, complicated by the company-wide pool), edit the career timeline (PP exclusive?), create feedback (§4.15 names the manager line, which is an access fact not a role), manage custom fields, and change organisational relationships. The last is flagged as needing the most care: it is the only permission in the list that changes who can see whom, so a generous default quietly widens data access in a way §2.3 otherwise forbids.
- (decision) OQ-AC-EDIT is effectively already decided and only needs execution: the merged `access-control-facade.adapter.ts` carries "Unseeded today (Open Decision (i) = option (a), pending) -> isAllowed fails closed". Runtime fails closed, which is why it is P1 not P0. Recommendation is to append `user-management:edit` and `mentorship:assign` in ONE kernel seed AD-1 sequence — same class of gap, and AD-7's append-only key rule makes a combined append no riskier than a single one. Option (c) "remove the design references" is not viable: those two keys ARE the functional half of the §2.2 write dual gate and the willing-mentor-pool gate.
- (open) TT-IDENTITY-01 recommendation: (c) require a durable id on the Talent endpoint as the target, (b) amend AD-13 to accept email as an INTERIM with a recorded expiry trigger. Option (a) — the Accounting endpoint's durable Employee.id — was rejected as the primary path because that endpoint requires month+year and returns only employees with time entries in that period, so identity would depend on timesheet activity and silently omit people; a weaker key that omits nobody is safer than a durable key that does. Note (b) needs an amendment at the REQUIREMENTS altitude (§6 is what forbids email), not a spine edit. TT-PMDM-01 must be decided in the same conversation — same payload, and splitting them invites two different join keys in one object.
- (open) OPERATIONAL-ENVELOPE is the cheapest P0 on the board: its closure_condition explicitly accepts "explicitly deferred with a named owner", so it needs eight owners assigned, not eight problems solved. Two are NOT safely deferrable: BUSINESS_TIME_ZONE validation (AD-20 classifies mixed process configuration as a startup/deployment failure, so per-environment divergence is a live defect) and secret management (the only declared TimeTracker server is a dev host on a per-partner X-Api-Key, and both TT decisions would add a second partner key).
- (event) Full mechanical completeness verification run 2026-09-03 and saved as a re-runnable script at `global-coverage/verify-coverage.py` (exit 0/1, seven checks). It found a REAL defect the 2026-09-02 audit missed entirely: `user-management/epics.md` contained ZERO `UM-E*` ID literals, so all 11 UM story IDs the coverage model referenced resolved nowhere — the identical defect that report flagged for mentorship only. The first pass under-reported it (6 of 11) because a cross-reference from another slice counted as "exists"; the check was tightened to require the ID literal in its OWN slice file, which is the only correct test.
- (event) Fixed: 16 `**ID:** · **Sprint key:**` blocks added to `user-management/epics.md` covering every story including the five outside the FR mapping, with sprint keys taken verbatim from `implementation-artifacts/user-management/sprint-status.yaml` rather than derived from titles. Frontmatter gained `slice` / `id_namespace`. All 11 slices now pass the strict per-slice bidirectional check with 0 unresolved.
- (event) Normative section coverage checked for the first time — the check that would reveal a MISSING epic rather than a broken link. 31 §-sections in `docs/project-requirements.md`, 29 distinct `normative_refs` in the model, and exactly 2 sections carry no requirement: §4.13 Notifications and §4.14 Analytics and reports, both marked `[GOOD TO HAVE]` in the source itself. No normative scope is silently unowned.
- (event) Epic-coverage review run 2026-09-03 (`bmad-review`, adversarial lens, 11 slices + PRD + model + requirements v1.5), saved at `global-coverage/epic-coverage-review-2026-09-03.md`. It answered a question the model could not: `epics:` was populated on 7 of 42 requirements, so epic-level coverage was recoverable only by parsing story-ID prefixes, and no check required a `specified`-or-better requirement to name an epic. `verify-coverage.py` printed ALL CHECKS PASS while being structurally unable to detect a requirement with no epic.
- (event) `epics:` filled for all 42 requirements from `stories[]` unioned with prior explicit values. Diffed field-by-field against the pre-edit file: only the `epics` key changed anywhere, every other field byte-identical. 35 requirements gained the key; `PM-FR-3` gained `UM-E0`, which its own `UM-E0-S0.1` story had always implied and which the 7-entry hand-maintained set had missed. Four legitimately empty: `PM-FR-5`, `PM-FR-6`, `PM-FR-9`, `PM-FR-38`.
- (event) Three checks added to `verify-coverage.py` section 5: every requirement declares an `epics[]` key; every `specified`/`in-progress`/`implemented` requirement names an epic; `epics[]` is never narrower than the epics its `stories[]` imply. Two INFO lines added: epic-assigned-but-story-uncovered requirements, and requirements whose entire story coverage sits in a non-final slice. All checks pass. `README.md` completeness rules updated to match.
- (decision) The three real coverage gaps are `PM-FR-6`, `PM-FR-9` and the undeclared dependency on `PM-FR-5`. `PM-FR-6` (runtime functional role administration) is `deferred` with zero epics while `prd.md:616` §6.1 puts it in MVP and requirements §9 makes it a graded DoD item — §13 discloses the deferral without reconciling it against §6.1. `PM-FR-9` is the only requirement in the model with no gate, no notes and no recorded deferral decision; its sole rationale is `platform-capabilities/epics.md:37` citing an absent UX surface, which is not a product decision to drop a normative §4.1 bullet.
- (decision) `PM-FR-5`'s missing gate is NOT a defect and was left alone — this log already records "Removed live OQ-114/OQ-115/CC-05 gates after PM/AD-32/33/28", so the deferral is bound to PM/AD-32 by decision. What stays open is that `PM-FR-8` (via `PMC-E1-S1.8`) and `PM-FR-3` (via `PLAT-E6-S6.6`, S16) both depend on it and carry that dependency only as prose in `notes`. `PM-FR-8` therefore reads as fully `specified` and `PMC-E1-S1.8` can be pulled into a sprint with its dependency intact. Needs an FR→FR dependency edge or a story-level gate; not applied, because it changes the model's schema.
- (open) `platform-capabilities/epics.md:33` carries a stale "REGISTRATION GAP (must be closed before this slice enters a sprint)" block. All three of its conditions are now false: `prd.md:70` lists `PMC-E*`, and the model has both `namespace_rules.platform_capabilities_epics` and a `PMC` `source_slices` entry. `PMC-E1` (`PM-FR-8`, `10`, `11`) stands behind an already-closed blocker. Not edited — it is the PMC slice's own text.
- (open) Slice maturity is disclosed unevenly and 17 of 42 requirements have their entire story coverage in a non-final slice. `feedback/epics.md` is `status: draft` but its §13 row (`PM-FR-35`) says plain `specified`, unlike the cds/mentorship/profile-sharing rows which do disclose draft. `user-management/epics.md` has no `status:` field at all and `stepsCompleted: [1, 2, 3]` — step 4 never ran — while solely owning `PM-FR-4`, `7`, `29`, `40`, `42` and co-owning `12`, `13`, `28`, `41`.
- (open) Two identifier collisions in `user-management/epics.md`, both against the §0.2 rule this README also states. Its local NFR renumbering of §7 collides with the canonical `prd.md` §8 IDs with an exact inversion at NFR-4 (local: access-control correctness; canonical: graceful degradation), so the story line "This makes NFR-4 concrete and testable" reads backwards to a cross-referencing reader — `mentorship/epics.md` avoids this correctly with `NFR-M*`. And ~104 bare `FR-N:` requirement headings collide with `PM-FR-N`, most sharply `FR-16`/`FR-17` against the Delivery Manager and Project Manager dashboards. The other ten slices use bare `FR-N` only inside a provenance citation attached to a namespaced ID.
- (open) `verify-coverage.py` check 7 cannot see any top-level section of `docs/project-requirements.md`: its heading regex `^#{2,4}\s+(\d+(?:\.\d+)*)\s+` cannot match the `.` in `## 7. Non-functional requirements`. 31 dotted subsections are checked; §1, §2, §3, §6, §7, §8, §9, §10 are not, while the check prints "every normative section has >=1 requirement". Deliberately NOT fixed: adding `\.?` makes it FAIL at once, because §7 and §8 genuinely carry no `PM-FR-*` and §8 Engineering process requirements is `[NORMATIVE]` and self-describes as graded. That is a product-scope decision, not a regex edit. The limitation is now stated in the script's docstring.
- (open) No NFR is in the coverage model at all. `NFR-1`..`NFR-7` are asserted per-slice in prose with no rollup, so an NFR claimed by no slice is undetectable — `engagement/epics.md:127` explicitly declines NFR-3 for the risk dashboard and nothing records where NFR-3 does land.
- (open) UNREGISTERED WORK ON DISK as of 2026-09-03: a `bmad-create-epics-and-stories` re-entry targeting `PM-FR-5`, `PM-FR-6`, `PM-FR-9` produced `role-administration/epics.md` (`RA-E*`, `status: draft`, `stepsCompleted: [1, 2, 3]`, 2 epics, covering `PM-FR-6`) and `user-management/epics.md` Epic 6 + Epic 7 (covering `PM-FR-5` in two halves — storage/declared visibility, then anti-inference completion). Nothing is registered: `RA-E*` has no `namespace_rules` entry, no `source_slices` entry and no PRD §0.2 row, and both `PM-FR-5` and `PM-FR-6` still read `deferred` with `stories: []` and `epics: []`. `verify-coverage.py` check 3 FAILs on the unregistered slice — the check working on its first day. `PM-FR-9` still has no epic anywhere and remains the one open gap of the three. Registration is the CDS/PSH sequence (namespace_rules + source_slices + PRD §0.2 + §13) and the status flip is a coverage decision; neither applied.
- (event) Registration closed 2026-09-03, resolving the prior "UNREGISTERED WORK ON DISK" entry above. `RA-E*` added to `namespace_rules` and `source_slices` (role: `bounded-context-slice` — not `draft`, since `access-control` is already AD-5-confirmed and this file proposes no new context, unlike `CDS`/`PSH`). `global-fr-epic-story-coverage.yaml` rows updated: `PM-FR-6` `deferred`→`specified` (stories: `RA-E1-S1.1`–`S1.5`, `RA-E2-S2.1`; gate `OQ-PERM-01` carried on `RA-E2` only); `PM-FR-5` `deferred`→`specified` (stories: `UM-E6-S6.1`–`S6.3`, `UM-E7-S7.1`–`S7.2`; gate `AC-SECTION-MATRIX-01` carried on `UM-E7`); `PM-FR-9` `uncovered`→`specified` (stories: `PMC-E4-S4.1`–`S4.3`; gate `OQ-AC-EDIT`). `PM-FR-8`'s note updated to point `PMC-E1-S1.8`'s dependency at the now-`specified` `PM-FR-5`, transitively gated on `AC-SECTION-MATRIX-01` via `UM-E7` rather than on a stories-less row. PRD §0.2 gained a Role Administration row and `RA-E*` in the identifier bullet; §13's Access Control and Directory rows and the rollup were rewritten against the actual yaml statuses. `verify-coverage.py` check 3 should now pass on this slice; not re-run as part of this event.
- (decision) `PM-FR-5`'s two epics were placed in `user-management/epics.md` (as Epic 6/7) rather than a new `custom-fields/*` slice, after an explicit trade-off pass: `transition-debt.yaml` TD-12 already names `owner: user-management` for the `User.customFields` jsonb retirement, and AD-5 lists custom-fields in neither its five confirmed contexts nor its five *pending-confirmation* ones — unlike `CDS`/`PSH`, nobody has ever proposed it as a context. Folding in also let this pass retire the S16-visibility slice of `user-management/epics.md`'s own long-standing `FR-17` "deferred Profile Projection story" placeholder (aliased to both `PM-FR-4` and `PM-FR-5` in `namespace_rules`), which had a coverage-map row but no story body anywhere in the file. The S10/S11 colleague-narrowing, S7/S8 flag, and S1 derived-field-immutability slices of that same placeholder remain unwritten and stay tracked under `PM-FR-4`'s aliases — not closed by this pass, and not claimed as closed.
- (decision) `PM-FR-6`'s two epics were placed in a **new** `role-administration/epics.md` file rather than appended to `platform/epics.md`, even though the functional-roles-catalog SPEC states its code lives inside the same already-confirmed `access-control` bounded context that `PLAT-E1`–`E7` already decompose. Rationale: `platform/epics.md` never once mentions `PM-FR-6` or the permission catalog across its ~1500 lines — it decomposes the audience-*resolution* half of access-control (Phase-0 tiers, project-line, department walk, section matrix, shared-link policy), a materially different concern from runtime role/permission CRUD administration. Registered `bounded-context-slice`, not `draft`, since no new context is being proposed — only a new decomposition file for an already-confirmed one, the same relationship `CDS`/`PSH` do NOT have with their contexts.
- (decision) `RA-E1` (catalog engine) and `RA-E2` (admin screen) were split because §2.3 itself draws that exact boundary: a confirmed default role-to-permission matrix is required "before the roles admin screen is built," while AD-12's fail-closed rule means the engine needs no default-assignment decision at all to ship. `RA-E2` is gated on `OQ-PERM-01` (P1, PO-owned, still open) with **no downstream dependents of its own** — a cascading-failure pass traced this explicitly and found `OQ-PERM-01` does not propagate to `UM-E6`, `UM-E7`, or `PMC-E4`, all of which depend on `RA-E1` only. `RA-E1-S1.5` proposes appending `user-management:edit` and `mentorship:assign` to the bootstrap seed as a **named decision inside its own Stage-1 scenario document** (closing `OQ-AC-EDIT` once that document is independently approved per AD-1) rather than declaring the P1 blocker closed by this planning pass — `OQ-AC-EDIT` remains `status: open` in `blockers.yaml`, untouched here.
- (decision) `PM-FR-9` re-entered `platform-capabilities/epics.md` as Epic 4, reversing that slice's own original exclusion ("`PM-FR-9` — EXPERIENCE.md records No surface; prototype table is read-only"). The UX gap is not resolved by this reversal: `Story 4.2` specifies denial and conflict-state interactions directly, with no mock to draw from, flagged as an addition beyond the UX spine — the same treatment this slice already gave Story 1.7's export flow (UX-DR8). Epic 4 hard-depends on `role-administration` `RA-E1` (the `user-management:edit` key and a working `isAllowed`); Story 4.3 additionally depends on `user-management` `UM-E7`. `PM-FR-9` is recorded `specified`, not `implemented`, pending both.
