---
topic: Mentorship bounded-context PRD (People Management Platform)
updated: 2026-09-01T00:00
---

- (event) Created `prd-mentorship-2026-09-01/prd.md` — decomposes People Management PRD FR-32/33/34 (§4.12) and requirements §4.11 into the `mentorship` bounded context. Mirrors `prd-user-management-2026-08-20/prd.md` shape. `status: draft`, nothing approved.
- (decision) Context boundary per AD-17: `MentorshipPair` is a durable workflow record, never a `Relationship` row, never an access edge, never an audience-resolution input. Replaces the retired UM Story 4.2 (`POST /users/:id/relationships {type:'mentorship'}` model — `um-rel-04..06` RETIRED).
- (decision) 17 FRs (FR-M1..FR-M17). The `MentorshipPair` field set, the open-to-mentoring flag's owning aggregate and endpoint, status stored-vs-computed, and the S1/S13 inline read-export shape are architect hand-offs — this PRD describes behaviour and data-needed only. Spine Deferred "S13 flag endpoint unresolved" stays open.
- (decision) Closure-note visibility (FR-M10) is a matrix exception in the DEC-UM-001 pattern — reporting line + project line + PP only, narrower than the S13 RW cell; not mentor, not mentee, not colleague.
- (decision) Career events emitted via `user-management`'s application boundary in the same transaction as the pair mutation (AD-11); mentorship never writes `UserEvents`. Departure auto-close driven by the AD-20 executor calling this context's exported application service.
- (open) 7 open questions: OQ-M1 missing *assign and end mentorships* FR permission (same shape as `user-management:edit` gap); OQ-M2 who-may-assign PO-confirm (requirements line ~131); OQ-M3 S13 `canAccessSection` pending AC increment (stage-2 gate like career-timeline S9); OQ-M4 AD-20 executor / CC-06; OQ-M5 AD-11 career-event boundary (UM Epic 3 Story 3.1); OQ-M6 architect schema/aggregate/endpoint hand-offs; OQ-M7 closure + pool route names.
