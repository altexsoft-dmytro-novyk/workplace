---
topic: User Management domain PRD (People Management Platform)
updated: 2026-08-21T13:32
---

- (event) Run started; scoped to 'user-management' bounded context per docs/architecture/domain-driven-design.md (org facts: users, relationships (reports-to, project membership), projects, departments), distinct from access-control (policies engine) and other pending contexts
- (override) User corrected initial framing: drop timetracker sync / reports-to hierarchy from this PRD's scope for now — it was over-emphasized
- (decision) Department/project creation and assignment is HR-Admin-only and belongs to access-control/policies, not this PRD
- (decision) Scope narrowed to: User entity CRUD (profile-page self-service) + temporary magic-link signup/login, to be replaced later by SSO
- (decision) Concrete endpoint URLs / API contract = technical-how -> addendum.md now, formalized later in architecture/spec handoff; PRD captures capabilities and entity fields
- (decision) User entity: split fullName into firstName + lastName (was single field in S1 source, user wants split)
- (decision) User entity: birthdayDay/birthdayMonth replaced with single birthDate field (full date incl. year), overriding S1's 'day and month' framing -- stored full, display redaction of year for non-privileged audiences deferred to access-control policy layer
- (decision) User entity: currentProjectIds renamed to projectIds; departmentId confirmed as its own standalone field (not merged with anything)
- (decision) User entity: drop departmentId as a stored column too -- department membership deferred to policies/department-edge work, same reasoning as manager/projects
- (decision) User entity: companyStartDate renamed to companyJoinDate
- (decision) User entity finalized (pending only workEmail-as-login-identity confirmation): id, firstName, lastName, photo, position, country, city, workEmail, workPhone, birthDate, companyJoinDate, peoplePartnerId, ttId, customFields(jsonb), createdAt, createdBy, updatedAt, updatedBy. managerId/projectIds/departmentId/mentorId are derived, not stored, per AD-11 and pending Department/Mentorship modeling
- (event) Synced User entity into prd.md (Data Model section) and docs/architecture/database-schema.md (User table + erDiagram self-ref for peoplePartnerId)
- (decision) Bootstrap: first User created by seed script w/ HR Admin FR assigned directly (AD-12), not via registration flow
- (decision) Auth: passwordless magic-link is sole login mechanism (temp, pre-SSO); completing registration triggers same magic-link email as any login, no separate invite-link mechanism
- (decision) Department & Project CRUD confirmed OUT of scope for this PRD entirely -- HR-Admin/policy work
- (assumption) Open: who submits registration form for employee #2+ -- HR Admin on behalf of new hire, or employee self-registers with work email. Blocks Create semantics and whether a status field (invited/active/deactivated) is needed
- (note) User raised: add a UserEvents table + join field on User -- purpose/event-types not yet clarified
- (decision) career-timeline folded into user-management as UserEvents table (not spun up as separate context) -- domain-driven-design.md updated to reflect this, removed from pending list
- (decision) UserEvents schema: id, userId FK, type enum (8 tracked types per S4.9), eventDate, details jsonb, source system|manual, deletedAt (soft delete, ASSUMPTION), createdAt/By, updatedAt/By. Join is UserEvents.userId -> User.id, no new field on User
- (event) Synced UserEvents into prd.md, database-schema.md, and domain-driven-design.md
- (decision) UserEvents placement confirmed provisional, not final: kept in user-management for now (sole consumer = profile page, nothing reuses independently); revisit as own context if the automated cross-context write mechanism grows. Annotated in domain-driven-design.md and prd.md so it's not lost. No schema changes.
- (decision) Strengthened UserEvents-stays-in-user-management rationale: primary reason is AD-2 bars cross-context table joins, and profile page needs a plain User<->UserEvents join; 'nothing reuses it yet' is secondary. Updated in both docs
- (decision) Added User.isActive boolean (default true) for soft-delete/deactivation. May later need to become a fuller status enum depending on FR-4 answer (invited vs active vs deactivated), noted as future dependency not yet resolved
- (override) Corrected: 'invited/active/deactivated' status was my own speculation, not sourced from project-requirements.md. User caught this -- the doc's only statuses are S4 employment status, S6 risk level, S10 leave status, S13 mentorship status, all unrelated different tables. PRD wording fixed to not overclaim sourcing
- (override) peoplePartnerId removed from User entity -- it's an access role per S2.1 ('arises from assignment', same as Manager), so it's a policy attachment under AD-7, not a User column. Earlier 'flat one-hop, so it's fine as a column' justification was wrong; contradicted User's own 'no access-derived field' rule already written into database-schema.md. Removed from prd.md, database-schema.md schema block, and erDiagram self-ref
- (override) updatedAt/updatedBy dropped from User -- no concrete consumer named, user's 10yr experience: added such fields once, used once, in one table. New convention logged in database-schema.md Conventions: audit columns only per-table with a concrete named consumer, not speculatively. Kept on UserEvents (has a real consumer: S4.9 PP/UM correcting inferred events) as a deliberate contrast case
- (override) updatedAt/updatedBy also dropped from UserEvents -- an event is an immutable fact, not a mutable record. S4.9 'edit' is modeled as soft-delete-and-append-corrected-entry, not in-place update. Both docs updated
- (decision) Both audit-column rules (no speculative updatedAt/updatedBy; event tables are immutable) codified as team rules in services/backend/.claude/rules/nest-prisma.md (auto-loads for anyone touching prisma/schema.prisma), not just noted in this PRD's docs
- (decision) UserEvents.type changed from enforced enum to plain string, with the 8 tracked types documented as examples only (not DB CHECK-constrained). User edited database-schema.md directly; synced into prd.md
- (event) prd.md updated by spec-user-management-test-cases run: FR-4 resolved (HR Admin creates on behalf, isActive alone is sufficient) and workEmail's 'pending confirmation as login identity' note resolved (FR-2 already committed to it) -- both confirmed directly by the user during that run
- (decision) 2026-09-01 reconcile pass (post-v1.5, Kernel MVP headless): added FR-16 Access Control adoption (real facade via ACCESS_CONTROL_PORT, interim adapter removed AD-21, GET /users/:id allowed for Self/reporting/PP, colleague whole-profile read denied until projection) and FR-17 Profile Projection as its own deliverable; authoritative contract is spec-user-management-access-control-adoption/SPEC.md. FR-16/17 continue past epics.md's derived FR-15 to avoid colliding with the derived FR-7..FR-15 numbering
- (decision) FR-9 refined: PATCH /users/:id and PUT photo require the functional permission AND write S1 section access (§2.2 dual gate); photo Self-only unless Product widens; manager/PP/department never writable through S1 (§3.2 fn 1)
- (decision) Story 1.1 / seed scope folds kernel reality: DEC-UM-007 is canonical-at-write (seed writer normalizes and stores normalized, DB index remains raw — writer-side guarantee, DB functional index deferred); DEC-UM-009 import reuses the ACM-0 root User id; ACM-0 (npm run db:seed) creates the normalized active root User before import
- (decision) 2026-09-01: the seed/import source is the concrete file `docs/Accounts_template.csv` — semicolon-delimited TT export, header FirstName;LastName;Email;Birthday;PositionId;PositionName;RegistrationDate;DepartmentId;DepartmentName;DismissedDate;IsDismissed;EmployeeType;TimeZone;CountryId;CountryCode;CountryName;CountryStateId;CountryStateName. No employee-id column → import keyed by normalized Email; ttId left null (no source column). Column→User mapping and OPEN items (PositionId, Department*, Country*, EmployeeType→S4, IsDismissed/DismissedDate→EmploymentStatus §4.16, TimeZone) recorded in the seed scenarios + spec-1-1. A CSV row matching ROOT_WORK_EMAIL updates the ACM-0 root User in place (DEC-UM-009).
- (open) CC-07 added to Open Questions (AD-19 Journal gate) — PP write path blocked on CC-04 AND CC-07; missing user-management:edit permission decision (option a/b) referenced, not defaulted
- (decision) 2026-09-01 human product decision: colleague `GET /users/:id` returns the S1 identity card in Story 0.1 (§3.2 S1 = R for Colleague; every active authenticated viewer is at least a Colleague). FR-16 updated: 200 S1 card for any non-empty audience (self/reporting/pp/colleague), empty audience → leak-free 404; S1-card DTO (drops ttId/isActive/customFields/createdAt/createdBy) replaces toUserResponse's whole-row spread. Two-state colleague rule and adoption story UMAC-3 removed. FR-17 shrinks to the S10/S11/S16 colleague views on their own surfaces, S7/S8 flags, S1 derived-field immutability — decoupled from GET /users/:id.
- (event) 2026-09-01: the "Mentorship workflow and pair persistence — future mentorship bounded context" Scope line now has a home — `prd-mentorship-2026-09-01/` + `planning-artifacts/mentorship/epics.md` (both `draft`). UM still receives only `mentorship_start`/`mentorship_end` via Epic 3 Story 3.1's application boundary. No UM PRD scope change.
- (decision) 2026-09-01 (same day, later): empty-audience denial on `GET /users/:id` is **`403`**, not leak-free `404` — internal directory, id existence is not sensitive. FR-16 body corrected to match the adoption SPEC / `stories.yaml` / approved `umac-05`. The earlier memlog line that recorded `404` is superseded.
