---
scope: People Management Platform (Iteration 2 / requirements v1.5 — greenfield, required features only) — full-system architecture spine: BE architecture, two-dimensional role/access model, custom fields, dashboard engine, lifecycle, integrations, quality-gate/testing strategy
purpose: Binding contract for parallel human + AI development across features. Canonical decisions (AD list) live in the spine; rendered as a docs/architecture/ rule-file set (DDD, TDD/testing, access-control, etc.) that every developer and agent reads as primary strategy before building.
altitude: initiative
updated: 2026-09-02T11:04
---

- (decision) Quality gate is a three-stage pipeline per feature, mandatory for every developer: (1) author a written scenario/test-case document under /docs/test-cases/ (line-by-line: actor, request, expected outcome, traced to a requirements section) -> approved by a developer; (2) translate the approved scenario into an E2E test -> approved by a developer; (3) only then write production code until that E2E test passes. Binds: every feature's workflow, start to finish. Prevents: test code or production code being written against an un-agreed scenario, and pushes misunderstanding-catching to the cheapest stage (prose) before either test code or implementation exists.
- (decision) Architecture paradigm is hexagonal (ports & adapters). Binds: domain logic (access resolution, functional-role permissions, resourcing state machine) has no direct dependency on HTTP, persistence, or external integrations. Rule: timetracker and PeopleForce are outbound ports with adapter implementations; inbound HTTP is one adapter into the domain core. Prevents: domain logic coupling to transport/third-party SDKs, which would block fast + fake-backed E2E tests and violate the 'integration failures degrade gracefully' NFR.
- (decision) 'E2E' for stage 2 of the gate = real HTTP request -> real router -> real access-resolution -> real test database. Outbound ports (timetracker, PeopleForce) are bound via DI token to fixture-backed fakes in test configuration, never live third parties. Binds: test infrastructure and CI. Prevents: the mandatory E2E suite being slow, flaky, or blocked by third-party availability, while still satisfying the NFR to assert what the API actually returns.
- (decision) The gate is enforced per feature-owner, not centralized: whoever owns a feature drives its own scenario doc -> E2E test -> code sequence, with approvals happening asynchronously (a developer reviews and signs off, not a blocking handoff to a single dedicated author). Binds: team workflow / branch process. Prevents: any single role or person becoming a serialization point, which would violate the Section 8.2 mandatory-parallelism rule.
- (direction) Deliverable shape has two layers: the BMad ARCHITECTURE-SPINE.md (stable AD IDs) is the canonical decision record; a rendered docs/architecture/ directory holds a per-topic rule-file set (e.g. domain-driven-design.md, testing-strategy.md, access-control.md, custom-fields.md, dashboards.md) for team + agent consumption, each citing spine AD IDs so they can't silently drift from the source of truth.
- (question) How is it guaranteed every AI agent reads docs/architecture/ rule files as primary strategy before acting, not just on request? Candidate answer: wire them into project-context.md / AGENTS.md via the bmad-project-context skill so they're auto-loaded as persistent facts on every agent activation. To be resolved after the spine is finalized.
- (version) Stack pinned: NestJS 11.x (v12 not GA, targeted Q3 2026 - do not adopt until stable), Prisma ORM 7.x (Rust-free TS runtime), PostgreSQL. Verified current via web 2026-08-19. NestJS DI container is the mechanism for port/adapter token swapping required by the E2E test gate.
- (decision) Bounded contexts from the start, each with application/(actions,controllers,dtos) + domain/(interfaces,services,entities) + infrastructure/(repositories,adapters) internal layout. Confirmed contexts so far: user-management (org facts: users, relationships, projects, departments), access-control (policies engine covering BOTH access roles and functional roles), dashboards; further contexts (profile, resourcing, CDS, mentorship, risk, feedback, campaigns, career-timeline) to be confirmed. Binds: repo structure and team decomposition for parallel work. Prevents: contexts inventing incompatible internal layouts.
- (decision) Vocabulary invariant: entities expose assignFunctionalRole/revokeFunctionalRole only. NO method anywhere named grant/revoke access-role; access roles are never assigned, only computed. Prevents: Section-2 dimension-collapse leaking into code via naming.
- (decision) Access control = AWS-style policy attachment model, one engine for both role dimensions. Tables: Policies {id uuidv7, operator, targetType 'project'|'department'|'user'|..., targetId uuid (polymorphic, NO db FK), targetRole, type 'AR'|'FR', managedBy 'sync'|'admin' provenance}; Permissions {id uuidv7, title, description} (granular feature list per 2.3); UserPolicies {userId, policyId} attachment join (one policy shareable across many users). Managerial facts (user Y manages project X) ARE policy attachments - Project carries NO pmUserId/dmUserId fields and NO users[] array. FR policies runtime-editable via HR Admin UI per 2.3; AR policies seeded, no UI. Prevents: role logic duplicated per context; schema changes when new assignable capabilities (e.g. trainings) arrive.
- (decision) Operator whitelist: '==' and 'IN' only at start. '!=' BARRED from AR/tier-granting rules: negation is satisfied by missing data, inverting fail-closed (user on no projects matches every != condition). If ever needed, != is FR-scoping/deny-only. Exotic/app-side operators barred from the tier-resolution path entirely - AR evaluation must remain expressible as indexed SQL joins.
- (decision) AccessControl facade is the single authorization entry point for all contexts; direct role-flag checks (isManager||isPP style) forbidden everywhere. Two call shapes: isAllowed(feature) for FR capability checks (no target), and tier/section checks which ALWAYS take target employee scope. Combines access-policies-projector + functional-policies-projector.
- (decision) Tier resolution is BULK and live: one recursive SQL query (WITH RECURSIVE) resolves viewer tier (Self/Manager-line/PP/Colleague) to all requested employees in one round trip, walking reports-to edges AND manages-project/department policy attachments as ONE transitive graph (compound chains like reports-to THEN project-manager-of must resolve). Policies join Relationship via indexed uuid columns (targetId=projectId), no FK needed. Section visibility = tier map joined against seeded tier->section mapping. Profile single-target check = degenerate case of same query. Derived access decisions NEVER stored. Polymorphic secondary lookups (second DB call resolving targetIds against the table targetType names) allowed for feature/audience resolution but BARRED from the tier hot path - and when a resolution spans both calls (policy query + per-targetType lookup) the two MUST run inside one transaction, so the rows resolved are consistent with the policies that referenced them. Prevents: per-row walks breaking the 500-record/2s NFR; stale grants; torn reads between policy and target tables; broken compound chains.
- (decision) Org-fact schema: Relationship {id uuidv7, userId FK, type 'direct'|'project', reportsToUserId FK nullable, projectId FK nullable} with CHECK constraint per type - one table, but edge types never share a polymorphic target column. No roleOnProject field: managerial semantics live in policy attachments. At most one active reportsTo edge per user (tree, not graph). Project membership derived solely from Relationship rows. Orphaned policy rows (dangling targetId) fail closed on AR path (join yields zero members -> zero grants); periodic consistency sweep for hygiene.
- (decision) Fail-closed bootstrap: NO superuser derived from data shape - empty reportsTo grants nothing; orphaned data yields LESS access, never more. Top-of-tree gets Manager access via the normal transitive walk, no engine special case. Seed script creates first user with explicitly assigned HR Admin functional role; all admin power flows through ordinary FR assignment, delegable via UI per 2.3. Prevents: privilege escalation via data glitches; hierarchy position granting functional permissions.
- (decision) Timetracker integration deferred (API still draft): User.ttId external-identity field added now per Section 6 identity note; policy rows written by seed/admin meanwhile. BINDING when sync lands: timetracker sync is sole writer of sync-provenance managerial policy rows, replacing a user's rows transactionally (no window of old+new coexisting) - 2.1 non-sticky access depends on it. managedBy provenance column present from day one.
- (question) OPEN (user checking, expected ~2026-08-20): semantics of non-manager project assignment - can a user hold a project (and dependent users) without being manager/PP (info-sec training case)? Affects Department modeling and possible new targetRole values.
- (question) OPEN: is Profile its own bounded context (assembling S1-S16 from owning contexts at application layer) or folded into user-management? Asked, not yet answered.
- (direction) NEW SCOPE vs requirements doc: Departments added as resource-hierarchy level above Projects (manager-of-manager sees all nested); treated as generalizing 2.1 relation 2 over a resource tree, NOT a third relation. Flag to requirements owner - currently lives only in this conversation.
- (constraint) This is ITERATION 1 for this team: greenfield, development only starting, no prior codebase. Scope = REQUIRED features only (NORMATIVE + DESIGN FREEDOM sections of docs/project-requirements.md); GOOD TO HAVE sections (notifications 4.13, analytics 4.14) and any speculative extensions are out of scope. The requirements doc's own 'Iteration 2' title refers to the bootcamp assignment's numbering, not this team's delivery state.
- (event) Interim render 2026-08-19: ARCHITECTURE-SPINE.md written (status: draft, AD-1..AD-13, Deferred list) + docs/architecture/ rule-file set rendered (README, domain-driven-design, testing-strategy, access-control, database-schema, nestjs-di-tokens, custom-fields stub, dashboards stub). Custom fields and dashboards deliberately left as do-not-build stubs pending topics 4-5. Reviewer gate NOT yet run - spine stays draft until topics 4-5 close and Finalize runs.
- (decision) Router tree convention (AD-14, new): no generic /sections/:sN wrapper anywhere - it was test-doc-only vocabulary, never a real route. Plain resource CRUD: GET/PATCH /users/:id (S1 identity IS the User entity's own fields, no sub-path), DELETE /users/:id soft-deletes (isActive=false). Single-value sub-resource: PUT /users/:id/photo (settles the PUT-vs-PATCH clash between suites in favor of PUT - full-replace semantics, multipart upload). Owned collections (real row identity) nest as plural resources: /users/:id/events (settles events-vs-timeline-events clash in favor of events, matches UserEvents), /documents, /notes, /feedbacks, /assessments, /idps, /risks, /leaves, /request-history - each with GET (list)/POST(create)/GET,PATCH,DELETE :itemId. Field-groups with no row identity (S2/S3/S4/S16 pending contexts) nest as singular resources with GET/PATCH only, no item id: /users/:id/personal-contacts, /emergency-contacts, /employment, /custom-fields. Binds: every controller across every context, and test-case authors translating placeholder URLs. Prevents: incompatible endpoint shapes emerging per feature-owner - already happened (photo verb, events naming) before this AD existed.
- (decision) Generic attachment endpoints (AD-14 cont.): mirrors AD-7's one-policy-engine and AD-11's one-Relationship-table decisions at the HTTP layer instead of inventing bespoke per-feature endpoints. POST /users/:id/relationships {type:'direct'|'project'|'mentor', targetId} creates the Relationship row (reports-to, project membership, or mentor pairing); DELETE /users/:id/relationships/:id revokes it. POST /users/:id/policies {type:'AR'|'FR', targetType, targetId, targetRole} creates a Policies+UserPolicies attachment (PP assignment, project/department manager assignment, functional role grant to a user); DELETE /users/:id/policies/:id revokes. Role/permission CATALOG management (defining a functional role and its permission set, not attaching a user to one) is cross-user and goes top-level: POST /roles, PATCH /roles/:id/permissions - never nested under /users. mentorship-pairs as a standalone top-level resource is removed (see next entry - mentor folded into Relationship); action-items, campaigns, resourcing/requests, share-links stay top-level, unchanged, already consistent. Prevents: roles nested under /users (confusing - a role isn't scoped to one user) and each new assignable fact reinventing its own endpoint shape instead of reusing the generic engine.
- (decision) AD-11 amended: Relationship gains type='mentor'. User-decided (explicit call: 'I don't see any reason why we can't simply add another type of relationship mentor' - reopens the PRD's prior 'mentor is its own future entity' note). Column reportsToUserId renamed to relatedUserId since it now serves two semantically different edge kinds (reports-to superior, mentor-of) that both target User - same target table, so sharing the column still satisfies AD-11's 'each target is a real indexed FK per target table' rule; only the name was misleading post-rename. CHECK constraint gains a third arm: (type='mentor' AND relatedUserId IS NOT NULL AND projectId IS NULL). The existing UNIQUE-one-active-reportsTo-edge constraint stays scoped to type='direct' only - NOT extended to mentor (no source for a one-mentor-at-a-time rule; left unconstrained, flagged as open question below). Start/end dates for a mentor pairing are NOT added as new Relationship columns (would be speculative - see no-speculative-fields precedent on User/UserEvents) - the existing, already-reserved UserEvents types mentorship_start/mentorship_end already capture that history; attach/detach on /users/:id/relationships (type=mentor) fires the matching UserEvents row. This also deletes the need for a standalone mentorship-pairs resource.
- (question) OPEN, not resolved today: (1) does a 'mentor' Relationship edge participate in AD-10's tier-resolution walk / grant any access tier? Default until wired in: no, per fail-closed (AD-12) - presence of an edge type the walk doesn't know about contributes nothing. (2) Is exactly one active mentor per user required (uniqueness like reportsTo)? Not sourced from project-requirements.md, left unconstrained by design pending confirmation.
- (decision) Section addressing: human-readable kebab-case names, derived from the existing matrix test-case folder names (already chosen by each section's feature-owner, e.g. s07-management-notes -> notes, s10-leaves -> leaves) rather than sNN numeric ids. sNN survives only as an internal cross-reference in the tier-to-section mapping table (access-control.md) for traceability to project-requirements.md's Sec3.2 numbering - never in a URL.
- (decision) Propagation: normalize the inputURL lines (and any prose mentioning old routes) across all ~155 existing test-case files now, both suites - user's explicit call, overriding the suites' own READMEs which currently say endpoints are placeholder vocabulary deferred to stage-2. Delegated to a subagent using docs/architecture/api-conventions.md's mapping table as the single source of truth; folder/file names and scenario prose besides the URL lines are untouched.
- (event) 2026-08-22 session: amended spine (AD-14 new, AD-11 amended) + rendered docs/architecture/api-conventions.md (new), database-schema.md, domain-driven-design.md, access-control.md, README.md index; updated prd-user-management.md's mentor notes to match. lint_spine.py clean (0 findings). Dispatched Reviewer Gate (web-verify + adversarial-incompatibility lenses, per workflow.finalize_reviewers) as background subagents writing to reviews/review-*.md, plus a background subagent normalizing inputURL lines across all ~155 existing test-case files (both suites) + their two READMEs to the new convention, per user's explicit choice to propagate now rather than defer to stage-2. Spine stays status:draft — unrelated Deferred items (custom-fields, dashboards, department edges, operational envelope) still open.
- (constraint) Web-verify reviewer (2026-08-22) found and this session fixed two real gaps, both applied directly (no re-ask needed, mechanical/low-risk): (1) AD-11's 3-armed CHECK and partial-unique-on-reportsTo have no schema.prisma representation in Prisma 7.9.1 (no @@check attribute ever; partial index 'where' is preview-only via partialIndexes, not enabled in services/backend) - documented in database-schema.md as a required hand-authored raw-SQL migration step. (2) AD-14's /users/export sits at the same depth as /users/:id - NestJS/Express route matching is declaration-order-sensitive and a :id handler declared first silently swallows /users/export. Fixed by adding an explicit ordering rule to api-conventions.md (literal siblings before :id in every controller). Stack table itself (NestJS 11.2.1, Prisma 7.9.1, PG18, Node LTS) re-verified current as of 2026-08-22, no drift since 2026-08-19. Full review: reviews/review-web-verify.md.
- (constraint) Adversarial reviewer (2026-08-22) found 5 findings, all closed directly (clear fixes matching established precedent, no new business decision required - not re-asked to the user): (1) shape2-vs-shape3 boundary had no mechanical test - added one to AD-14 (independently creatable/deletable members = collection; fixed one-per-user cardinality = field-group) + made PATCH full-replace uniform across field-groups + flagged custom-fields router-shape-before-storage-decision sequencing hazard. (2) AD-10's text read as if manages-department policies were already live while Deferred says department modeling is pending - real spine-internal contradiction, not introduced today. Resolved by making AD-10 explicit that department targetType is schema-only, not yet honored by the walk (same fail-closed treatment as unwired mentor), mirrored into access-control.md and database-schema.md. (3) database-schema.md's own phrasing ('this is what lets WITH RECURSIVE walk the graph') read as endorsing mentor participating in the tier walk via the shared relatedUserId column - reworded, and AD-10/access-control.md now state explicitly the walk join filters type='direct' only, project and mentor rows never read by that query. (4) Relationship used 'active' language with no backing status column and undecided DELETE semantics - pinned as hard-delete, no deletedAt/isActive, consistent with feedback-no-speculative-fields (no manager_change UserEvents type added either, same reasoning, no named consumer). (5) UserEvents write mechanism (sync vs async) was named but unspecified - pinned as synchronous, same-transaction, explicit call from the use-case, no event bus in iteration 1, matching AD-10's existing one-transaction precedent. Two lower-priority watch-items added to spine Deferred: resourcing's future Relationship write path must go through user-management's application layer (AD-2), and /roles catalog request/response shapes are unspecified pending its own AD-1 doc. Full review: reviews/review-adversarial.md. lint_spine.py re-run clean after all fixes.
- (event) Propagation subagent completed: 152 test-case files + 2 suite READMEs rewritten to the new router convention (303 inputURL occurrences reviewed, 175 needed no change). Reviewed its self-flagged judgment calls and fixed 3 real defects it left behind: (1) sf-06's 's99 nonexistent section' probe had been mapped to a truly unrouted path, which would hit the framework's default 404 instead of the AccessControl facade being tested - retargeted to a nonexistent item id inside the same forbidden collection so it still exercises the right code path. (2) 4 users/roles/ and fail-closed files (assign-role-success, assign-role-without-permission, fc-03) kept a stale {userId:...} request body left over from the old role-slug-in-path shape after the URL itself was correctly rewritten to /users/:id/policies - fixed bodies to {type:'FR', targetType:'user', targetId, targetRole} matching AD-14's documented shape and AD-7's existing targetType enum (which already lists 'user'). (3) 4 s12-cds read files assert a single GET /users/alice/assessments returns matrix-link+assessments+IDP data together, which no longer holds now sections/:sN is gone and cds's real schema/endpoints are still pending (Deferred) - added an inline route-note flag to each rather than inventing cds's unbuilt schema. Also closed 3 remaining stale 'endpoints are placeholder' claims the propagation task's scope didn't cover: docs/test-cases/README.md (shared top-level doc), spec-user-management-test-cases/SPEC.md, spec-access-control-test-cases/SPEC.md - all now point to api-conventions.md/AD-14 as bound, not placeholder. lint_spine.py clean. Router-tree work for this session is complete.
- (decision) User-directed rename (2026-08-22, post-Reviewer-Gate): reverted the reportsToUserId->relatedUserId rename from earlier this session. Reasoning given: reportsToUserId's directional naming (userId points 'up' to the other party) should be preserved since mentorship has the same shape (userId=mentee, reportsToUserId=mentor) - a generic name like relatedUserId loses that. Type value renamed 'mentor'->'mentorship' instead (reads better as an enum value alongside 'direct'/'project'). Applied across ARCHITECTURE-SPINE.md (AD-10, AD-11, AD-14, Deferred), database-schema.md, access-control.md, domain-driven-design.md, api-conventions.md, prd-user-management.md, and the two s13-mentorship test-case files with POST bodies (pp-create-pair.md, manager-create-pair.md - also fixed their bodies from a stale {mentor,mentee} shape to the documented {type,targetId} shape, another leftover the propagation subagent hadn't caught since it only touched inputURL lines). Also flagged (not resolved - out of scope for a naming pass) a real tension surfaced while touching manager-end-pair.md: that scenario's feedback-gated pair-ending and 'kept in history on both profiles' exceed the bare hard-deleted Relationship fact + UserEvents-only-history model decided earlier this session - richer mentorship workflow is still the pending future mentorship context (spine Deferred), noted inline in that file rather than resolved. lint_spine.py clean after all renames; full repo sweep for stray relatedUserId/bare-mentor-type confirmed none left.
- (event) 2026-08-24..26 Epic 1 (Employee Record Lifecycle) implementation surfaced two real spine gaps, both caught in human code review, not self-caught: (1) application/actions injecting ports directly (fixed in-flight, logged as AD-2's 2026-08-26 extension in the spine file itself, but that edit was never memlog'd until now); (2) a FakePhotoStorageAdapter built for Story 1.3's own photo-upload deliverable — the story's whole acceptance criterion faked instead of built. Both were corrected in code before commit c42de7d: actions now call domain/services/ only, and a real src/storage/ module (ObjectStoragePort + S3StorageAdapter) was built and wired for real; the only fake left in the codebase is MagicLinkDispatcherFake, which is legitimately Epic 2's unbuilt dependency. User's explicit ask afterward: turn this incident into a binding, stricter, LLM-legible rule so a future dev agent can't rationalize the same shortcut — fakes are for another story/epic/context's not-yet-built dependency ONLY, never this story's own deliverable; if a needed real dependency's technology choice is still open (e.g. which object-storage provider), the agent must stop and ask rather than assume or fake, then build the real supporting module once told.
- (decision) New AD-15 — Fakes/mocks are scoped to out-of-scope dependencies only, never this unit's own deliverable. Binds: every developer and dev agent, every context. Prevents: a story/epic being marked done by a green test against a fake standing in for its own acceptance criterion (concretely happened: FakePhotoStorageAdapter for Story 1.3, whose entire deliverable IS photo storage). Rule: before adding any Fake/Mock/Stub/interim adapter, name which story/epic/bounded-context owns building the real thing. If it's this unit's own current deliverable -> build it for real, following the hexagonal pattern (port in domain/interfaces, real adapter in infrastructure/, wired through domain/services per AD-2) -- even if that means standing up a new shared module (precedent: src/storage/ with ObjectStoragePort + S3StorageAdapter, LocalStack for local/CI, no adapter-level fake at all since prod never runs tests). If the real implementation requires a technology choice that is genuinely still open (Deferred in the spine, or simply never decided) -- the agent must STOP and ask the architect/user which one, not assume a default and not fake around the gap; once answered, implement the real supporting domain/module before the story can be considered done. Legitimate fakes only bind outbound ports whose real adapter belongs to a different, not-yet-built story/epic/context (precedent: MagicLinkDispatcherFake, Epic 2's dependency) -- these are AD-3's E2E-fake category, not a shortcut on this unit's own scope. A PR/story is not done if any of its own acceptance criteria is satisfied by a fake.
- (decision) AD-1 extended (stricter, per feedback-ad1-gate-enforcement incident from Story 1.1) -- the three-stage gate requires a genuine HUMAN checkpoint between stages, not code that happens to satisfy the stages' ordering. Prevents: a single agent dispatch writing scenario-approval + E2E tests + production code in one uninterrupted run, then asserting in its own report that it 'reviewed and approved' its own prior stage -- a documented real incident. Rule addition: no dev agent may self-certify a gate stage; each of the two approval points (post-scenario-doc, post-E2E-test) requires the agent to STOP, surface the actual artifact (the scenario doc text, or the test file content) to a human, and wait for an explicit approval before proceeding -- never proceed on the strength of the agent's own review of its own prior output, regardless of time/token pressure or a generic workflow skill's default cadence.
- (event) 2026-08-26 update: distilled AD-1 extension (no self-certification of gate stages) and new AD-15 (fakes/mocks scoped to out-of-scope dependencies only, never the current unit's own deliverable) into ARCHITECTURE-SPINE.md; propagated to rendered docs/architecture/testing-strategy.md (new no-self-certification + done-means-real sections), domain-driven-design.md (new fakes/mocks scope-test section citing the real src/storage/ and MagicLinkDispatcherFake precedents already in the backend codebase), and README.md (non-negotiables list + file-coverage table). lint_spine.py clean (0 findings). Self-run adversarial-incompatibility check found no conflict with AD-2/AD-3/AD-4. No new technology named, so no web-verify pass needed. Spine stays status:draft — unrelated Deferred items (custom-fields, dashboards, department edges, operational envelope) still open.
- (decision) P-4 / v1.5: AD-10 amended — audience resolution splits Reporting line (reports-to + department management) from Project line (PM/DM via project assignment + project-management chain only). Removed unified Manager-line graph and compound reporting→project chains. Full-profile grant (§2.4) and journal scope (§3.4) bound. Revocation: platform relations next-request; project-derived ≤15m; timetracker outage withdraws project access after 4h. Department management specified as reporting-line relation; walk phasing unchanged (fail-closed until live).
- (event) P-4 complete: AD-10 amended + docs/architecture/access-control.md updated for v1.5 Reporting/Project line split, department-as-reporting relation (phased), full-profile grant, journal scope, revocation timing. lint_spine pending.
- (event) P-4.1 follow-up complete: access-control.md hardened through adversarial/edge-case review passes — dual-dimension gate, all §3.2 audiences, matrix exceptions, shared-link/resourcing overlays, departure revocation, open product decisions table (5 items), campaign-author scoped access (§3.3.7), request-scoped expected compensation (§4.7). Critical-only re-review clean.
- (event) 2026-08-28 v1.5 exhaustive architecture alignment: added AD-16 (seeded population and employment lifecycle), AD-17 (durable mentorship pairs), and AD-18 (fixed requirements bind deferred designs); retired the employee-create/delete API and historical registration decisions; corrected authenticated share consumption; bound dashboard risk/Unassigned facts, employment status, departure preconditions, department/CDS/resourcing constraints, and the timetracker events-vs-state discovery gate. Historical entries above remain as chronology and are superseded where they conflict with AD-16..18. DEC-01..05 remain unresolved; INT-02 remains an explicit provider-contract follow-up.
- (decision) CC-04 storage direction approved in coaching: People Partner assignment is an organisational fact stored as Relationship type='people_partner', directed employee userId -> PP reportsToUserId. AccessControl derives the PP audience from this edge; the section 3.2 access matrix/policy projection separately decides that audience's R/RW/— access. Binds: user-management relationship schema/API, access-control PP audience resolution, organisational-change journal. Prevents: conflating the fact that a person is somebody's PP with the permissions granted to the derived PP audience, and avoids using a generic policy row as the source of organisational truth. Existing database-schema.md/api-conventions.md statements that PP assignment is a policy attachment are superseded pending final reconciliation.
- (decision) CC-04 cardinality approved: each employee has zero or one active People Partner; one People Partner may partner any number of employees. Enforce the employee-side maximum at the database level with a partial unique constraint scoped to Relationship type='people_partner'. Creating a second active PP edge without first performing the approved replacement/removal contract returns 409. Binds: schema constraints, repository concurrency behavior, PP assignment API scenarios. Prevents: ambiguous simultaneous PP audiences and last-write-wins races.
- (decision) CC-04 write contract approved: PUT /users/:employeeId/relationships/people-partner atomically creates or replaces the employee's zero-or-one PP edge and writes one organisational journal record carrying before/after assignment in the same transaction. The request includes targetId and expectedCurrentTargetId (null for first assignment); a mismatched current assignment returns 409 with no mutation, preventing concurrent last-write-wins. Self-assignment and unauthorized writes are rejected before mutation. DELETE /users/:employeeId/relationships/people-partner explicitly removes the assignment and journals old->none atomically, using the same expected-current precondition. PP access is recomputed from committed Relationship state on the next request. This intentional fixed-cardinality relationship command differs from direct-manager DELETE-then-POST because UM Story 4.2 requires atomic before/after replacement. Binds: AD-14 relationship route family, CC-04 implementation scenarios, access invalidation, journal transaction boundary. Prevents: a gap with no PP during reassignment, split journal history, and silent concurrent overwrite.
- (decision) CC-06 scheduled-state model approved: represent a recorded future departure as a dedicated durable Departure aggregate, separate from EmploymentStatus. Departure owns userId, effective date/time semantics (to be fixed next), required reason, execution state, attempt/diagnostic metadata, and idempotency identity. EmploymentStatus remains the single source of applied active/dismissed facts for profiles, directory filters, dashboards, and analytics; recording a future Departure does not create an effective dismissed fact early. The executor materializes the dismissed EmploymentStatus fact only when the departure is applied. Binds: lifecycle schema, record-departure command, executor query, analytics source. Prevents: counting scheduled leavers as already departed and overloading a temporal business-fact row with job-processing state.
- (decision) CC-06 executor approved after checking all visible backend remote refs (origin/main aca387b and PR #1 head c42de7d; no existing departure/employment executor implementation): one or more application worker instances poll durable PostgreSQL Departure rows whose effective time is due. Workers claim rows with database row locking/skip-locked semantics so one departure has one active executor; overdue scheduled rows are picked up after restart/outage. PostgreSQL remains the sole source of schedule truth; no external queue or database cron is introduced for this iteration. Poll cadence and batch size are operational configuration, not business state. Binds: lifecycle application worker, deployment process, concurrency tests. Prevents: duplicate execution across app replicas, lost in-memory timers, and schedule drift between PostgreSQL and a second queue.
- (decision) CC-06 effective-date semantics approved: Departure.effectiveDate is a PostgreSQL DATE, interpreted as becoming due at 00:00 in one deployment-configured businessTimeZone. The initial deployment value may be Europe/Kyiv, but timezone is configuration rather than persisted per employee/browser and must be an IANA zone. All workers and request-time lifecycle checks use the same conversion. Binds: API validation, due-query, controllable-clock tests, deployment config. Prevents: server-local timezone drift, browser-dependent execution, and different employees entering dismissed state at inconsistent instants for the same recorded date.
- (decision) CC-06 immediate-revocation rule approved: authentication/session validation and the AccessControl facade treat an actor with a due Departure (effectiveDate at or before the configured business date) as inactive on every request, regardless of Departure execution state and before worker materialization completes. This request-time cutoff is fail-closed, uncached across requests, and overrides the normal project-derived 15-minute revocation window. The worker persists the complete departure outcome idempotently, but worker delay/retry/failure can never restore actor access. Target-profile visibility continues to follow dismissed-target rules once effective. Binds: auth consume/session guard, AccessControl entry point, directory/profile projection, lifecycle executor. Prevents: post-midnight access during worker polling lag or outage and stale project grants surviving departure.
- (decision) CC-06 transaction/retry boundary approved: claiming a due Departure uses a short durable lease/claim operation; the complete PostgreSQL-owned lifecycle outcome then commits in one cross-context database transaction in the modular monolith. That transaction materializes dismissed EmploymentStatus, makes profile/account inactive, cancels open action items as departed, system-closes active MentorshipPairs, ends persisted access assignments held by the actor, and marks Departure applied. Any local failure rolls back all effects; the lease expires or the failure path returns the row to retryable state with attempts/lastError recorded separately. Every effect uses departureId-backed uniqueness/idempotency so retry after an uncertain commit cannot duplicate status, cancellation, closure, journal, or cleanup. External effects, if later introduced, use a post-commit outbox and do not weaken the local atomic boundary. Binds: unit-of-work interface across bounded contexts, executor state machine, recovery tests. Prevents: user-visible partial departure state and duplicate effects after crash/retry.
- (decision) CC-06 post-scheduling race rule approved: once a future Departure is scheduled, platform-owned writes reject assigning that employee any new direct reports, department-manager responsibility, or People Partner assignments; the timetracker adapter does not materialize a new PM/DM access grant for the scheduled leaver and raises an observable data-quality incident instead. Ordinary work and existing non-blocking assignments continue until effective date. Record-departure checks all authoritative active v1.5 manager relations plus PP assignment in the scheduling transaction; executor revalidates defensively. Request-time effective cutoff remains unconditional. Binds: relationship/policy commands, timetracker sync, departure precondition query, executor verification. Prevents: management/PP blockers appearing between an approved re-parenting check and effective execution.
- (decision) CC-06 defensive blocker behavior approved: effective departure is a security boundary and is not postponed if legacy/manual/inconsistent data violates the post-scheduling assignment guard. The executor applies the full departure and unconditional access/account cutoff on schedule, does not invent automatic re-parenting, and emits a high-severity remediation incident identifying the stale blocker only to authorized operators. Any retained blocker edge pointing to the inactive person grants no access and remains visible for explicit correction. Binds: executor preflight, AccessControl inactive-actor rule, operations incident projection. Prevents: stale organisational data keeping a departed account authorized and unsafe automatic reassignment without product intent.
- (decision) CC-06 retry policy approved: execution states are scheduled, processing, retry_wait, and applied; there is no terminal failed state that silently abandons an effective departure. On failure record attempts, sanitized lastError, and nextAttemptAt; retry with configurable exponential backoff capped at an operational maximum. Crossing the configured alert threshold emits a high-severity incident and exposes an authorized manual retry action, while automatic capped retries continue until applied. The request-time access cutoff remains active throughout retry_wait. Binds: Departure state machine, worker query, operations surface, controllable-clock recovery tests. Prevents: abandoned partial offboarding and hot-loop retries.
- (decision) CC-06 record-departure HTTP/idempotency contract approved: POST /users/:userId/departures with Idempotency-Key, effectiveDate, and required reason creates the durable scheduled Departure and returns 201. Repeating the same key returns the original result without duplicate effects; a different key while that user has a non-applied pending Departure returns 409. GET /users/:userId/departures/:departureId exposes authorized status/diagnostics. PATCH, DELETE, cancellation, and rescheduling are not inferred in this iteration and remain a product follow-up. The record command evaluates all departure blockers and writes the Departure/idempotency record atomically. Binds: AD-14 departure route, lifecycle controller/action, uniqueness constraints, stage-1 scenarios. Prevents: duplicate schedules from client retries and accidental invention of an unapproved cancellation lifecycle.
- (decision by architecture-review) Reviewer hardening accepted: AD-21 makes v1.5 a brownfield code cutover; PP HR-line inheritance is fail-closed behind the Department-boundary gate; CC-07 owns the immutable journal and blocks AD-19 stage 2; departure re-parenting is an explicit versioned command, while sync-owned PM/DM remains externally remediated.
- (decision by architecture-review) Departure execution now binds required BUSINESS_TIME_ZONE, persisted effectiveTimeZone and immutable dueAt, PostgreSQL-time comparisons, stable skip-locked claim ordering, per-lease fencing token, a single transaction-propagating offboarding coordinator, directional request-time cutoff, principal-scoped canonical idempotency, and an operational release gate.
- (event by architecture-review) CC-04/CC-06 reviewer findings mapped and resolved in reviews/review-cc04-cc06-resolution.md for AD-1 stage-1 story regeneration. Spine remains draft; Department/HR boundary and CC-07 remain explicit stage-2 blockers, and no application implementation is claimed.
- (event) 2026-08-31: AD-21 amended in place — interim access-control adapter retirement and the ACCESS_CONTROL_PORT rebind are owned by the new adoption slice _bmad-output/specs/spec-user-management-access-control-adoption/; the interim session resolver is a separate UM Epic 2 retirement; the write dual gate is blocked on a missing user-management:edit permission (adoption SPEC Open decisions).
- (decision) 2026-09-01: mentorship technical design landed — AD-5 confirms `mentorship` as a `src/mentorship/` context; AD-17 refined (status is the lifecycle SoT, `endedByDepartureId` is the FK-less system-closed marker + AD-20 idempotency provenance, `MentorshipAvailability` is a standalone per-user aggregate written by `PATCH /users/:id/mentorship-availability`, mentorship status is derived not stored); Deferred "S13 flag endpoint" retired and the "S10/S15" mentorship clause updated. Binding doc: docs/architecture/mentorship.md. Open for the human: unseeded `mentorship:assign` permission and the S13 `canAccessSection` increment (same class as user-management:edit and the S9 gap). Nothing approved.
- (event) User-approved batch architecture update 2026-09-02 recorded from ratification workspace. Single batch sign-off.
- (decision) AD-22 Employment lifecycle owns EmploymentStatus. Seed import may initialize initial state only and must not overwrite an applied departure. Departure executor owns active to dismissed. TimeTracker does not write employment state. User.isActive is a temporary technical authorization projection updated atomically in the same departure transaction. Legacy direct-deactivate must not remain an independent writer. Binds: import, departure, authz filter, TT sync. Prevents: re-import restoring access after departure; split writers of dismissed fact.
- (decision) AD-23 Shared applyDepartureEffects({departureId, leaseToken, departingUserId, effectiveDate, tx}): Promise<void>. Executor owns claim/fencing. Participants use supplied tx and never open nested transactions. Binds: mentorship, action-items, access-control departure participants. Prevents: incompatible call signatures across contexts.
- (decision) AD-24 HTTP denial oracle. Invalid/inactive session 401. Missing resource or hidden-existence target 404. Visible resource forbidden feature/action 403. Lists omit invisible rows. Hidden-target 404 takes precedence over mutation permission checks. Binds: every HTTP adapter. Prevents: 403/404 split leaking target existence; UMAC 403 empty-audience rule remaining live.
- (decision) AD-25 Frontend authorization and cache contract. Backend response projection is the sole authorization SoT. UI must not duplicate role/access matrices; renders returned fields and server capabilities such as canEdit. Permission-sensitive queries staleTime 0, refetch appropriately, actor/session in query keys, clear on session change, no persistent personal-data query cache, mutations invalidate related queries. Preserve installed React/Vite/Router/TanStack/Axios/Tailwind/shadcn/i18n and api/hooks boundary. Binds: frontend. Prevents: client-side permission reconstruction and stale authorized views.
- (decision) AD-5 amended: action-items is a confirmed bounded context exporting applyDepartureEffects. Mentorship remains confirmed and is now user-approved (batch 2026-09-02) owning MentorshipPair, MentorshipAvailability, derived status, willing pool, pair lifecycle, closure notes, and read projections. It does not own authorization, UserEvents persistence, employment orchestration, All Employees filter engine, or TimeTracker.
- (decision) AD-17 amended: effective departure atomically sets openToMentoring=false, auto-closes active pairs, pool and directory projections additionally require active employment, rehire requires explicit opt-in again. Binds: mentorship departure participant. Prevents: departed people remaining in the willing pool.
- (decision) AD-16 and AD-20 amended to bind AD-22/AD-23. AD-20 cancel rule refined: departure cancels only open items assigned to the departing user; items authored by the departing user for active assignees remain active. Persist cancelled status, fixed system reason, cancelledAt, sourceDepartureId. Manual and campaign-generated items follow the same assignee rule. No new campaign lifecycle. Verified vs 4.16 open action items close as cancelled-departed (ambiguous on author vs assignee) - this is a refinement not a contradiction. um-dep-03 owned-by clause marked stale.
- (decision) Spine status set to approved-with-open-items. Governance: memlog batch sign-off is sufficient for architecture behavioral/security/ownership/data-contract choices. Editorial corrections do not require user approval. ACF AD-3 persisted AD-1 ledger with distinct author/approver is unchanged.
- (event) Architecture Update 2026-09-02 architect-owned design batch. User-approved: OQ-105, OQ-116, CC-05; keep OQ-PERM-01 open. Architect-owned: CC-07, CC-09, CC-11, OQ-114, OQ-115, OQ-117 plus architecture OQ-118, DEPARTMENT-EDGE. No application code. No PRD regeneration. No historical rewrite.
- (constraint) OQ-PERM-01 remains open. Requirements section 2.3: default role-to-permission assignments are drafted by the team and confirmed by the PO. Do not seed or infer that matrix.
- (decision) AD-26 OQ-105 HR Admin lifecycle. Nobody manages HR Admin means no separate superior administrator or special HR-Admin management chain. Ordinary FR administration: a current HR Admin may assign or revoke HR Admin for another user; self-assignment forbidden; removal/deletion/self-revocation of the final HR Admin holder forbidden. HR Admin remains a data-defined FR, not a hard-coded authz bypass. Preserve ACM-1/AD-12 bootstrap holder and ordinary delegation. Consistent with sections 2.2/2.3 and AD-12. Closes OQ-105 at design.
- (decision) AD-27 OQ-116 ordinary project membership. Membership is Relationship type=project only. It does not imply PM/DM, grants no Project-line audience, and grants no access to other members profiles. Positive Project-line requires explicit PM or DM AR policy grant on a project the target belongs to. No new targetRole for ordinary members. Matches section 3.2 Project line = PM and DM. Closes OQ-116. Unblocks DEPARTMENT-EDGE from this dependency.
- (decision) AD-28 CC-05 Self vs full-profile. Self is exclusive of Reporting, Project, PP, Colleague when viewer equals target. Full-profile is a separate read-only overlay, not a matrix column. Calculate Self first, then apply full-profile. Effective section access is max(Self, full-profile) with write greater than read greater than none. Overlay supplies read where Self has none and never supplies write. Does not bypass functional permissions, command rules, field/record restrictions, or narrower rules such as mentorship closure-note visibility. Closes CC-05.
- (decision) AD-29 CC-07 AccessJournal. Immutable append-only table AccessJournal with id uuidv7, occurredAt, actorUserId, subjectUserId, kind, before jsonb, after jsonb, idempotencyKey unique. Kinds: manager, people_partner, department_membership, department_manager, full_profile_grant, full_profile_revoke, shared_link_access. before/after are complete snapshots of the fact. Readers: full-profile overlay holders, and the subject's current Reporting-line manager or assigned PP, via AccessControl; not HR Admin by role. user-management owns the writer; same PostgreSQL transaction as the fact write. Retry uses idempotencyKey. Closes CC-07 design.
- (decision) AD-30 CC-09 UserEvents. user-management is the sole owner and writer of UserEvents persistence. Other contexts deliver events only through the user-management career-event application boundary in the supplied transaction. Durable unique idempotencyKey (sourceContext, sourceAggregateId, eventType, occurrenceId) prevents duplicate at-least-once delivery for departure and mentorship retries. Manual corrections remain soft-delete plus append. Closes CC-09 design.
- (decision) AD-31 CC-11 project Relationship sole writer. TimeTracker sync is the sole writer of Relationship type=project rows. Manual admin, HR Admin, and resourcing fulfilment must not independently write those rows. Fulfilment records request outcome only; membership appears when sync reflects it. AD-13 remains sole writer of managedBy=sync policy rows. Residual identity join is TT-IDENTITY-01. Closes CC-11 design.
- (decision) AD-32 OQ-114 custom fields. Column-per-field forbidden. Do not query User.customFields jsonb as the directory path. CustomFieldDefinition plus typed EAV CustomFieldValue with btree indexes so new fields are filterable/sortable without migration. Access Control visibility applied before filter execution. Interim User.customFields jsonb is transition debt. Closes OQ-114 design.
- (decision) AD-33 OQ-115 dashboards. No generic widget framework in v1.5. dashboards context owns four fixed read models composed from owning-context application queries. AccessControl resolves authorized target ids before aggregation. Dashboard never widens access. Closes OQ-115 design.
- (decision) AD-34 OQ-117 and architecture OQ-118. No new profile bounded context. user-management owns GET/PATCH /users/:id HTTP and the data/canEdit response envelope. It assembles data from owning-context application read exports after AccessControl section decisions. canEdit is a read-only projection of the same dual gate computed by AccessControl. Closes OQ-117 and architecture OQ-118.
- (decision) AD-35 DEPARTMENT-EDGE. Department is platform-owned nested entity with parentId and isHr. Indexed parentId. Acyclicity in the write path. Exactly one current membership via UserDepartment, not a Relationship type. Department manager remains Policies targetType=department. TimeTracker has no department concept. Seed CSV is flat: import as roots; nesting and isHr are platform-administered. PP HR-line walks assigned PP direct chain only while ancestor department.isHr; unmarked HR is fail-closed to assigned PP. Closes DEPARTMENT-EDGE design.
- (assumption) Department.isHr is the simplest marker for section 2.1 inside-HR restriction. Seed must mark the HR department before HR-line inheritance scenarios can pass; unmarked remains fail-closed.
- (constraint) Implementation divergences recorded as debt, not design weakening: interim position HR Admin check; no last-HR-Admin guard; no journal/UserEvents/Department/EAV tables; User.customFields jsonb; GET /users whole-row serializer; no TT project sync writer.
- (event) Spine distilled 2026-09-02-architect-design-batch: PM/AD-26..AD-35 added; AD-5/10/11/12/13/19 amended in place. IDs not renumbered. lint pending.
- (event) lint_spine.py 0 findings after 2026-09-02-architect-design-batch distill. YAML companions parse. Reviewer Gate dispatched as independent prompt in ratification reviews/ — not self-certified.
- (event) Architecture Update 2026-09-02 reviewer-gate correction. Reconcile H1-H7 and H11 only; verify H8. Preserve PM/AD-1..PM/AD-35. No renumber of AD-26..35. Documentation-only.
- (decision) H3 ID disambiguation: architecture envelope blocker becomes ARCH-ENV-01 (PM/AD-34 design closed; implementation partial). Architecture project sole-writer blocker becomes ARCH-PROJ-WRITER-01 (PM/AD-31 design resolved; implementation open). Historical PRD OQ-118/OQ-119 and CC-11 Option 1 meanings preserved. Architecture OQ-118 and CC-11 IDs superseded, not rewritten.
- (decision) H4 AMEND AD-7 in place: Permissions physical identity is key (unique, append-only). title is not the identity field. ACF/AD-4 supersedes/refines the earlier physical shape Permissions {id, title, description}.
- (decision) H7/H11 Stack pin: Node runtime architecture pin is exactly major 24.x. NestJS 11.x is the project pin. NestJS 12 is an optional separately planned dependency upgrade requiring compatibility and regression validation; it does not require an ESM migration. Toolchain debt: engines >=24 admits later majors; backend @types/node 26; frontend @types/node 24; local Node 22 below pin. Closure needs runtime/CI Node 24.x, engines constrained to major 24, types aligned to 24.
- (event) Update mode complete for reviewer-gate-correction: H1–H7/H11 applied; H8 verified; PM/AD-1..35 preserved.
- (event) 2026-09-02-doc-cleanup companion: no PM AD text changed; ratification/register/coverage/docs reconciled. PM/AD-1..35 unchanged.
- (event) 2026-09-02 merge re-align: origin/main added the AccessJournal (PM/AD-29) table section to database-schema.md; a manual conflict resolution on our branch dropped it, leaving the schema doc contradicting spine AD-29 + the AD-19 gate + the ratification matrix. Restored the section and added an explanatory note (enforcement=policies/relationships/AD-10 walk; AccessJournal=§3.4 narrow audit of who-changed-whose-access + shared-link views, mutation-path write only, not on the 2s ACM-9 read budget, implementation still absent/gated on CC-07). Added AccessJournal to the file's ERD. AD-29 unchanged.
- (decision) 2026-09-11 formal package sign-off: the user's explicit workspace confirmation, acting in both Product Owner and Architect roles, grants formal sign-off for `PR-S-01` / `CC-04` and `PR-S-02` / `CC-06`. The authority is that explicit user decision through the PM spine governance path. This records package sign-off only: it is independent of PM/AD-19 and PM/AD-20, which remain binding architecture directions rather than the approval itself; it does not close `CC-04`, `CC-06`, `CC-07`, `CC-08`, `CC-09`, or `OPERATIONAL-ENVELOPE`, and it authorizes neither implementation nor production or release readiness.
