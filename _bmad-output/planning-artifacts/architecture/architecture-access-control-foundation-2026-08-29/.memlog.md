---
scope: Phase-0 Access Control foundation and approved headless Kernel MVP; excludes User Management ownership.
purpose: Decision history for the foundation, kernel evidence boundary, and implementation blockers.
altitude: epic
updated: 2026-09-02T08:42
---

- (constraint) Parent architecture AD-1 requires independently human-approved Stage-1 scenarios, then independently approved red HTTP E2E, before production code; this local spine cannot weaken that gate.
- (constraint) Parent AD-2, AD-3, AD-9, AD-10, AD-11, AD-12, AD-14, and AD-19 bind this scope: hexagonal code, real HTTP E2E, facade-only authorization, bulk/live audience resolution, typed relationship facts, fail-closed behavior, established route conventions, and PP semantics.
- (decision) The two-day foundation owns only Access Control Phase-0 audience derivation: Self, live direct Reporting, direct assigned People Partner, Colleague fallback, empty bulk short-circuit, and fail-closed broken/orphaned facts. It does not own User Management routes, response projection, UI, section/matrix decisions, Project, Department, PP HR-line, or functional permission evaluation.
- (decision) User Management remains the sole owner of the HTTP route and response projection. The Access Control foundation exposes a port/facade to be consumed by an existing or future User Management endpoint; it must not change User Management files.
- (question) Integration blocker: no identified User Management endpoint and response contract currently consumes the future facade. Before valid Stage-1 HTTP scenarios and Stage-2 E2E can be authored, the UM owner must name the route, request identity source, response-level observable outcome, and commit/branch that will wire it.
- (direction) PP mutation, journal details, scheduled departure, full-profile mapping, Project, Department, and timetracker decisions are excluded from this foundation. Their open product/architecture questions do not block the Phase-0 audience derivation, but may not be pulled into its implementation.
- (event) Decision review started on fast path at user request; decisions are inherited from existing reality or explicitly bounded as an open integration contract.
- (constraint) Reality check: User Management already has GET /users/:id protected by RequireFeatureForTarget and AccessControlPort.isAllowedForTarget. It is bound to InterimAccessControlAdapter, whose target check returns Boolean(userId); it is not the real facade. The candidate integration endpoint is therefore known, but the UM-owned port replacement and observable contract are not approved.
- (decision) 2026-08-30 Correct Course approved a separate Access Control Kernel MVP. ACF-1 remains the Phase-0 baseline and keeps its historical failed gate; the new kernel may be composed into AppModule without rebinding the User Management port.
- (decision) The kernel's scoped Stage-2 boundary is the public AccessControlFacade through a real Nest module, real AccessControlModule, real Prisma adapters, and migrated PostgreSQL. This is integration evidence, not HTTP E2E, and it cannot close the /users product gate.
- (constraint) No artificial HTTP/debug endpoint and no User Management provider override may be introduced for the kernel gate. The future User Management-owned integration story still requires production rebinding, projection, and real-consumer HTTP E2E.
- (decision) Human workboard IDs are adopted without reassignment: ACM-3 inactive viewer/bridge/target results; ACM-4 multi-audience inputs; ACM-1 minimal FR data; ACM-2 isAllowed; ACM-5 S1/S10/S11; ACM-8 composition; ACM-9 500-target evidence.
- (constraint) ACM-3 and ACM-4 end at audience-resolution outputs and do not own canAccessSection; ACM-5 alone owns the base section decision.
- (question) FR-AMD-1 must independently resolve and receive human approval for OQ-3, OQ-4, OQ-6, OQ-7, and OQ-11 before ACM-1 may enter Stage 1.
- (decision) OQ-3: Functional-role grants use PolicyPermissions(policyId, permissionId). The composite primary key prevents duplicate grants; indexed permissionId→policyId and userId→policyId joins support isAllowed; both foreign keys restrict deletion in the Kernel MVP so role-deletion semantics remain deferred.
- (decision) OQ-4: Policies.id is roleId. For type='FR', targetType and targetId are NULL; for type='AR', both are NOT NULL. A PostgreSQL CHECK enforces this type-specific shape. The single seeded FR role has targetRole='hr-admin', but authorization never compares targetRole.
- (decision) OQ-6/OQ-7: [MVP REDUCTION] The Kernel MVP permission catalog is seed/migration-owned, has no HTTP mutation surface, and contains exactly the immutable unique keys user-management:create, user-management:deactivate, and user-management:list. Keys use lowercase context:action. The evaluator remains data-driven and branches on no key; unknown keys deny.
- (decision) OQ-11: The functional-role slice in access-control owns the FR repository port and isAllowed domain service. AccessControlFacade delegates to and exposes that service; it does not implement a second evaluator. The later catalog slice consumes this foundation.
- (constraint) Seed exactly one hr-admin FR policy, its three PolicyPermissions grants, and one UserPolicies attachment to the active ROOT_WORK_EMAIL user, atomically and idempotently. Seed no other attachment or default grant. position='HR Admin' is profile data and is prohibited as an authorization rule or fallback.
- (constraint) [MVP REDUCTION] No /roles HTTP surface or permission mutation port ships in the Kernel MVP. targetRole is not an authorization predicate. Runtime role management and the complete §2.3 catalog remain deferred.
- (constraint) Reviewer gate: FR rows require targetRole IS NOT NULL; the FR/AR CHECK and partial FR role-key uniqueness are implemented in reviewed custom PostgreSQL migration SQL under the repository's existing raw-SQL pattern.
- (constraint) Reviewer gate: isAllowed accepts an open case-sensitive string constrained by lowercase context:action syntax, not a closed three-key union. Permission keys are append-only identities; future catalog writes remain Access Control-owned and may not update keys in place or bypass the owned mutation boundary.
- (constraint) Reviewer gate: exact three-row/one-role/one-attachment cardinality is the fresh-database bootstrap baseline, not permission to delete later catalog state. Seed reruns non-destructively ensure owned bootstrap identities and fail atomically on conflicting owned drift; they never delete or rewrite non-bootstrap rows or attachments.
- (constraint) Reviewer gate: active authorization eligibility includes User.isActive and the AD-20 request-time due-departure cutoff. Seed selection also excludes an already-due ROOT_WORK_EMAIL user without choosing a fallback.
- (direction) Future FR user attachment must reference an existing Policies.id roleId; the current inline FR body in api-conventions.md is superseded for FR and must be synchronized before a User Management attachment story. No attachment HTTP shape is added by the Kernel MVP.
- (constraint) Artifact authority: AD-4 and FR-AMD-1 supersede contrary or incomplete FR acceptance wording in the Kernel SPEC, stories, and sprint proposal, including the phrase 'closed kernel compatibility set'. Every ACM-1/ACM-2 Stage-1 dispatch must load both architecture artifacts until bmad-spec synchronizes upstream delivery artifacts.
- (event) spine finalized
- (event) P1 approved FR architecture update on 2026-08-30
- (decision) AD-4 supersession: AD-20 due/departure enforcement is formally deferred from ACM-1/ACM-2 because the Kernel MVP has no Departure persistence seam; User.isActive remains the live MVP eligibility predicate, while the documented dismissed-target projection remains binding future work.
- (decision) AD-4 root bootstrap: normalize ROOT_WORK_EMAIL by DEC-UM-007 before lookup; a deploy-time prerequisite outside ACM-1 must ensure exactly one active normalized User exists, authorizing no User Management API, CRUD, runtime role management, or other User Management feature work.
- (constraint) ACM-1 must begin one database transaction, lock the candidate ROOT User and any seed-owned hr-admin attachment, then revalidate normalized email, exact-one active eligibility, and existing attachment identity before any bootstrap write and again before commit; every conflict rolls back with actionable diagnostics.
- (decision) If normalized ROOT_WORK_EMAIL differs after bootstrap from the User holding the seed-owned hr-admin attachment, ACM-1 treats it as conflicting bootstrap drift and fails atomically; it never transfers that attachment or creates an additional root attachment.
- (decision) PolicyPermissions is database-enforced FR-only through a stored policyType discriminator fixed by CHECK to FR and a restrictive composite foreign key (policyId, policyType) to unique Policies(id, type); reviewed custom PostgreSQL migration SQL owns the check, unique support key, and composite FK, so AR policy links are structurally impossible.
- (decision) AD-1 traversal supersession: a repeated node in a Reporting chain invalidates and denies Reporting only for that affected target; Self and direct People Partner are evaluated independently, with Colleague applied only under the ordinary no-stronger-audience rule.
- (decision) The exact ACM-9 depth sequence is an operational measurement protocol, not architecture or product behavior; architecture retains the absolute 500-target PostgreSQL gate and representative Reporting, direct PP, Colleague, and mixed graph coverage.
- (decision) ACM-9 fails on either absolute two-second warm-p95 or worst-case breach, or statement timeout; a post-composition slowdown within both absolute limits is recorded but does not fail this MVP, and measurement stops immediately at the first absolute breach while preserving separate baseline and final artifacts.
- (event) Architecture repair decisions recorded for re-distillation; status remains in-review and implementation not authorized.
- (decision) Scoped precedence: local AD-4 formally amends inherited AD-20 only for ACM-1/ACM-2 in this Kernel MVP; the request-time cutoff remains the binding future target and requires a new AD-1 gate after a Departure seam exists.
- (decision) Root ownership is persisted in one AccessControlBootstrap singleton keyed root-hr-admin with normalizedRootEmail, rootUserId, and policyId plus restrictive references; it distinguishes bootstrap provenance from later administrator hr-admin attachments.
- (constraint) Every ACM-1 transaction first acquires the common transaction-scoped PostgreSQL advisory lock for access-control:bootstrap:root-hr-admin, then locks/revalidates the singleton, candidate User, and recorded attachment; lock timeout or conflicting configuration fails atomically, covering the absent-row race.
- (decision) Reporting repetition is path-local per requested target; shared ancestors across targets are not repeats, and traversal ends successfully when the viewer is reached, so only repeated nodes encountered before viewer proof invalidate Reporting for that target.
- (constraint) Policies.type is NOT NULL and restricted to FR or AR in the reviewed PostgreSQL migration.
- (decision) ACM9-MVP-v1 measurement protocol fixes five warm-ups, twenty samples, nearest-rank p95, public-facade end-to-end timing including transaction/mapping, independent gates per fixture/depth, and EXPLAIN outside timed samples.
- (constraint) Every started ACM-9 run writes an immutable append-only PASS/FAIL/INCOMPLETE artifact with run role/id, protocol/version, fixture hash, source and migration revisions, environment, completed results, first breach/timeout, and stop reason; final references a comparable baseline and never overwrites it.
- (decision) AD-20 scoped precedence applies to the entire Access Control Kernel MVP, explicitly ACM-1 through ACM-5; ACM-8 adds composition and ACM-9 measures without adding due behavior. The future target and new AD-1 gate remain binding.
- (constraint) ACM9-MVP-v1 baseline and final must share an environment-manifest hash covering PostgreSQL config/version, runtime, CPU/memory limits, database topology, and isolation/load policy; mismatch yields INCOMPLETE and no gate or slowdown comparison.
- (event) Repair reviewer gate PASS: deterministic lint and good-spine, current-reality, and adversarial lenses report no remaining Critical or High findings; spine remains in-review and implementation not authorized.
- (event) Repair round 3 (P2) opened after an independent review confirmed eighteen findings; prior verdicts are superseded, the spine stays in-review, and implementation remains not authorized.
- (decision) Reporting cycle supersession now applies to this spine's own memlog: the earlier entry that traversal ends successfully when the viewer is reached is RETIRED. Reaching the viewer is provisional; the walk continues to chain termination; a repeated node anywhere in the chain, before or after viewer proof, denies Reporting for that target only, so a viewer inside a cycle is denied rather than proven.
- (constraint) Identity validation precedes Self: viewer existence and active state are confirmed before any audience derivation, and Self is exclusive only after both viewer and target are confirmed present and active. An unconfirmed viewer or target yields an empty audience Set, never Self and never the Colleague floor.
- (decision) Bridge taxonomy resolves the absent-edge versus invalid-endpoint contradiction. An absent manager edge is clean chain termination. An edge whose endpoint is inactive is unusable and treated as absent for traversal: before viewer proof Reporting is denied for that target, after viewer proof the chain has terminated without a repeat so Reporting is granted and nothing above the dead node becomes reachable.
- (constraint) An edge whose endpoint row is missing is unreachable in supported operation: relationships_shape_check requires a non-null reportsToUserId on direct and people_partner rows and the endpoint foreign key is ON DELETE RESTRICT. It is recorded as an invariant and kept as a defensive rule, never required as a constructible Stage-2 scenario.
- (decision) Soft-deleted User and bridge behavior is removed from the Kernel MVP: prisma/schema.prisma User carries no soft-delete column and isActive is the only lifecycle field. No field is invented to support the retired wording.
- (decision) CAP-8/ACM-0 is executable on a fresh migrated database: ACM-0 CREATES and validates the DEC-UM-007-normalized root User rather than validating an assumed one. A validation-only ACM-0 could never pass on a zero-user database, and the package no longer contains an unnamed external prerequisite.
- (constraint) Named production entrypoints and deployment order: ACM-0 is services/backend/prisma/seed.ts invoked by npm run db:seed; ACM-1 is src/access-control/infrastructure/bootstrap/access-control-bootstrap.ts wrapped by scripts/bootstrap-access-control.ts and invoked by npm run db:bootstrap:access-control. Order is db:deploy, db:seed, db:bootstrap:access-control, start:prod. Stage-2 evidence must invoke those exact entrypoints and may not re-implement their logic inline.
- (decision) ACM-1 keeps its own entrypoint rather than extending the seed because OQ-11 gives the functional-role slice sole ownership of the seed contract and evaluator. prisma/seed.ts is deploy tooling outside src/user-management/**, so AD-2 is not crossed.
- (decision) Normalized workEmail identity uses canonical storage with the database gap named. ACM-0 normalizes by trim and lowercase ON WRITE so the stored value is canonical, and the existing users_workEmail_key unique index then blocks a duplicate normalized row.
- (constraint) Exact-one eligibility counts ALL normalized matches first and only then checks active state: a count other than one fails as unmatched or ambiguous BEFORE isActive is consulted, so two rows differing only in case with one inactive cannot silently resolve to a single active match.
- (constraint) ACM-0 collision and concurrency: a normalized match that is not the intended root fails with actionable diagnostics and is never adopted, mutated, or reactivated; concurrent runs converge when the loser of the insert race takes the unique violation on users_workEmail_key, re-reads, and re-runs the exact-one validation, leaving no partial state.
- (question) Named residual risk: there is no database-enforced normalized uniqueness. users_workEmail_key indexes the raw stored value and DEC-UM-007's claim that uniqueness is enforced on the normalized value is not true of the database today. Pre-existing non-normalized rows can produce more than one normalized match; ACM-0 fails closed and a unique functional index is separately gated deferred work outside CAP-8's boundary.
- (decision) Dependency graph corrected to approved FR architecture, ACM-0, ACM-1, ACM-2. ACM-0 is a real predecessor of ACM-1 in every artifact, not an assumed precondition.
- (decision) FR-AMD-1 carries exactly one gate statement, replacing the contradiction between its Status and Authority section and its Remaining Gate section: architecture decisions are approved and final under approval_scope architecture-decision-only, and NO Stage-1 dispatch is authorized until the Kernel package review is approved.
- (constraint) AD-1 stage approvals are persisted and independently verifiable in an append-only approvals ledger recording story id, stage, artifact path, commit or revision, author, approver, decision, and timestamp. Author and approver must differ, and a dispatch may not start until the prior stage's record exists and verifies: the commit resolves and the named artifact is present at that revision.
- (constraint) Conditional dependencies are executable against persisted artifact state rather than free-text ordering. ACM-5 requires acm-4-disposition.yaml with disposition no-gap. ACM-8 and ACM-9-final require the ACM-9 baseline artifact with status PASS. Each is expressed as an exact path, field, and required value triple that a validator can check.
- (decision) Absent-singleton bootstrap adoption is explicit. With no AccessControlBootstrap singleton, ACM-1 adopts an existing FR hr-admin policy by natural key and adopts an existing attachment only when it already belongs to the located root, then writes the singleton; attachments belonging to anyone else remain non-bootstrap administrator state and are neither adopted nor transferred. A changed configured root with no singleton has no recorded provenance and therefore no drift to detect.
- (constraint) The provenance asymmetry is deliberate and security-relevant: with the singleton ABSENT adoption is permitted, and with the singleton PRESENT a changed normalized root is conflicting drift that fails atomically and never transfers or duplicates the root attachment. More than one candidate root attachment is impossible because UserPolicies is keyed (userId, policyId).
- (decision) Cross-type hr-admin collision resolved: FR role-key uniqueness is the PARTIAL index UNIQUE targetRole WHERE type='FR', so an AR policy carrying targetRole='hr-admin' is legal and is a different object. ACM-1 lookup and ACM-2 evaluation always filter type='FR'; the AR row is never adopted, mutated, counted, or reported as drift, is preserved, can hold no grant, and any UserPolicies row attaching to it is never bootstrap state.
- (constraint) ACM-1 Stage-1 and Stage-2 cover every CAP-3 database invariant: non-null FR|AR policy type; the FR/AR row-shape CHECK; the partial unique FR role key; the Policies(id, type) support key; unique immutable Permissions.key; PolicyPermissions pair uniqueness; the policyType='FR' discriminator with its restrictive composite foreign key rejecting an AR grant; the restrictive Permissions foreign key; the permission-first (permissionId, policyId) index; UserPolicies integrity including rejected bad-user, bad-policy, and duplicate attachments; the AccessControlBootstrap singleton constraints; and ON DELETE RESTRICT on all four functional-role-side foreign keys.
- (constraint) Index existence is observable evidence: the permission-first index is asserted by querying pg_indexes against the migrated database. That is legitimate Stage-2 evidence rather than a facade behavior, stated so the story cannot stall on how to observe an index.
- (constraint) ACM-9 reservation precedes fallible setup in two phases: the artifact path is reserved with run id, role, protocol version, and status INCOMPLETE BEFORE fixture construction, database connection, or manifest hashing, and manifest hashes are filled in as computed, so a failure during hashing still leaves a reserved auditable artifact carrying its stop reason.
- (constraint) ACM-9 records non-timeout infrastructure errors: connection failure, query error, and fixture-setup failure finalize the run INCOMPLETE with an error class and message. They are never PASS and never FAIL, because FAIL is reserved for a measured threshold breach or statement timeout.
- (decision) ACM-9 precedence is explicit: a recorded warm-p95 or worst-case value above two seconds, or a statement timeout, ALWAYS finalizes the run FAIL whether or not the environment manifest matches, because the gate is absolute rather than comparative. A manifest mismatch sets comparability mismatched and suppresses the baseline slowdown comparison; a mismatched run recording no breach finalizes INCOMPLETE. A mismatch can never upgrade a run to PASS and never erases a recorded breach. This supersedes the earlier rule that a mismatch produces no absolute gate verdict.
- (decision) ACM-4 process contradiction resolved by a named validation-only evidence exception in testing-strategy.md rather than by reclassifying the story: characterization tests over already-shipped behavior that change no production code may be committed green, are not a Stage-2 gate for any production change, and any found gap re-enters the ordinary three-stage sequence with a real committed-red test.
- (constraint) ACM-4 halt trigger widens from a concrete unmet behavior to ANY missing approved scenario coverage or concrete behavior gap, and it halts Stage 2 onward rather than production alone, opening a separately approved AD-1 sequence and requiring a Story Breakdown re-run.
- (constraint) Mechanical validation is persisted as a runnable validator with an exact reproducible command; no unverifiable check count may be claimed without it.
- (decision) Preservation is binding for this repair: all existing CAP IDs and all existing story IDs are unchanged, AD IDs are amended in place and never renumbered, both memlogs are append-only, and the spine keeps status in-review with implementation not authorized.
- (event) P2 repair re-distilled into the spine: AD-1, AD-3, and AD-4 amended in place with no renumbering; capability map, decision register, and Deferred updated; FR-AMD-1 unified to one gate statement and given absent-singleton provenance, cross-type collision, and brownfield reality; access-control.md, database-schema.md, and testing-strategy.md synchronized. lint_spine.py reports 0 findings. Status remains in-review and implementation not authorized.
- (event) Package gate APPROVED by the user on 2026-08-31. status becomes approved and kernel_spec_status becomes approved across SPEC, spine, FR-AMD-1, and both sprint proposals. Per FR-AMD-1's single gate statement this authorizes Stage-1 dispatch only, so implementation_status becomes stage-1-authorized rather than authorized: Stage-2 and Stage-3 still require their own per-stage human approvals. approvals.yaml is deliberately left empty because the Stage-1, Stage-2, and Stage-3 artifacts do not exist yet, so there is nothing an approver could have read and no commit any record could resolve against; pre-filling it would fabricate audit history and fail the ledger's own verification rule.
- (event) 2026-08-31: Decision Register gets one added row pointing "User Management consumer contract and HTTP mapping" at um-integration-contract-response.md (the six questions answered) and at the new package _bmad-output/specs/spec-user-management-access-control-adoption/ (rebind, per-route mapping, projection boundary, real-consumer HTTP E2E). No other spine change. The product gate stays open pending that package plus the separate Profile Projection story.
- (event) User-approved additive spine_id ACF and qualified-ref convention. Existing AD IDs and historical prose preserved.
- (constraint) Inherited from parent PM spine (2026-09-02): PM/AD-22 employment ownership; PM/AD-23 applyDepartureEffects contract for any Access Control departure participant; PM/AD-24 HTTP denial oracle 401/404/403.
- (constraint) Inherited Invariants now list PM/AD-22, PM/AD-23 (exact applyDepartureEffects), PM/AD-24. ACF/AD namespaces remain separate from PM/AD.
- (change) H5 correction: Inherited Invariants PM/AD-23 row amended to the five-field PM/AD-23 signature `{departureId, leaseToken, departingUserId, effectiveDate, tx}`; supersedes the erroneous three-field `{departureId, userId, effectiveOn, tx}` prose.
- (event) 2026-09-02-doc-cleanup: Decision Register reconciled to package_review_approved 2026-08-31 / stage-1-authorized; Inherited Invariants use PM/AD-n; local headings remain AD-1..4 for lint_spine; ACF/AD namespace noted above Decision Register.
