---
scope: People Management architecture ratification package (ARCHITECTURE-RATIFICATION.md + blockers/evidence-matrix/transition-debt companions)
purpose: Update run: apply Reviewer Gate blocker and ratification corrections without editing either spine
altitude: initiative
updated: 2026-09-02T08:42
---

- (event) Update run opened 2026-09-02 to apply the Reviewer Gate (Validate) findings. Gate verdict was REQUIRES ARCHITECTURE UPDATE. Scope: ratification package only; neither spine is edited.
- (constraint) The original ratification run left no .memlog.md, so this update could not resume from one. The Reviewer Gate report from the prior Validate session is the authority for what to apply; every finding was independently re-verified against the working tree before being written. This memlog is initialized fresh at update time - a gap in the ratification's own auditability, recorded rather than papered over.
- (constraint) User directives binding this run: preserve all existing AD IDs; do not resolve CC-08; do not invent an EmploymentStatus ownership rule; do not claim release readiness. Corollary: no AD may be added to or amended in either spine by this run.
- (decision) AD-7 implementation downgraded conformant -> partial. Binds: the ratification register and evidence-matrix. Prevents: a planner skipping the FR policy slice on a 'conformant' rating. Verified: Permission model is {id, key, description} (schema.prisma:121-131) while the spine specifies {id, title, description}; AD-7 also carries TD-08, which the package's own status model (:40) makes mutually exclusive with conformant.
- (decision) AD-8 implementation downgraded conformant -> partial. Binds: register and evidence-matrix. Prevents: treating the operator guard as enforced. Verified: Policies.operator is TEXT NOT NULL DEFAULT '==' with no CHECK (20260831070000 migration.sql:17), and functional-role-evaluator.service.ts never reads the column. CORRECTION to the gate report's 'zero enforcement': access-control-bootstrap.ts:185-188 does throw on operator drift, but only at bootstrap and only for the single FR policy row - it is not a schema constraint and not an evaluation-time guard.
- (decision) TD-02 re-scoped P1 -> P0. Binds: transition-debt register and deploy sequencing. Prevents: the root self-provisioning path staying invisible behind a 'fixture-style bearer identities' description. Verified: interim-session-resolver.adapter.ts:74-87 creates a User row with position 'HR Admin' when no root exists, which interim-access-control.adapter.ts:11-14 then grants user-management:create|deactivate|list.
- (decision) TD-04 re-scoped from 'GET /users/:id' to the whole-row serializer across all six handlers. Verified: toUserResponse is applied at users.controller.ts:65,76,84,90,104,112, i.e. including GET /users list; and isAllowedForTarget returns Boolean(userId) for the three target-scoped routes GET/PATCH /users/:id and PUT /users/:id/photo, so writes are as ungated as reads.
- (decision) TD-10 decisions link corrected from [AD-1] to empty. Verified: AD-1 (ARCHITECTURE-SPINE.md:57-62) is the three-stage quality gate; TD-10 is a document-status governance mismatch with no AD owning it. Left empty deliberately rather than reassigned - the absence of an owning decision is the finding, and it is carried by new blocker CC-10.
- (question) CC-08 recorded OPEN, not resolved, per user directive. Accurate framing differs from the gate report's wording and this matters. The report said 'AD-13 designates the TimeTracker sync as sole writer of employment state'. Verified: AD-13's sole-writer rule (ARCHITECTURE-SPINE.md:145) is scoped to managedBy:'sync' POLICY rows, not to EmploymentStatus. The real gap is that EmploymentStatus has more than one candidate writer and NO AD assigns ownership: (a) the AD-20 departure executor materializes the applied dismissed fact (:194 and memlog :51); (b) the AD-16 seeded-import path carries CSV IsDismissed/DismissedDate mapped to the same 4.16 active/dismissed fact (docs/test-cases/user-management/seed/README.md:37), currently stopgapped onto User.isActive; (c) AD-13's future timetracker sync is the third plausible writer and its rule does not exclude it. No ownership rule invented here - the architect must decide.
- (decision) QUALITY-GATE-AC blocks list expanded to name ACM3-II-06 explicitly, and severity set P0. Verified: gate-decision.json evaluated 2026-08-31 reports gate_status FAIL, p0_status NOT_MET, critical_open 1, rationale 'P0 coverage is 95% (required 100%). 1 critical requirement(s) uncovered: ACM3-II-06'. Prevents: an untested inactive-identity fail-closed invariant being read as a paperwork item behind the generic phrase 'unqualified production-readiness claim'.
- (decision) OPERATIONAL-ENVELOPE added as both a blocker and a named register dimension with an owner, because a blocker row alone does not fix the finding: the omission was a whole dimension the initiative altitude owns being silent. Added section 3.1 Dimension register to ARCHITECTURE-RATIFICATION.md rather than a new AD, since this run may not amend the spine. Owner: Architect. Evidence: spine AD-20 operational release gate (:196) and Deferred 'Remaining operational envelope ... must be resolved before first release' (:316).
- (decision) Baseline pinned to commit SHAs. binds_spine -> 0e703d19150b4727c1f2b42e2f359df9995735dc (last commit touching ARCHITECTURE-SPINE.md, working tree clean for that path). evidence_baseline -> workplace 0e703d19150b4727c1f2b42e2f359df9995735dc, services/backend 08931ad14778f1953ca551c0e25c782afa4ccb1b, services/frontend d06b977c714d69036eb9407dffaf33d953e6fa15. Prevents: an unverifiable 'ratified' claim six weeks out.
- (assumption) Pin caveat recorded in the frontmatter rather than smoothed over: both submodules report dirty in the parent, but only from UNTRACKED files (backend .claude/worktrees/ and AGENTS.md; frontend AGENTS.md) - no modified tracked files, so code evidence at the pinned SHAs is reproducible. The ratification package itself was untracked at pin time, so its own SHA cannot be self-referential.
- (decision) Applied the full blockers.yaml patch from the gate report, not only the enumerated items. Rationale: the user's instruction was to apply the report's blocker corrections, and the report's own net effect is that ZERO blockers close outright - CC-04/CC-05/CC-06 re-scopes keep blocking status and TIMETRACKER-CONTRACT is superseded by two open successors, so nothing in the patch reduces the open surface. Added severity, evidence, and closure_condition to every pre-existing entry so the section 7 'high-impact items include' ranking has a machine-readable basis.
- (event) Twelve previously-untracked blockers added: SEC-AUTH-01, CC-08, CC-09, CC-10, OPERATIONAL-ENVELOPE, TT-IDENTITY-01 (P0); CC-11, OQ-AC-EDIT, OQ-116, OQ-105, OQ-118, TT-PMDM-01 (P1). Blocker count 13 -> 25.
- (event) Reviewer Gate rerun against the updated package. Deterministic pass: lint_spine does not apply (no ARCHITECTURE-SPINE.md in a ratification workspace) and returns 0 findings against the pinned bound spine. Four lenses dispatched as parallel subagents: rubric walker, version verification (configured floor), adversarial seams (configured floor), correction verification (ad-hoc). Verdict: FAIL - adversarial seams FAIL, the other three PASS WITH FINDINGS.
- (decision) Self-correction 1 of 3: the SHA-pin caveat I wrote was false. Verified: git diff --name-only shows seven modified TRACKED files in the superproject; docs/integrations/timetracker-external-api.json is UNTRACKED (git ls-files --error-unmatch fails) so it does not exist at the pinned workplace SHA despite being P0 evidence for TT-IDENTITY-01; services/backend 08931ad is on no branch, tag, or remote. Frontmatter rewritten to reproducible: partial with five explicit caveats.
- (decision) Self-correction 2 of 3: TD-04's deviation was widened to six routes but its expiry_trigger was left at the single-handler UMAC-1 slice, so retiring the P0 exactly as written would leave five whole-row routes emitting non-S1 fields. Trigger widened to all six routes plus real target-scoped authorization on all three target routes.
- (decision) Self-correction 3 of 3: reverted the AD-19/AD-20 design and CC-04/CC-06 status relaxations. Two independent lenses judged them a softening resting on memlog entries and draft-spine prose - the evidence class section 7.1 excludes - and adversarial seams found docs/architecture/mentorship.md routing the unresolved applyDepartureEffects signature back to CC-06, so CC-06's design is demonstrably not resolved. Design findings retained in design_status fields where the two-axis model puts them.
- (decision) CC-08 statement extended to a fourth candidate writer, User.isActive - written by user.repository.ts:59 and read as the authorization filter by prisma-identity.adapter.ts:24. Still not resolved. This widens the question rather than answering it: a rule naming only the EmploymentStatus aggregate would leave the column that currently gates authorization unowned.
- (decision) AD-15 register cell corrected and TD-05 re-scoped P3 -> P2. Verified: MagicLinkDispatcherFake is bound in production providers at user-management.module.ts:34, so the previous 'dispatcher fake is scoped' wording was false. OQ-118 evidence repointed from api-conventions.md (zero occurrences of canEdit) to access-control.md and the UMAC adoption scenarios.
- (event) Update run closed. Gate still FAILs; blocking findings carried into section 11 rather than resolved. Package is NOT release-ready and makes no such claim. 25 blockers: 24 open, 1 superseded by two open successors.
- (event) final entry
- (event) Disregard the preceding 'final entry' line: it was an accidental placeholder emitted by the tooling command, not a record of work. The memlog is append-only by design, so it is corrected here rather than deleted. The true closing entry is the one before it.
- (event) Update run opened 2026-09-02: approved batch architecture update. This prompt is the single user batch sign-off. No per-decision approval requested.
- (constraint) Batch sign-off supersedes the prior memlog constraint that forbade resolving CC-08, inventing EmploymentStatus ownership, and amending spines. This run MAY amend both spines while preserving all existing AD IDs. Application code is not implemented. No commit, push, or tag.
- (constraint) Governance: existing BMAD memlog plus this single user batch sign-off plus BMAD Update plus a future commit SHA. No architecture-approvals.yaml. No second architecture approver. AD-1 stage ledgers and ACF AD-3 author-differs-from-approver remain in force for scenario/test/code gates.
- (decision) USER-APPROVED PM/AD-22 CC-08 employment-state ownership. Verified vs docs/project-requirements.md 4.16 (EmploymentStatus is the single source) and 4.17 (seeded population); vs PM/AD-13 (ttId and managedBy:sync policy rows, not employment); vs PM/AD-16 (isActive is technical). No product contradiction. Implementation divergence recorded: DELETE /users/:id and user.repository.ts:59 still write isActive independently; seed maps IsDismissed onto isActive; EmploymentStatus model is absent.
- (decision) USER-APPROVED PM/AD-23 shared applyDepartureEffects contract. Verified: docs/architecture/mentorship.md:321-327 already uses this shape; domain-driven-design.md:16 already requires one shared PostgreSQL UoW. Not a new product choice. Implementation absent.
- (decision) USER-APPROVED PM/AD-24 global denial policy: 401 invalid/inactive session; 404 missing or hidden-existence target; 403 visible resource forbidden feature/action; lists omit invisible rows; hidden-target 404 precedes mutation permission checks. SUPERSEDES 2026-09-01 UMAC empty-audience 403 (Dmytro Novyk). Historical approvals.yaml and SPEC 403 text remain as historical evidence; umac-05 and related AD-1 artifacts marked superseded/stale for regeneration. Compatible with access-control.md:187 original matrix; conflicts with the 2026-09-01 amendment in the same file which is now stale.
- (decision) USER-APPROVED qualified refs: spine_id PM and ACF. Additive decision_refs field. TD-09 decisions [] decision_refs [ACF/AD-3] artifact_refs [ACM-3]. Historical bare AD numbers preserved. Verified ACF/AD-3 prohibits User Management provider override as kernel evidence seam.
- (decision) USER-APPROVED PM/AD-25 frontend architecture. Verified installed stack: React 19, Vite 8, react-router-dom 7, TanStack Query 5, Axios 1, Tailwind 4, shadcn, i18n in services/frontend/package.json; api/client.ts and hooks/ boundary exists. Divergence: QueryClient default staleTime is 5 minutes (main.tsx:13). localStorage used for sidebar and a generic hook, not currently for personal-data query cache.
- (decision) USER-APPROVED Action Items boundary as AD-26 via AD-5 confirmation plus AD-23 participant. No campaign lifecycle invented. Implementation absent.
- (decision) USER-APPROVED mentorship bounded-context design (closes CC-10 mentorship-approval design gate). Implementation remains absent. docs/architecture/mentorship.md status updated from draft for the approved boundary/routes/contracts; AD-1 still gates feature delivery.
- (decision) USER-APPROVED mentorship departure effect: effective departure atomically sets openToMentoring=false; active pairs auto-close; pool and directory projections additionally require active employment; rehire requires explicit opt-in. Implementation absent. Recorded on PM/AD-17.
- (decision) USER-APPROVED closure tightening: CC-08 requires EmploymentStatus, isActive, import, TimeTracker non-writer, and re-import-cannot-restore-access proof. CC-06 requires all AD-23 participants, atomic UoW, fencing, retry/idempotency, and AD-20 operational release gate. CC-10 split into CC-10-MENTORSHIP (open, implementation) and ARCH-GOV-01 (closed after README/spine status). QUALITY-GATE-AC closes only on gate_status=PASS, p0_status=MET, critical_open=0, ACM3-II-06 covered. QUALITY-GATE-AC-NFR added for ACM-9 500/2s.
- (decision) Editorial: TD-09 decisions [] decision_refs [ACF/AD-3] artifact_refs [ACM-3]. TD-10 retired. TD-11 records five-minute frontend staleTime. Frontend dimension register added. CC-04 closure no longer requires architecture-approvals.yaml.
- (event) Batch update distilled. Spine status approved-with-open-items. No application code. No commit/push/tag. Package is NOT release-ready.
- (event) Ratification overlay 2026-09-02-architect-design-batch: register AD-26..35; closed CC-05 OQ-105 OQ-114 OQ-115 OQ-116 OQ-117 OQ-118; CC-07/09/11/DEPARTMENT-EDGE design-approved implementation-open; OQ-PERM-01 remains open; TD-12 added.
- (event) Correction overlay: H1 mentorship SPEC; H2 CC-04/CC-06 classification; H3 ARCH-ENV-01 and ARCH-PROJ-WRITER-01; H4 AD-7 key; H5 ACF register; H6 TT gates; H7/H11 stack debt; H8 verified AD-19 not design-blocked by CC-04.
- (event) Correction overlay applied: ARCH-ENV-01, ARCH-PROJ-WRITER-01, ACF register, TD-13, TT live gates, AD-7 key, H8 CC-04 note.
- (change) H5 follow-up: ACF spine Inherited Invariants PM/AD-23 row corrected to five-field applyDepartureEffects contract matching PM/AD-23.
- (change) Ratification §carry-over refreshed: second-pass bullets superseded table added; live open findings list no longer contradicts H2/H5/H7/H8 correction state.
- (event) 2026-09-02-doc-cleanup: editorial/status/traceability reconciliation from bmad-review §§1–2. Frontmatter revision synced to 2026-09-02-reviewer-gate-correction. §2 vocabulary mapping added; §7 retitled Blocker register with mechanical 18/8/4 counts; §11 condensed to pointer list. No new ADs; PM/AD-1..35 preserved. Historical enforcement notes moved out of normative §2.
- (decision) Doc-cleanup binds: assignee-only Action Item cancel wording; AD-28 scenarios not authored (no matrix/full-profile-access claim); mentorship gates canonicalize to blockers.yaml IDs only; UMAC empty-audience 403 annotated superseded at three sites only.
- (event) QUALITY-GATE-AC-NFR closed 2026-09-02 against acm9-final-acm9-1788173458416-ff94a3e685d1.json (ACM9-MVP-v1, role final, status PASS, stop_reason completed). Every closure clause verified independently rather than taken from the artifact's own verdict: 500 requested active targets on all 32 gates; warm p95 11.603 ms and worst case 12.357 ms against the 2 s absolute limit; step 2 four independent gate shapes (reporting, direct_pp, colleague, mixed); step 3 depths 5/25/50/100/200/300/400/499 in the prescribed order, 5 warm-ups and 20 measured samples per gate; step 5/6 baseline acm9-1788173258311-697e946d9f11 status PASS with protocol, fixture-manifest and environment-manifest hashes all matching, comparability comparable, final faster than baseline (11.603 vs 12.702 ms).
- (event) NFR closure currency check: artifact source_revision f89e034 is an ancestor of backend origin/main and `git diff f89e034..origin/main -- src/access-control/` is empty, so no resolver change landed after the measurement. Platform story 3-8 moved review to done. Blocker counts 18/8/4 to 16/10/4; the two live count assertions in ARCHITECTURE-RATIFICATION.md updated as a status edit under the governance rule that statuses and closure wording need no approval. gate-decision.json deliberately NOT regenerated: its gate_basis is priority_thresholds over trace coverage and does not include this blocker, so a rerun would restate the same verdict with a new timestamp.
- (event) 2026-09-03 bmad-correct-course pass: SEC-AUTH-01 status_note and TD-02 status_note added recording implementation evidence on the unmerged `dn-um-implementation` branch (services/backend) — both interim-session-resolver.adapter.ts and interim-access-control.adapter.ts deleted, replaced by jwt-session-resolver.adapter.ts and access-control-facade.adapter.ts respectively. Status stays `open` on both entries: the branch has not merged into main or this docs branch (diverged from a common ancestor at a9c6873, neither is an ancestor of the other) and the evidence has not been through this project's own verification process. ARCHITECTURE-RATIFICATION.md §5 P0/P1 transition risks annotated with a pointer to the same evidence, not rewritten — the existing description remains accurate for the code checked out on this branch (backend submodule on acm3-cap1-stage2 still has both interim adapters). A matching pointer note was added to the "Slice-level preconditions" description of SEC-AUTH-01 in the nine downstream epics.md files that carry it as a precondition/gate (profile-sharing, resourcing, platform-capabilities, timetracker, platform, cds, engagement, risk; user-management's sole mention is a design-caution example, not a status claim, and was left untouched). Purpose: pre-empt a stale-status merge conflict when dn-um-implementation lands; TT-IDENTITY-01 was checked in the same pass and remains genuinely open — no code on that branch addresses it.
