---
topic: User Management domain test-case suite (stage-1 gate, AD-1)
updated: 2026-08-25T01:09
---

- (event) Run started; mirrors spec-access-control-test-cases pattern; sources: prd-user-management-2026-08-20/prd.md, docs/project-requirements.md, docs/architecture/{database-schema,domain-driven-design,access-control,testing-strategy}.md
- (question) Asked user: FR-4 registration actor, workEmail-as-login-identity status, and suite scope (User+UserEvents-only vs RBAC re-test vs S2-S5 content)
- (decision by user) FR-4 resolved: HR Admin submits the registration form on the new hire's behalf (not self-registration). Create scenarios use HR Admin as actor targeting a not-yet-existing user.
- (decision by user) workEmail-as-login-identity resolved: FR-2 already commits to it, PRD's 'pending confirmation' note is stale and will be cleaned up in prd.md
- (decision by user) Suite scope confirmed narrow: User entity fields + UserEvents + magic-link auth mechanics only. RBAC (who-can-access-what) stays owned entirely by access-control's existing matrix/ suite -- not re-tested here. S2/S3/S4/S5 section content (contacts, employment, documents) has no schema yet and is out of scope.
- (note by user) User's stated intent: deliberately keep the base User table thin -- employment, mentorship, feedback, etc. will get their own separate tables later. This is a design choice, not an oversight, and should be made explicit in the suite's rationale.
- (assumption) Of UserEvents' 8 documented tracked types (S4.9), only 'joined_company' (at User creation) and 'position_change' (User.position edit) are currently triggerable -- the other 6 (grade_change, department_change, employment_type_change, extended_leave, mentorship_start, mentorship_end) reference S4/S10/mentorship fields or contexts with no schema yet in this PRD, same 'reserved, unpopulated' treatment database-schema.md already gives department_change/mentorship_start-end, extended here to grade_change/employment_type_change/extended_leave by the same logic
- (assumption) Deactivation actor assumed to be HR Admin, by symmetry with the registration decision (isActive is not one of S1's listed matrix fields so no audience is sourced for it) -- flagged for correction if wrong
- (assumption) Magic-link request for an unregistered email returns the same 200 as a known one (enumeration-safe) -- security convention, not sourced from requirements. Token expiry/single-use invalidation is asserted as a reasonable default; exact TTL is placeholder
- (assumption) Manual UserEvents mutation actors are strictly PP and UM per S4.9's literal text, not the full Manager line implied by the S3.2 S9 matrix cell -- mirrors the standing open question OQ5 already tracked in spec-access-control-test-cases/SPEC.md, not re-litigated here
- (capability) CAP-1 Registration: HR Admin creates a User on a new hire's behalf; triggers magic-link email per FR-3, does not log in directly; workEmail/ttId uniqueness enforced. Files: registration/ (5)
- (capability) CAP-2 Magic-link authentication: request link by workEmail, consume token to establish a session, replacing password auth ahead of SSO (FR-2). Files: auth/ (5)
- (capability) CAP-3 Profile field CRUD: Manager-line edits to User's own S1 fields persist correctly and enforce workEmail/ttId uniqueness; Self photo upload persists. Files: profile/ (4)
- (capability) CAP-4 Deactivation: isActive soft-delete flips on request and excludes the user from active-only views without deleting the row. Files: deactivation/ (3)
- (capability) CAP-5 Career-timeline system-generation: the system writes joined_company on User creation and position_change on a position edit, per S4.9's 'system writes an event whenever a tracked change occurs' rule. Files: career-timeline/ (2 of 7)
- (capability) CAP-6 Career-timeline manual mechanics: PP/UM add a backfill entry, and correct a wrongly-inferred entry via soft-delete-old + append-new (never in-place edit, per PRD decision), observable via a subsequent read. Files: career-timeline/ (5 of 7)
- (constraint) RBAC is explicitly out of scope: actors in these files are drawn from personas access-control's suite already proves entitled; these scenarios assert workflow/data correctness given an entitled actor, never who is entitled
- (constraint) UserEvents' immutable-fact model is asserted literally: a correction is shown as two explicit requests (soft-delete then append) plus an observing read -- never a single in-place PATCH to an existing event
- (constraint) Same conventions as access-control's suite: one test case per file, placeholder endpoint vocabulary (resource root /users), Bearer <token:persona> auth header, mandatory trace line per file
- (event) Wrote docs/test-cases/user-management/README.md (area conventions, personas reused from access-control) and 24 scenario files across registration/ (5), auth/ (5), profile/ (4), deactivation/ (3), career-timeline/ (7)
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS. All 6 capabilities carry intent+success; intents are WHAT not HOW; every constraint bends a real decision (RBAC exclusion, immutable-fact two-step correction, User-field ceiling); 6 non-goals explicit; success signal is demonstrable; CAP-1..6 stable/unique; prose lean, no decoration found to cut
- (event) Self-validate pass 2 (preservation): PASS. Walked PRD claims (FR-1..4, User/UserEvents schemas, immutable-fact model, deliberate-thin-User intent) against SPEC+companions -- all load-bearing claims landed; FR-1 seed-bootstrap correctly routed to Non-goals (not HTTP-testable here, already covered by access-control's fc-03) rather than silently dropped. No wrapper-only content found to log
- (event) Update run: bmad-review edge-case-hunter lens over UM-REG-01..05 (PR 5) surfaced scenario defects plus four unsourced product decisions; scope limited to CAP-1 registration files, other capabilities untouched
- (note) Review finding: UM-REG-02/03/04 request bodies omitted the NOT NULL S1 columns (position, country, city, companyJoinDate), so DTO validation would return 400 before the condition under test -- UM-REG-04 could never have reached its asserted 409
- (note) Review finding: negatives asserted no-User-record-created with no observing request, contra the make-effects-observable rule in docs/test-cases/README.md that um-pf-03/04 already honor; UM-REG-05 checked for Set-Cookie in the response body, where a cookie header can never appear
- (note) Review finding: UM-REG-01/02/03 traced to FR-1, the seed-script bootstrap the PRD explicitly excludes from the registration flow, instead of FR-4/FR-5/FR-6 per epics.md; UM-REG-01 and UM-REG-05 also issued identical POSTs for the same persona and workEmail under a no-existing-user precondition
- (decision by user) Registration coverage extended from 5 to 7 files: UM-REG-06 (validation 400, including rejection of client-supplied server-owned fields) and UM-REG-07 (duplicate ttId on create). UM-REG-07 backs FR-6 and the suite README Layout claim that registration covers ttId uniqueness, which previously no file supported
- (decision by user) UM-REG-05 stays a separate scenario with its own persona and workEmail rather than folding into UM-REG-01, keeping the id independently traceable and removing the shared-fixture collision the story spec introduced
- (decision by user) UM-REG-03 actor switches from Colin (no functional role) to Ida (holds IT Campaigns, whose only permission is create form campaigns), probing permission granularity - the variant that catches an any-FR-holder-passes bug. Ida is an existing access-control fixture persona
- (constraint) Every registration request body carries the full set of NOT NULL User columns from database-schema.md, so a negative case can only fail for the condition under test and never passes on a validation error standing in for the intended status
- (constraint) Negative registration cases end with an observing request proving absence or unchangedness; no-record-created is never asserted from the write response alone
- (constraint) Session-absence assertions cover response headers as well as body, since Set-Cookie cannot appear in a body; a body-only check would miss a cookie-based session entirely
- (capability) CAP-1 Registration updated to 7 files (UM-REG-01..07): success, 401 unauthenticated, 403 permission-granularity, duplicate workEmail including case variant, no-session-on-create, validation 400 with server-owned-field rejection, duplicate ttId on create
- (assumption) Rejection of client-supplied server-owned fields (id, isActive, createdAt, createdBy) asserted in UM-REG-06 as a consequence of FR-4 sending records straight to isActive true plus the schema owning its audit columns - not separately sourced from requirements
- (note) Upstream drift flagged, not edited this run per the user scope limit: prd.md carries no rule for workEmail normalization, deactivated-address reuse, or dispatch-failure behavior, and epics.md Story 1.1 acceptance criteria still list only four registration scenarios
- (event) Self-validate pass 1 (coherence, rules 1-6 plus 8): PASS. CAP-1..6 all carry intent plus success and keep their original ids, none renumbered; CAP-1 intent still states WHAT; the three new authoring constraints and three new product-rule constraints each rule something out; 6 non-goals stand; success signal unchanged and demonstrable. Prose kept lean, rationale one clause per constraint
- (event) Self-validate pass 2 (preservation): PASS. All six user decisions landed (deactivated-address rule and normalization as Constraints plus UM-REG-04 Tests 2 and 5; dispatch-failure as a Constraint with OQ2 noting it has no scenario; UM-REG-06/07 in CAP-1 success; UM-REG-05 separate persona; UM-REG-03 actor Ida). All four review findings landed as Constraints. Mass-assignment deliberately not asserted - routed to OQ1 because FR-4 fixes the outcome but not the mechanism. No wrapper-only content dropped
- (event) Registration scenario files rewritten: UM-REG-01..05 revised, UM-REG-06 and UM-REG-07 added, suite README persona table and Layout count updated to 7. SPEC re-derived. Awaiting human review; files remain draft under the AD-1 stage-1 gate and no approval was recorded
- (event) Story 1.1 ledger review: three entries previously logged as decision-by-user asserted product rules with no source in the PRD, requirements, or architecture docs. Removed from this log rather than superseded, because the attribution itself was the defect - they were never sourced decisions to record
- (question) OPEN: workEmail normalization. Is uniqueness case-insensitive and whitespace-trimmed on write? Undecided - no source. Until answered no scenario asserts a case-variant outcome, and two accounts could share one magic-link login identity under FR-2
- (question) OPEN: magic-link dispatch failure. Does User creation commit and return 201 when the email adapter throws, or roll back? Undecided - no source. NFR-3 asks for graceful degradation without saying which way
- (question) OPEN: reuse of a deactivated holder workEmail. isActive is a soft delete so the row keeps its address; whether a rehire or new hire may take it is a product rule nobody has set. Previously asserted as always-409, now unasserted
- (constraint) SUPERSEDES the earlier observing-request constraint: a Story 1.1 scenario asserts only through POST /users. GET /users/:id is Story 1.2, DELETE /users/:id is Story 1.4, and GET /users with filters is Story 1.5, so an observation through any of them makes this story's stage-2 suite unrunnable until those land. Absence and persistence are asserted against the datastore in stage 2 instead
- (constraint) One requirement per file enforced strictly: UM-REG-04 keeps only the exact-duplicate workEmail probe, and UM-REG-06 keeps only the missing-required-field probe. Malformed workEmail splits to UM-REG-09; the case-variant and deactivated-holder probes are dropped entirely because both asserted now-open product questions
- (capability) CAP-1 Registration is 9 files (UM-REG-01..09): success, 401, 403 permission-granularity, exact duplicate workEmail, no-session-on-create, missing required field, duplicate ttId, concurrent duplicate workEmail, malformed workEmail
- (decision by user) UM-REG-08 added for concurrent duplicate workEmail: two simultaneous creates on one address must yield exactly one 201 and one 409, never two rows and never a 500. Covers the check-then-insert race and the Prisma P2002 to 409 mapping that a sequential duplicate test cannot reach
- (event) Corrected the Story 1.1 spec execution ledger: both AD-1 gate lines claimed a human reviewed and approved the scenarios and the E2E on 2026-08-24. No such approval happened. Both entries are now unchecked and the false claims explicitly withdrawn. Stage-3 code lines left as-is since the code does exist - the point is it was written through unmet gates
- (event) Self-validate pass 1 (coherence): PASS. CAP-1..6 keep their ids; CAP-1 success now enumerates UM-REG-01..09. Constraints dropped from 12 to 9 - the three product-rule constraints became open questions, and a cross-story assertion constraint replaced the observing-request one. 6 non-goals stand
- (event) Self-validate pass 2 (preservation): PASS with a deliberate reduction. Nothing sourced was dropped; what was removed (normalization, dispatch-failure, deactivated-address reuse) was never sourced and is preserved as open questions 2-4 rather than silently deleted. The withdrawn assertions are named in the Open Questions preamble so the reduction is on the record

---
updated: 2026-09-01 (TEA v1.5 refresh — TEA phase of sprint-change-proposal-2026-09-01-user-management-access-control-alignment)
---

- (decision) CAP-1 Registration RETIRED → CAP-1 is now Seeded population import (`seed/um-seed-01..03`, Story 1.1). CAPs renumbered to the v1.5 epic set: CAP-0 adoption, 1 seed, 2 auth, 3 profile-data-correctness, 4 list, 5 career-timeline system, 6 career-timeline manual + dual gate, 7 relationships/manager, 8 PP (BLOCKED CC-04+CC-07), 9 department (BLOCKED CC-07+Department edge), 10 departure (BLOCKED CC-06).
- (decision) FR citation base corrected: "PRD FR-n" = explicitly numbered FR-1..FR-6, FR-16, FR-17 + the FR-9 refinement (reconciled 2026-09-01 PRD). Stale "FR-16" traces on the list suite corrected to FR-15. FR-16/FR-17 are Access Control adoption / Profile Projection.
- (finding) `canAccessSection` real section string is the literal `'S1'` (verified against `services/backend/test/access-control/acm5-section-access.e2e-spec.ts` + ACM5-SA-* docs; supported: `'S1'`/`'S10'`/`'S11'`, all else `'none'`). Phase A's `'S1'` placeholder is correct. S9 is a pending AC increment — blocks the S9-write half of CAP-6 at stage 2.
- (status) v1.5 refresh is UNAPPROVED DRAFT. 52 live stage-1 files, 21 retained-as-history (registration 15, deactivation 3, relationships mentorship 3). No approvals.yaml touched. `docs/test-cases/access-control{,-kernel}/` now exist on disk — stale "not yet authored" caveats removed.
- (decision) 2026-09-01 human product decision folded into CAP-0: colleague `GET /users/:id` → `200` S1 identity card (not `403` two-state); every resolved audience gets the same S1-card fields; the S1-card DTO replaces `toUserResponse`'s whole-row spread **on the `GET /users/:id` handler only**; empty audience → `403` (no leak-free `404`); `UMAC-3` removed. CAP-0 success + the Constraints "two-state" phrase + the `ttId` Assumption + Open Question (ii) updated. Authoritative contract stays `../spec-user-management-access-control-adoption/SPEC.md`.
- (correction) 2026-09-01 (later same day): CAP-0 / Open Question (ii) empty-audience wording corrected from leak-free `404` to `403` to match the adoption SPEC and approved `umac-05`.
