---
topic: Functional Roles catalog — the FR dimension of AD-6 (permission catalog, runtime roles, isAllowed)
updated: 2026-08-30
status: unapproved draft; no human review, no validation pass claimed
---

- (direction) Author a SPEC for the Functional Roles catalog slice — AD-6's second dimension, absent from the codebase — mirroring the house pattern of spec-access-control-audience-foundation and spec-access-control-facade-audience-resolution. Planning only: no code, tests, schema, or migrations; nothing under services/; no git operations; services/backend and services/frontend untouched except for read-only inspection.
- (direction) Explicit standing instruction: do not invent answers to open questions. Inventing a contract is the failure mode being avoided.
- (fact) Confirmed by reading services/backend/prisma/schema.prisma (read-only): models are User, Relationship, Project. Policies, Permissions, and UserPolicies do not exist. src/access-control/ exists as a hexagonal context holding the audience resolver, a relationship-graph port, a Prisma adapter, and a facade — and has no controller.
- (decision) Bounded context: the existing access-control context, not a sibling. AD-5 assigns both role dimensions to it; AD-7 puts both on the same three tables; a sibling would give two contexts write access to those tables, which AD-2's entry-point rule cannot express cleanly. Cost recorded rather than hidden: one context now mixes a read-only hot path with a mutating admin surface, so AD-7 type separation becomes a tested convention rather than a structural boundary.
- (capability) CAP-1 permission catalog as data; CAP-2 runtime-created roles with mutable permission sets, no deploy and no schema change; CAP-3 immediate revocation, which forbids caching; CAP-4 isAllowed as a global, target-free, type-separated decision; CAP-5 fail-closed with exactly one bootstrap HR Admin attachment and no other default grant.
- (constraint) FR rows only; operator == only; managedBy 'admin' only; no stored or cached FR decision; FR never widens data access; AD-1 blocking and ordered; AD-20 due-actor cutoff precedes any feature decision.
- (constraint) The catalog's HTTP shapes are established by the Stage-1 scenario document, never by this SPEC — api-conventions.md line 30 leaves them deliberately unspecified.
- (decision) Non-goals each carry their reason: default assignments (PO gate, three unsettled points), UI, AR rows and the AR hot path, policy-level IN and !=, managedBy 'sync', the dual-dimension write gate (needs the section matrix), enforcement on existing endpoints (UM-owned), re-authoring AC-FP-01..03, and the §3.4 journal schema (CC-07).
- (question) OQ-1 attachment route ownership — three options with costs recorded, none chosen; option (c) /roles/:roleId/members would be a new route absent from api-conventions.md and needs a deliberate amendment.
- (question) OQ-2 catalog request/response shapes undefined pending its own AD-1 scenario doc.
- (question) OQ-3 database-schema.md defines no join between Policies and Permissions, yet PATCH /roles/:roleId/permissions presumes a many-to-many. Largest gap; blocks any migration.
- (question) OQ-4 an FR Policies row has no defined shape — targetType/targetId are AR-shaped and non-null, a global feature grant has no target, and POST /users/:id/policies takes a policy definition rather than a catalog-row reference.
- (question) OQ-5 no permission in §2.3 gates the catalog itself; a name check on "HR Admin" would contradict §2.3 and AD-6.
- (question) OQ-6 whether Permissions is a closed seeded set or runtime-extensible.
- (question) OQ-7 canonical feature identifier and spelling; prose names in §2.3 vs kebab-case examples in database-schema.md; enum vs string interacts with OQ-6.
- (question) OQ-8 "view a given dashboard" is parameterized but Permissions has no parameter column and the dashboard engine is design-pending.
- (question) OQ-9 role deletion semantics with holders attached; no last-holder protection stated for FR, unlike §2.4.
- (question) OQ-10 whether FR mutations are journaled; §3.4's list omits them; a yes inherits the CC-07 gate.
- (question) OQ-11 the facade spec's CAP-3 already claims isAllowed and the bootstrap seed; ownership must be settled so two implementations do not land.
- (question) OQ-12 §2.3 states no self-assignment bar for functional roles, unlike §2.1 and §2.4.
- (note) Source discrepancies found while reading, recorded rather than silently normalized: §2.3 lists sixteen granular permissions, not fifteen; database-schema.md's example title 'assign-mentors' matches no §2.3 bullet ("assign and end mentorships"); the spine's Deferred entry refers to catalog scenarios as `users/roles/` while api-conventions.md and the test-case README both fix the route as top-level /roles.
- (note) Value caveat recorded in Why: most of the sixteen permissions gate features that do not exist. Real or near consumers are `change organisational relationships` (Epic 4, 4-1 in progress), `edit the career timeline` (Epic 3, in progress, though its default holder is PO-unsettled), `record a departure` (named in api-conventions.md for the AD-20 routes, unbuilt), and `manage departments` (department model unbuilt).
- (note) Forward link recorded as sequencing, not as delivered value: manage departments gates the §4.17 department model, which in turn gates two withheld audience cells on the Phase-0 resolver — department management as a Reporting-line relation, and the HR line above an assigned People Partner (AD-19).
- (note) No validation pass is claimed. Unlike the sibling specs' memlogs, this file records no coherence or preservation PASS event, because none was independently performed and AD-1 forbids self-certification.
- (note) deferred-work.md's first entry still reads `source_spec: none`. It is left unchanged: this SPEC is unapproved, so pointing the entry at it would assert an approval that does not exist. Story 3 carries the update.
- (decision) 2026-08-30 Correct Course approved the Kernel MVP planning boundary: ACM-1/ACM-2 reserve minimal FR schema/seed and isAllowed ownership, but remain blocked until FR-AMD-1 independently resolves OQ-3, OQ-4, OQ-6, OQ-7, and OQ-11.
- (constraint) The later runtime catalog consumes the approved ACM-1/ACM-2 foundation and must not create a second migration, seed, bootstrap attachment, repository port, or evaluator. This planning decision does not approve this catalog draft or authorize a dispatch.
