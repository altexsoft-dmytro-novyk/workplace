---
topic: Access-control test-case suite: stage-1 quality-gate scenario documents (one scenario per file) under docs/test-cases/access-control/, covering §2 roles, §3 access matrix, 4.8 sharing, fail-closed rules
updated: 2026-08-29T22:46
---

- (direction) User mandate: docs/test-cases/access-control/**.md, one test-case/scenario per file, split to minimal scenarios, access-control logic ONLY; prior decisions in docs/* and architecture memlog are binding input
- (decision) Express mode, autonomous run: input is rich (requirements §2-3 NORMATIVE + arch spine AD-6..13 + AD-1 gate format); gaps become assumptions/open questions instead of blocking elicitation
- (decision) Suite structure: 6 groups - tier-derivation (TD), functional-roles (FR), fail-closed (FC), matrix/sNN-slug per-cell files, shared-link (SL), surfaces (SF); stable IDs AC-<group>-NN for stage-2 E2E tracing
- (decision) Canonical persona fixture (Alice target; Bob UM, Carol UM's mgr, Pete PM, Dave DM, Frank Dave's mgr, Paula PP, Hana PP's mgr, Colin colleague, Root HR Admin, Ida custom-FR holder, Eve no-edges) + placeholder endpoint vocabulary defined once in README; scenarios reference both, real routes bound at stage 2
- (decision) Path x section cross-product NOT enumerated: relationship paths proven in TD group, sections per audience in matrix group; composition guaranteed by single-resolver AD-10; manager persona varied across matrix files (Bob/Carol/Dave/Pete/Frank) to spread path coverage anyway
- (decision) Denial convention: write to readable section = 403; any request to a '—' section = 404 with no section data in body (covers §3.3.1 error-message leak); invisibility asserted as key ABSENT from payload, never null/empty
- (decision) Out of scope per user + iteration constraint: performance NFR tests, notifications 4.13, UI-rendering assertions (API-level only per §3.3.4), non-access-control feature workflows
- (capability) CAP-1 Tier derivation scenarios: every §2.1 relationship path (direct, transitive, PM, DM, compound reports-to+project, PP, HR-line-above-PP, per-target evaluation, non-sticky revocation) has one scenario file; success = TD-01..13 each translatable to a red E2E without extra context
- (capability) CAP-2 Matrix cell scenarios: every §3.2 cell for Self/Manager-line/PP/Colleague incl. flag-gated S7/S8 records and per-field S16 visibility, one file per cell; success = negative file exists for every '—' cell + unflagged S7 vs employee AND vs PM (§9 DoD)
- (capability) CAP-3 Functional-role scenarios: grant/deny/independence/immediate-revocation/no-data-widening/runtime-extensibility/admin-only-management/AR-not-extensible per §2.3; success = FR-01..09 cover every §2.3 bullet
- (capability) CAP-4 Shared-link scenarios per 4.8: cfg eligibility, never-shareable S3/S7/S13, sensitive-default-off S2/S5/S6/S8, S1 default-on, enabled-only visibility, read-only, expiry default+configurable, revocation, access logging, creator authorization; success = SL-01..11
- (capability) CAP-5 Fail-closed scenarios per AD-11/12: empty reportsTo grants nothing, orphaned policy grants zero, bootstrap admin is ordinary revocable FR; success = FC-01..03
- (capability) CAP-6 Cross-surface leak scenarios per §3.3.1/3.3.3/3.3.5: colleague whitelist exactness, xlsx export, list columns, filter inference, inline edit, direct-request no-leak; success = SF-01..06
- (assumption) S1 derived subfields (manager, PP, mentor, current projects) read-only for ALL audiences incl. Manager-line RW cell; RW applies to directly-stored identity fields — matrix silent on this
- (assumption) S10/S11 are R for every audience: write attempts denied 403 for all incl. Manager/PP (data owned by timetracker sync)
- (assumption) Shared-link viewer modeled as authenticated employee without Manager/PP relation to target (4.7 DM-candidate case); anonymity question left open
- (assumption) TD-13 department-manager scenario included per arch spine new-scope direction (Departments generalize §2.1 relation 2) though absent from requirements doc — provisional pending requirements owner
- (question) OQ1 (mirrors arch memlog, user checking ~2026-08-20): non-manager project assignment semantics (info-sec training case) — may add tier scenarios later
- (question) OQ2: does a shared link require authentication, or is the token bearer-access? 4.8 silent; SL scenarios currently auth-agnostic where possible
- (question) OQ3: §3.1 grants HR Admin full data access, but §2.3 says FR never widens data access — mechanism unresolved in architecture (seeded AR-type policy attached to HR Admin role?); scenario asserts §3.1 behavior, mechanism left to architect
- (question) OQ4: S7 'visible for employee' + shared-link column is '—' — confirmed no interaction; but does employee-flagged note appear in ANY export? Assumed no (S7 never leaves UM/DM/PP/flag-gated readers)
- (question) OQ5: S9 write scope conflict — §3.2 gives Manager line RW on career timeline, §4.9 grants manual add/edit/delete to 'PP and UM' only; can a DM/PM edit timeline events? Scenario AC-M-S09-02 uses a UM actor (safe under both readings) pending resolution
- (note) Source inconsistency: §4.8 lists S3/S7/S13 as never-shareable but §3.2 shared-link column also marks S14 '—'; matrix strictness (§3.3.1) wins — SL-02 asserts S14 unshareable too, flagged for requirements owner
- (decision) Rendered 117 scenario files + README under docs/test-cases/access-control/: TD 13, FR 9, FC 3, matrix 75 (incl. hr-admin-full-access), SL 11, SF 6; README carries conventions, persona fixture, endpoint vocabulary, index
- (decision) Spec-authored README lives outside the spec folder (user-mandated location docs/test-cases/access-control/) and is the sole spec-authored companion; 117 scenario files discovered via the README index, not frontmatter, to avoid bloat; docs/architecture/access-control.md adopted as companion
- (note) Deferred to feature-owned suites (drop on record, not silent): dashboard composition/scoping positives (4.4, 4.6 risk-dashboard scope beyond the Self-negative in AC-M-S06-01), resourcing workflow states (4.7), custom-field creation mechanics (4.1), saved-view sharing; each renders through the same AccessControl facade so section/tier coverage here still binds them
- (event) Self-validate pass 1 (coherence): PASS — CAP-1..6 all carry intent+success; intents are WHAT-level; all 6 constraints rule something out; 5 non-goals; success signal reviewable; IDs fresh and unique; prose lean
- (event) Self-validate pass 2 (preservation): PASS — walked §2.1/§2.2/§2.3/§3.1/§3.2 (all 4 audience columns x 16 sections + shared-link column)/§3.3.1-5/§4.1 access bullets/§4.3/§4.5 scope/§4.8 all bullets/§4.9-4.12/§4.15/§9 DoD negatives against the 117 files; every load-bearing access claim lands in a scenario or the README; workflow-feature content deferred per note above; 2 source tensions surfaced as OQ5/OQ6 rather than silently resolved
- (direction) User feedback 2026-08-19 on fr-06: (1) functional-roles renamed to users/roles — folder AND endpoint (POST /users/roles); (2) scenario format wrong — must describe behavior as explicit request specs: inputURL, inputRequest{headers.authorization, body}, expectedResult with HTTP status; auth-layer negatives (401 unauthenticated, 403 without-permission) are first-class scenarios, one per file
- (decision) Full regeneration (~200 files): strict split — one requirement per file, read and write as separate files; multi-request only when one requirement is inherently compound (per-target tiers, never-shareable set, no-read-path probes); resource root renamed employees->users for consistency with users/roles; token convention 'Bearer <token:persona>' documented in README
- (decision) 401 coverage per endpoint family, placed in owning group: users/roles create-role-unauthenticated (user's exemplar), matrix AU-01..03 (profile read, section read, section write), sl-12 create-link (open-link 401 blocked by OQ2), sf-07 list, sf-08 export; README states global rule: every endpoint rejects missing/invalid authorization with 401
- (decision) ID scheme extended: AC-UR-* (users/roles), AC-M-AU-* (matrix auth); files generated via scratchpad script from per-file data to guarantee format uniformity
- (capability) CAP-7 Authentication scenarios: every endpoint family rejects missing/invalid credentials with 401 before tier reasoning; success = au-01..03, UR-01, SL-12, SF-07/08 + README global-401 rule bound per route at stage 2
- (event) Regeneration complete: 202 scenario files (TD 13, UR 12, FC 3, matrix sections 148 + au 3 + hr-admin 3, SL 12, SF 8) + README; SPEC.md re-derived (CAP-2/3/4 successes updated, CAP-7 added, constraints rewritten for request-spec format)
- (event) Self-validate pass 1 (coherence) after regeneration: PASS — CAP-1..7 carry intent+success; format constraint now matches user's exemplar (verified against rendered UR-01 and S07-01); IDs unique (UR-*/AU-* new, FR-* retired not reused)
- (event) Self-validate pass 2 (preservation) after regeneration: PASS — re-walked §2.1/§2.3/§3.2 all cells x read+write/§3.3/§4.8/§4.9-4.15/§9 DoD against the 202 files; no claim lost in the reformat; user feedback items both landed (users/roles naming incl. endpoints, request-spec format with 401/403 decomposition)
- (direction) User 2026-08-19: extract the test-case pattern to docs/test-cases/ level so every team member follows the same structure and can update the rules
- (decision) Pattern generalized into docs/test-cases/README.md (structure, file skeleton, ID scheme, granularity rules 1-5, status-code conventions, ownership per AD-1/AD-4, change process: team PR, spine-first on conflict); access-control/ named the reference implementation; testing-strategy.md gate step 1 now links the pattern doc (amplifies AD-1, no rule changed); access-control README points up to avoid drift
- (note) Flag to architect: on next spine re-render, AD-1's rendered rule should absorb the pointer to docs/test-cases/README.md so the link survives regeneration
- (direction) User 2026-08-19 on AC-M-HR-03: meaningfulness — a state transition must not hide in Preconditions; show it as steps: baseline request proving the before-state, the request performing the change, the request observing the after-state
- (decision) Pattern rule replaced (supersedes 'temporal setups become preconditions'): Preconditions = static seeded state only; transitions are explicit steps; non-API changes (sync-owned data, clock) written as stateChange steps; writes end with the request observing the effect. docs/test-cases/README.md updated (rules + granularity rule 5)
- (event) Rewrote 11 files as cause->effect sequences: HR-03 (Ida as delegated second admin revokes Root), TD-11/TD-12 (stateChange for sync-owned edges), UR-10 (4 steps incl. second holder Colin), UR-06 (403->assign->201), FC-02 (baseline->delete->colleague), FC-03 (delegate->revoke->403->colleague), SL-07/08 (clock as stateChange), SL-09 (baseline->revoke->dead), SL-10 (two accesses then log read), S08-06 (absent->share->present); grep confirms no 'moments ago' preconditions remain
- (direction) User 2026-08-20 on AC-M-S06-05: files must carry a plain-language description of the scenario, not just the request spec — a reader could not tell who Paula is or why she is entitled
- (decision) Mandatory '## Scenario' section (Given/When/Then prose: actor+relationship, action, expected outcome with the why) added between Trace and Preconditions in every file; pattern doc skeleton and rules updated; all 202 files regenerated with scenario prose; grep verified no file lacks the section
- (note) Route-collision discussion (users/roles vs /users/{userId}, also /users/export) parked at user's request — options A (top-level /roles, recommended) vs B (UUID-constrained userId param) presented 2026-08-20, decision pending; routes unchanged
- (direction) User validation/update 2026-08-29: refresh contract to v1.5 (Reporting line / Project line split, Phase 1 gates from facade spec); stale 202-file index and pre-v1.5 Manager-line language must not survive
- (decision) Headless express update: inputs are facade spec, access-control.md v1.5, deferred-work.md, test-design-qa-platform correction; pre-v1.5 suite withdrawn in 6086491 with zero scenario files on disk — this run refreshes the contract only, not scenario authoring
- (decision) v1.5 audience model binds this spec: Reporting line and Project line are separate §3.2 columns and separate graph passes; Phase 1 resolves Self, recursive direct Reporting, directly assigned PP, and Colleague; Project, Department, and PP HR-line contribute no audience until their contracts and AD-1 suites are approved (facade-contract.md gates)
- (decision) Retire merged Manager-line vocabulary in capabilities, constraints, assumptions, and README; matrix and derivation groups name Reporting line and Project line explicitly
- (decision) Suite layout re-scoped for Phase 1: audience-derivation/, matrix/ with self/, reporting-line/, pp/, colleague/, and project-line-gate/ (withhold negatives only), fail-closed/, auth/; defer shared-link/, surfaces/, users/roles catalog, hr-admin data-access, and full-profile/ to deferred-work.md separate dispatches
- (decision) HR Admin is configuration FR only (§2.2); it is not a matrix audience and has no default profile data access — remove hr-admin-full-access scenarios; full-profile overlay stays gated on product decision
- (decision) OQ2 resolved 2026-08-25: shared links require authentication in Iteration 2; shared-link scenario suite still deferred to its own spec per deferred-work.md
- (capability) CAP-1 Phase 1 audience derivation: every Self/Reporting/PP/Colleague path and every Phase 1 gate negative in facade-contract.md (Project withheld, no cross-kind inheritance, Department policy, PP HR-line withheld, AD-20 departure positions, empty bulk, broken edge, orphan policy) gets one scenario file under audience-derivation/
- (capability) CAP-2 Phase 1 matrix cells: every §3.2 Self/Reporting/PP/Colleague cell asserted per read/write action under matrix/; every — cell has a negative file; project-line-gate/ holds Phase 1 withhold negatives; S7/S8 flags and S16 visibility covered for Phase 1 audiences only
- (capability) CAP-3 Functional-permission boundary: isAllowed is separate from audience; dual-gate 403, FR-without-audience 404, bootstrap admin ordinary revocable — minimal files under functional-permission/; full role-catalog UI/API scenarios deferred
- (capability) CAP-4 Fail-closed scenarios per AD-11/12: empty reportsTo, orphaned policy row, bootstrap admin as ordinary revocable FR — fail-closed/
- (capability) CAP-5 Authentication scenarios: 401 per representative endpoint family under auth/ plus README global-401 rule; profile/list/export 401 negatives deferred to owning-context projection specs
- (assumption) S1 derived subfields remain read-only for every audience; Reporting-line and Project-line RW cells apply only to directly stored identity fields once Project line is enabled
- (assumption) S10/S11 R cells mean no write path for any audience — timetracker sync owns the data
- (assumption) DEC-UM-001 S9 manual write remains limited to assigned PP and direct UM even when Reporting line matrix shows RW — matrix read scenarios may use DM/PM actors; write scenarios use Bob or Paula only
- (question) OQ1: non-manager project assignment without PM/DM policy — info-sec training case; may add tier scenarios when product decides
- (question) OQ2: full-profile overlay §3.2 column mapping and Self precedence — blocks full-profile/ scenarios
- (question) OQ3: Department nested membership and HR boundary — blocks department-walk positives beyond current withhold negatives
- (question) OQ4: timetracker assignment/freshness contract — blocks Project-line positive matrix cells and PM/DM derivation positives
- (note) Wrapper-only drop on record: prior success criteria citing 202 existing files, FR-01..09, UR-12, SL-01..12, SF-01..08, hr-admin/, and merged Manager-line matrix counts describe the withdrawn pre-v1.5 suite shape only
- (event) Self-validate pass 1 (coherence) after v1.5 refresh: PASS — CAP-1..5 carry intent+success; intents are WHAT-level; constraints rule something out; non-goals explicit; success signal reviewable; capability IDs preserved (CAP-3..7 renumbered to CAP-3..5 with CAP-4/6 retired to non-goals); prose lean
- (event) Self-validate pass 2 (preservation) after v1.5 refresh: PASS — walked facade-contract Phase 1 gates, access-control.md v1.5 audience columns, §3.2 Phase 1 cells, §3.3 exceptions, AD-11/12/20, deferred-work boundaries; every load-bearing Phase 1 claim lands in SPEC.md or README companion; withdrawn-suite and deferred-scope drops logged
- (decision) Capability IDs preserved per Spec Law: CAP-1..3 and CAP-5/7 remain active with updated intent/success; CAP-4 (shared-link) and CAP-6 (cross-surface) retire from Capabilities and move to Non-goals — IDs never reused
- (event) Self-validate pass 1 correction: CAP-1,2,3,5,7 active; CAP-4 and CAP-6 retired to non-goals; no renumbering
- (event) Phase 1 scenario authoring complete: 167 draft files under docs/test-cases/access-control/ (AD 18, FC 3, AU 3, FP 3, matrix 128); generators at _generate_hand.py and _generate_matrix.py; pending per-file AD-1 approval
- (decision) Story breakdown: eight stories in execution order — seed fixture, four AD-1 approval batches by folder group, two stage-2 E2E translation batches, milestone handoff to facade dispatch; E2E stories invoke services/backend only after per-file stage-1 approval in their scope
- (decision) Checkpoints: spec_checkpoint true on every story (human gate); done_checkpoint true on story 6 after last AD-1 batch and on story 8 after E2E committed red
- (event) Story breakdown schema check: PASS — eight entries, quoted unique ids 1-8, prefix-free, no status field, required fields present
- (decision) Story breakdown trimmed to stage-1 only (stories 1–6); E2E stories moved to spec-access-control-facade-audience-resolution per SPEC non-goal; story 6 handoff points to facade stories 1–2
- (event) Story breakdown schema check after trim: PASS — six entries, ids 1–6, handoff on story 6 invoke_dev_with
- (event) Story 1 implemented in services/backend: migration access_control_fixture, seed module (24 users, graph, policies, S7 notes, AD-20 departures), fixture-ids + session resolver persona lookup; AD-08/13/15/16/17 aligned to variant target ids
- (decision) P-3 review 2026-08-29: OQ1 closed — project membership without PM/DM policy grants no Project audience in Phase 1; withhold negatives stand, no info-sec training positives until product decides
- (decision) P-3 review 2026-08-29: OQ5 closed — never-shareable set is exactly {S3, S7, S13, S14}; matrix shared-link column and §4.8 align; cfg sections default-off, S1 default-on, sensitive re-enablement recorded as resolved facts for deferred shared-link dispatch
- (decision) P-3 review 2026-08-29: OQ3 rewritten — fixed v1.5: department management is Reporting-line relation; PP HR-line propagates within HR once Department contract approved; open: nested membership representation, indexed traversal plan, event/state sync details
- (decision) P-3 review 2026-08-29: OQ4 rewritten — fixed v1.5: project-derived access withdraws within 15 minutes of assignment end and after four hours of failed sync; open: timetracker representation, query seam, partial/intermittent-sync behavior
- (decision) P-3 corrections: Self S5/S12/S13/S14 matrix exceptions — certificate upload, IDP complete, mentorship flag only, action-item complete; broader mutations stay denied
- (decision) P-3 corrections: Self S7/S8 assert record-level filtering with seeded note ids; S8 feedback ids bound when Feedback model seeds
- (decision) P-3 corrections: colleague S10/S11 assert dates-only and project-name-only payloads with forbidden fields absent; S16 colleague positive uses colleagueVisibleField; hidden fixture uses management visibility with deterministic 200 absent/present
- (decision) P-3 corrections: FC-01 TopLee issues empty-reportsTo denial; FC-02 baseline 200 on live PM policy join then orphan 404; AD-13 Morgan proves read/write merge across differing columns; AD-14 binds 403; AD-15 binds dismissed-target projection fields and write denial; AD-12 binds {items:[]}
- (decision) P-3 corrections: matrix generator replaces stage-2 shape placeholders with section field assertions; FC-02 sole project-line positive exception for orphan fail-open detection
- (event) Regeneration after P-3: 171 scenario files + README (172 total); self matrix 38 (+4 exception writes); generators _generate_matrix.py and _generate_hand.py updated
- (event) Self-validate pass 1 (coherence) after P-3: PASS — CAP-1..5/7 intact; OQ1/OQ5 closed; OQ3/OQ4 split fixed vs open; constraints and assumptions updated; prose lean
- (event) Self-validate pass 2 (preservation) after P-3: PASS — P-3 review corrections landed in memlog, README, generators, and 171 scenario files; deferred shared-link/surfaces/project positives unchanged except FC-02 orphan arc
- (event) 2026-08-30: story 1's services/backend output was reverted on the user's instruction — schema/migration/seed/fixture-ids removed and the User Management session-resolver edit restored; recoverable as backend `stash@{0}`. Story 1 is not done: the dispatch exceeded its declared seed/import-only scope (it edited `src/user-management/**` and created `departures` + `management_notes`). Re-run it scoped to the foundation subset once the User Management integration contract lands.
- (correction) 2026-08-30: stories.yaml counts realigned to the post-P-3 regeneration event above — matrix/self is 38, not 34; the stage-1 total is 171, not 167. Stage-2 translation targets Jest + supertest (`test/jest-e2e.json`), not Playwright; the backend has no Playwright harness.
- (correction) 2026-08-30: `_generate_matrix.py` left `{targetId}` unsubstituted in self-write-exception deny URLs and in S13/S14 write bodies; generator patched and the 5 affected files regenerated (`matrix/self/s5-self-write-denied.md`, `matrix/{pp,reporting-line}/s1{3,4}-*-write.md`). Regeneration is byte-identical elsewhere — verified against a full snapshot.
