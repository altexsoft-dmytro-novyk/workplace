---
topic: AccessControl facade, Phase 1 audience resolution, functional permission check, and base section access
updated: 2026-09-11T20:30
---

- (direction) User requested validation/update of _bmad-output/implementation-artifacts/access-control/spec-access-control-facade-audience-resolution.md; migrate it into the canonical bmad-spec folder without modifying the approved legacy source.
- (decision) Headless express update: source and linked architecture are rich enough to distill; unresolved product choices remain explicit open questions rather than invented answers.
- (decision) The legacy implementation spec is fully absorbed as an audit source; detailed I/O and edge behavior render in facade-contract.md, implementation rules render in implementation-constraints.md, and binding architecture documents remain adopted companions.
- (capability) CAP-1 Authorization facade: every context obtains functional-permission, audience, and base section decisions only through AccessControl; success means no external direct policy reads, role flags, or alternate authorization decisions.
- (capability) CAP-2 Phase 1 audience resolution: resolve Self, recursive direct Reporting, directly assigned PP, and Colleague live per viewer-target while withholding Project, Department, and PP HR-line grants behind approval gates; success covers bulk mapping, exclusivity, merge, and fail-closed cases.
- (capability) CAP-3 Functional permission decision: isAllowed evaluates persisted live FR policy, permission, and user attachments independently from audience data and due actors; success includes exactly one seeded bootstrap HR Admin attachment without inventing a default permission catalogue.
- (capability) CAP-4 Base section access: canAccessSection returns only none/read/write from live audiences and the seeded S1-S16 matrix; success means denied data stays absent and owning-context projections can only narrow the result.
- (capability) CAP-5 Departure-safe authorization: every feature and audience decision enforces the AD-20 request-time cutoff across actor, target, endpoint, recursion-node, and shared-link-authority positions; success never relies on worker completion.
- (constraint) AD-1 is a blocking sequence: independently human-approved stage-1 scenarios and stage-2 E2E committed red must precede production implementation; this spec authors or approves neither gate artifact.
- (constraint) Reporting and Project are separate passes; Phase 1 recursively follows only direct reports-to edges, resolves only the assigned PP endpoint, and grants nothing from Department, PP HR-line, or Project inputs until their contracts and AD-1 suites are approved.
- (constraint) Self is evaluated first and is exclusive of manager columns; other simultaneous audiences merge per section by write over read over none.
- (constraint) All decisions fail closed on missing, orphaned, broken, stale, or due-person data; derived decisions are neither persisted nor cached across requests.
- (constraint) Initial policy evaluation supports only equality through indexed joins; policy-level IN, target-set storage, and application-side membership are excluded, while internal SQL IN for requested IDs remains allowed.
- (constraint) A mutation requires both a live FR permission and write section access plus any narrower command rule; a read does not invoke FR permission merely to transform an audience denial.
- (constraint) Denials are leak-safe: missing or invalid token is 401, missing feature permission or write against readable data is 403, and a no-access cell or hidden field is 404; absent payload data means a missing key, never null.
- (constraint) Domain code imports no Prisma, HTTP, transport, SDK, or foreign-context internals; only domain services inject port tokens, infrastructure implements ports, and application exports the facade.
- (constraint) Bulk resolution must issue one indexed query plan per graph and fit within the platform 500-record, 2-second response budget; empty target input returns immediately with zero queries, and multi-read resolution uses one transaction.
- (decision) Non-goals: stage-1 scenario authoring, E2E translation, profile/list/export projection, role-catalog management UI/API, shared-link and full-profile overlays, timetracker adapter/sync behavior, Department traversal, PP HR-line recursion, mentorship audiences, and application code in the workspace root.
- (question) What target-set representation, target types, cardinality, mutation rules, and indexed plan would authorize policy-level IN?
- (question) What approved timetracker assignment and freshness contract enables Project-line grants, including 15-minute withdrawal, four-hour failed-sync cutoff, and partial/intermittent-sync behavior?
- (question) What approved Department model defines nested membership, management edges, HR boundary, and PP HR-line propagation?
- (question) Which matrix column does full-profile access map to, and how does that overlay interact with Self?
- (note) Wrapper-only content dropped during derivation: legacy title/type/date/status/review-loop metadata, change-log narration, frozen-block markup, and the historical note that the backend submodule was not checked out; none changes build or verification decisions.
- (event) Self-validate pass 1 (coherence): PASS — CAP-1 through CAP-5 each have WHAT-level intent and testable success; constraints bend design; non-goals are explicit; the success signal is demonstrable; capability IDs are unique; implementation detail is isolated in companions; four unsupported product choices remain open questions.
- (event) Self-validate pass 2 (preservation): PASS — walked the legacy Intent, Boundaries, 20-row I/O matrix, code map, three-method facade contract, Project and Department gates, sequencing, tasks, acceptance criteria, design notes, and verification; every load-bearing claim lands in SPEC.md, facade-contract.md, implementation-constraints.md, or an adopted architecture/deferred-work companion; wrapper-only drops are recorded separately.
- (decision) Story breakdown: stage-2 E2E dispatch lives here (stories 1–2), moved from test-cases spec; prerequisite is test-cases story 6 (all stage-1 prose AD-1 approved)
- (event) Story breakdown schema check: PASS — two E2E entries, ids 1–2, done_checkpoint on story 2 before facade implementation dispatch
- (decision) P-3 review 2026-08-29: OQ Department split — fixed v1.5: department management is Reporting-line; PP propagates within HR boundary; open: nested membership model, indexed traversal, event/state representation
- (decision) P-3 review 2026-08-29: OQ Project/timetracker split — fixed v1.5: assignment end withdraws within 15 minutes; failed sync withdraws after four hours; open: target-set representation, query seam, partial/intermittent-sync semantics
- (decision) P-3 alignment: never-shareable {S3,S7,S13,S14}; dismissed-target projection binds identity subset plus employmentStatus dismissed; due actor binds 403 for valid session; empty bulk binds zero-query {items:[]}
- (event) Self-validate pass 1 (coherence) after P-3: PASS — CAP-1..5 intact; open questions split fixed binding rules vs implementation contracts; companions updated
- (event) Self-validate pass 2 (preservation) after P-3: PASS — v1.5 revocation timing, department/project gates, never-share set, and AD-20 projections preserved in SPEC and facade-contract
- (decision) 2026-09-11: AD-1's per-stage human approval was retired 2026-09-04 (docs/architecture/testing-strategy.md:25-38, ruling D-1 / test-design-qa.md DG-01). implementation-constraints.md's Verification-obligations line updated to drop 'independently approved'; the Stage-1-then-committed-red-Stage-2-then-production ordering is unchanged, gated by PR review and CI rather than a per-stage reviewer. No SPEC-scope capability, constraint, or non-goal decision changes as a result.
