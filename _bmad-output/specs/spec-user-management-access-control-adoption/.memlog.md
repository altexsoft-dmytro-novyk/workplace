---
topic: User Management adoption of the headless Access Control facade
updated: 2026-09-01
---

- (direction) Separate UM-owned package that answers um-integration-contract-request.md and specifies the ACCESS_CONTROL_PORT rebind, per-route mapping, projection boundary, and real-consumer HTTP E2E parked as a non-goal by spec-access-control-kernel-mvp. Touches services/backend only at code stages; no Access Control file, prisma schema, migration, or seed.
- (decision) The seam is the ACCESS_CONTROL_PORT binding in user-management.module.ts, not a route; behaviour is decided per feature string inside a new real adapter in src/user-management/infrastructure/ that injects AccessControlFacade forwards across the AD-2 boundary and replaces interim-access-control.adapter.ts wholesale (AD-21, no dual-running).
- (decision) isAllowed(userId, feature) delegates straight to AccessControlFacade.isAllowed; the three no-target features in use (user-management:create/deactivate/list) are exactly the ACM-1 seeded keys, so the position-string check is retired with no behaviour regression.
- (decision) READ_USER_FEATURE adopts now: allow self/reporting/pp AND colleague — §3.2 S1 row is R for the Colleague column and every active authenticated viewer is at least a Colleague. All four get 200 with the same minimal S1 identity-card projection (id, firstName, lastName, photo, position, country, city, workEmail, workPhone, birthDay, birthMonth, companyJoinDate; drops ttId/isActive/customFields/createdAt/createdBy). The S1-card DTO replaces toUserResponse's whole-row spread and ships in Story 0.1 (CAP-3) — it is NOT the deferred Profile Projection story. Only denial is an empty audience set -> leak-free 404.
- (decision) 2026-09-01 human product decision: the "two-state colleague rule" and UMAC-3 (colleague deny->allow-narrowed flip) are REMOVED — there is nothing to flip. FR-17 Profile Projection shrinks to the S10 dates-only / S11 name-only / S16 per-field colleague views on their own surfaces, plus S7/S8 flags and S1 derived-field immutability; it is no longer coupled to GET /users/:id.
- (open) No user-management:edit or photo permission is seeded, so the §2.2 dual gate for PATCH / PUT photo cannot be completed today. Two options recorded, (a) recommended (add the permission via a new Access Control kernel seed AD-1 sequence, adoption consumes it), (b) adopt READ only with a recorded interim write rule. Final call deferred to the human.
- (decision) The interim SESSION resolver is out of scope and retired by UM Epic 2; this package keeps it and uses the Bearer <token:<uuid>> seeded-UUID convention it already supports for Stage-2 fixtures.
- (decision) CAP-3 scope pinned: the S1-card DTO is a dedicated mapper on the GET /users/:id handler only — NOT a rewrite of the shared toUserResponse. GET /users list items, POST /users, PATCH /users/:id, DELETE /users/:id, and PUT /users/:id/photo response bodies are unchanged by this slice (list projection is Epic 1 Story 1.5 / FR-15).
- (decision) 2026-09-01 (Dmytro Novyk): UMAC-1 Stage-1 scenario prose approved (approvals.yaml created — umac-01..06 + README + story spec, author=agent, approver=Dmytro Novyk, commit 5fe0bb8). GET /users/:id denials settled as 401 (unresolved session — session layer) + 403 (authenticated active viewer, empty audience); "leak-free 404" withdrawn — internal directory, id existence not sensitive. No guard/controller change. Propagated to SPEC, contract Q4, stories.yaml, epics.md, access-control.md.
- (decision) 2026-09-01 (Dmytro Novyk): fold the { data, canEdit } capability envelope into GET /users/:id NOW (option a). data = 12 S1 fields; canEdit = isAllowed(viewer, user-management:edit) && canAccessSection(viewer,'S1',target)==='write' — read-only hint, false for every viewer until user-management:edit is seeded, permanently false for colleague. Envelope is the section/detail-read convention; per-resource single canEdit bool (photo etc. get their own endpoint + bool). Rollout onto other routes + api-conventions + GET /me (global features) is the deferred item in access-control/deferred-work.md. umac-01..04 + README + story spec revised and re-approved same day (superseding entry in approvals.yaml, commit dffe20a).
- (issue) A Stage-2 E2E suite (services/backend/test/user-management/access-control-adoption/) was committed in 986e90a/0681939 with the scenario prose, before Stage-1 approval — AD-1 ordering violation. read-denial asserts 404 (now 403); write-adoption is UMAC-2 scope. UMAC-1-red-tests dispatch decides accept/re-derive/revert.
- (decision) 2026-09-01 (Dmytro Novyk, PO/Architect): SPEC + all Open Decisions approved. (i) = option (a) — Access Control adds user-management:edit via a new three-stage AD-1 kernel-seed sequence; Story 0.2 blocks on its stage-3-production. (ii) not a separate photo permission. (iii) photo Self-only. (iv) colleague S1 card from Story 0.1 (already resolved). (v) dedicated Epic 0, read path starts now, Story 1.2 authorization is Epic 0's. Integration-contract response accepted the same day. SPEC implementation_status -> stage-1-authorized. This authorizes Stage-1 scenario authoring only; every stage still needs its own independent human approval in this package's approvals.yaml.
- (status) 2026-09-01: UMAC-1 Stage 2 approved (approvals.yaml). SPEC implementation_status -> stage-3-authorized. Next dispatch is UMAC-1-production. Living corpus empty-audience wording corrected from leak-free `404` to `403` to match this package's SPEC / stories.yaml / umac-05.
