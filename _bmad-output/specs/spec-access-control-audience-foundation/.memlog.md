---
topic: Access Control audience foundation
updated: 2026-08-30T00:20
---

- (direction) User approved a two-day Access Control foundation slice and prohibited changes to User Management.
- (capability) CAP-1 resolves only Self, Reporting line, direct People Partner, or Colleague for a viewer and one target employee.
- (constraint) Resolution is live and fail-closed; broken or orphaned relationship data grants no audience.
- (constraint) Project, Department, PP HR-line, shared-link, full-profile, functional-permission, section-matrix, User Management, and frontend work are out of scope.
- (question) Which User Management-owned endpoint and response contract will consume the facade for the required HTTP E2E scenarios?
- (event) Coherence validation passed: two stable capabilities have intent and success; constraints bend design; non-goals and a concrete gate-aware success signal are present.
- (event) Preservation validation passed: every approved foundation-scope decision from the Sprint Change Proposal is represented; no HTTP response contract was invented.
- (decision) CAP-1 accepts one or more target employee IDs and returns a result for each requested target; empty input returns an empty result without database queries, matching ACF-1 and AD-10.
- (decision) 2026-08-30: user chose to wait for the User Management integration contract rather than approve an AD-3 evidence deviation. ACF-1 does not start; no facade-level substitute for HTTP E2E is authorized. The ask is `_bmad-output/implementation-artifacts/access-control/um-integration-contract-request.md`.
- (decision) 2026-08-30: implementing ACF-1 corrected two questions in the UM contract request (revision 2). The seam is the `ACCESS_CONTROL_PORT` binding, not `GET /users/:id`: one binding answers for READ, EDIT and UPLOAD_PHOTO. The feature-to-audience mapping therefore needs one answer per feature — an adapter ignoring its `feature` argument would grant reporting/pp/self a write path that §3.2 gates behind a dual check Phase 0 does not implement. Suggested: READ moves to the facade, EDIT and UPLOAD keep interim behaviour.
- (decision) 2026-08-30: `colleague -> deny` on the read is correct only while `toUserResponse` returns the whole row. Per §3.3.4 a colleague legitimately sees S1, S10 dates and the S11 project name, so the durable answer is allow-with-whitelist. Recorded as a two-state rule with profile projection as the trigger, so the temporary state is not mistaken for the final one.
- (note) 2026-08-30: adopting the facade on the write path will break `test/user-management/profile.e2e-spec.ts` — its persona tokens are literal placeholders, so `'Bob'` is a non-existent user that the interim `Boolean(userId)` check lets through. Under the real facade it resolves to colleague. Keeping EDIT/UPLOAD on interim avoids this until those fixtures use real seeded users.
- (event) 2026-08-30: ACF-1 trace gate recorded FAIL with base coverage PASS (8/8 P0, 20/20 tests) — `_bmad-output/test-artifacts/gate-decision.json`. Blockers: missing AD-1 approval, provisional mapping, dormant production wiring.
- (superseded) 2026-09-01 human product decision: the provisional `colleague -> deny (403)` half is answered the OTHER way — a colleague `GET /users/:id` returns the S1 identity card (200); only an empty audience denies (leak-free 404). The RESOLVER is unaffected (Colin/Frank/Hana still resolve to `colleague` fallback), but the HTTP `403` expected results in ACF-AU-05 / ACF-FC-01 / ACF-FC-02 are invalidated and must be reworked as resolver audience-set assertions (like ACF-FC-04) in a fresh AD-1 pass. README + SPEC Open-Questions updated; scenario files carry a superseded banner. The two-state-rule line above is history.
