---
topic: Access Control Kernel MVP without User Management adoption
updated: 2026-09-12T07:40
---

- (direction) Create a separate Kernel MVP package; preserve the existing broad draft and touch no application code, Prisma schema, migration, seed, or test.
- (decision) The approved 2026-08-30 Correct Course proposal, final Access Control architecture spine, approved FR-AMD-1, access-control architecture, and AD-1 testing strategy are adopted companions and are not reopened.
- (capability) CAP-1: ACM-3 returns exact fail-closed audience maps for inactive or due viewers, inactive or due targets, and inactive or due bridge/endpoints, while preserving independently valid active paths.
- (capability) CAP-2: ACM-4 retains all applicable active audiences, keeps Self exclusive, and uses Colleague only as the no-stronger-audience floor; FR permissions never enter the merge.
- (capability) CAP-3: ACM-1 provides the approved type-separated FR schema, reviewed migration, and atomic idempotent bootstrap seed for exactly three permissions, one hr-admin policy, three grants, and one exact ROOT_WORK_EMAIL attachment.
- (capability) CAP-4: ACM-2 depends on ACM-1 and exposes live type-separated isAllowed through AccessControlFacade, denying inactive/due/missing users, unknown or differently-cased keys, absent grants, and orphaned FR data without audience reads or decision caching.
- (capability) CAP-5: ACM-5 depends on ACM-4 and provides canAccessSection only for S1, S10, and S11, merging multiple audiences write over read over none and returning none for absent, empty, or unsupported inputs.
- (capability) CAP-6: ACM-8 depends on ACM-2 and ACM-5 and imports AccessControlModule into AppModule without changing User Management bindings, routes, projections, or files.
- (capability) CAP-7: ACM-9 measures the real migrated PostgreSQL resolver workload at 500 targets before and after composition, records latency/query-plan evidence, and changes no behavior.
- (constraint) AD-1 is ordered and blocking for every code slice: scenario prose in one dispatch, explicit human approval, red direct-facade PostgreSQL integration tests in another dispatch, explicit human approval, then production implementation in a third dispatch.
- (constraint) Every dispatch entry has spec_checkpoint and done_checkpoint enabled so automation stops after each stage and cannot cross two stages without human approval.
- (constraint) Kernel Stage-2 evidence uses AccessControlFacade through real AccessControlModule, Prisma adapters, migrated PostgreSQL, and no Access Control repository fake, User Management override, or artificial HTTP endpoint.
- (constraint) ACM-3 and ACM-4 stop at audience resolution; ACM-5 alone owns canAccessSection; ACM-2 FR evaluation remains type-separated and never widens audiences or section access.
- (constraint) ACM-9 is measurement-only, with baseline after ACF-1 and final rerun after ACM-8; any optimization requires a separate gated story.
- (decision) User Management adoption is deferred work outside this SPEC: later UM-owned port rebinding, projection, approved route mapping, and real-consumer HTTP E2E remain required before the product gate can close.
- (decision) Story execution order is ACM-3, ACM-4, ACM-1, ACM-2, ACM-5, ACM-8, then ACM-9; formal dependencies are ACM-2 on ACM-1, ACM-5 on ACM-4, and ACM-8 on ACM-2 plus ACM-5.
- (event) Self-validation coherence PASS: all seven capabilities have intent and testable success, constraints bend delivery, non-goals and concrete success signal are present, IDs are stable, and prose is lean.
- (event) Self-validation preservation PASS: all load-bearing Kernel MVP scope, FR architecture, AD-1 gates, dependencies, measurement rules, and deferred User Management adoption are preserved in SPEC.md, adopted companions, or stories.yaml; no approved decision was reopened.
- (event) Story schema validation PASS: 20 top-level dispatch entries parse, IDs are quoted unique valid and prefix-free, required fields are present, no unsupported fields exist, and every entry enables both human checkpoints.
- (event) Specification package rendered for human review; no scenario, test, migration, seed, production code, User Management, or frontend dispatch is authorized.
- (event) Repair opened after failed validation; prior PASS verdicts are superseded and the package remains in review with implementation not authorized.
- (decision) AD-20 due/departure behavior is deferred from this Kernel MVP because the backend has no Departure model or persistence seam; this does not redefine due targets as empty audiences, and the canonical read-only dismissed-target projection remains binding future work.
- (capability) CAP-1 supersession: ACM-3 covers inactive and missing viewers, targets, and bridge/endpoints only; inactive viewer denial precedes Self, each distinct requested target gets an entry, duplicates collapse to one map key, and independently valid active paths remain.
- (constraint) ROOT_WORK_EMAIL follows DEC-UM-007: trim outer whitespace and lowercase before validation and exact lookup against normalized workEmail; the User bootstrap must create an active normalized user first, while ACM-1 neither creates nor activates users and fails atomically if exactly one eligible user is not found.
- (constraint) Fresh-database ACM-1 order is preflight normalized root eligibility, begin one transaction, ensure the three canonical Permissions, ensure the hr-admin FR Policy, ensure the three canonical PolicyPermissions links, ensure the root UserPolicies link, verify invariants, then commit; any failure rolls back all ACM-1 writes.
- (decision) Seed-owned identity is natural and closed: the three canonical permission keys, the FR Policy with targetRole hr-admin, the three pairs joining that policy to those keys, and the root-user/hr-admin attachment; extra administrator-approved permissions, grants, roles, or attachments are non-bootstrap state and survive reruns, including a fourth permission granted to hr-admin.
- (constraint) Concurrent identical ACM-1 seed executions must serialize through database constraints and transactional coordination, both converge successfully to one bootstrap set, and expose no partial state; a conflicting execution fails atomically with actionable diagnostics.
- (constraint) PolicyPermissions may reference only Policies rows with type FR; the reviewed migration must reject an attempted AR-policy permission link, in addition to restrictive foreign keys and pair uniqueness.
- (constraint) The canonical hr-admin FR Policy uses operator == and managedBy admin; sync provenance is reserved to the timetracker integration and no other policy operator is supported in this MVP.
- (capability) CAP-4 supersession: ACM-2 evaluates live active-user FR joins and denies unknown keys, absent grants, wrong-type policies, and nonmatching joins; physically orphaned PolicyPermissions states are excluded as unreachable under restrictive foreign keys, and due-user evaluation remains deferred with AD-20.
- (constraint) Database timeout, connection, transaction, and query failures reject the facade operation as infrastructure errors and produce no authorization result; only validly completed reads may return false, none, or empty audiences for authorization-data denial.
- (capability) CAP-5 exact contract is canAccessSection(viewerId: string, section: string, targetEmployeeId: string): Promise<SectionAccess>, where SectionAccess is none | read | write; unsupported section strings return none after a successful read and infrastructure failures reject.
- (capability) CAP-6 supersession: ACM-8 depends on completed ACM-2, ACM-3, and ACM-5 as required by the approved sprint proposal.
- (capability) CAP-7 passes only when every required 500-target measured facade run, including warm p95 and worst case, completes within 2 seconds without statement timeout; an exceedance or timeout is FAIL and opens separate gated remediation, never a measurement-only pass.
- (constraint) ACM-9 investigation is bounded to the existing representative set: balanced depth 5 and acyclic depths 25, 50, 100, 200, 300, 400, and 499 with 500 targets; stop at the first failure or after depth 499, recording no threshold breach when all pass.
- (decision) ACM-4 is validation-only because its required merge behavior and regression tests already exist; its preserved story IDs verify scenario coverage, real evidence, and no production change rather than inventing retroactive red history.
- (decision) Story execution order preserves all 20 IDs and runs ACM-9-baseline before ACM-8: baseline; ACM-3 stages; ACM-4 validation entries; ACM-1 stages; ACM-2 stages; ACM-5 stages; ACM-8 stages; ACM-9-final.
- (constraint) Adopted companions required downstream are the exact sprint proposal, Access Control architecture spine, FR amendment, good-spine review, access-control architecture, database schema, testing strategy, and user-management test decisions paths; stories.yaml is not a companion.
- (decision) The approved sprint proposal and FR amendment remain approved planning decisions but do not approve implementation; SPEC status remains in-review and implementation_status not-authorized until independent review advances it.
- (decision) Due-target reconciliation is explicit: the earlier CAP-1 rule that due targets return empty audiences is retired, not live; this MVP has no due-target result, while AD-20 retains the future current-manager/PP read-only dismissed-target projection.
- (constraint) Dependency supersession is explicit: the earlier ACM-8 dependency on ACM-2 plus ACM-5 is retired; the live dependency is ACM-2 plus ACM-3 plus ACM-5.
- (event) Self-validation coherence PASS: all seven stable capabilities have WHAT-level intent and pass/fail success, constraints bend delivery, non-goals are explicit, statuses remain in-review/not-authorized, and no unresolved product question remains in this MVP boundary.
- (event) Self-validation preservation PASS: all load-bearing claims from the adopted companions and 15 repair findings land in SPEC.md or stories.yaml; wrapper-only validation instructions are recorded here rather than copied into the kernel.
- (event) Story schema and semantic validation PASS: 20 preserved IDs parse as quoted unique prefix-free strings, required fields and checkpoints are present, ACM-9 baseline precedes ACM-8, ACM-8 follows ACM-2/ACM-3/ACM-5, and ACM-9 final follows ACM-8.
- (event) Mechanical validation PASS: CAP-1 through CAP-7 are stable and unique, every capability has intent and success, all eight adopted companion paths exist, YAML validation passes, and scoped git diff checks report no whitespace error.
- (event) Repair validation complete; package is ready for independent review only, implementation remains not authorized, and no planning approval is claimed.
- (event) Validation correction: no-index git diff check passes for the three spec files and sprint proposal but reports pre-existing Markdown hard-break trailing spaces at FR amendment lines 189-191; they are outside status-alignment scope and were preserved, so the earlier blanket no-whitespace statement is superseded.
- (event) Evidence rerun PASS: one reproducible Ruby validation parsed SPEC and stories, checked 20 story IDs/order/schema, seven capability IDs and intent/success, eight existing companions, and emitted PASS for findings 1 through 15; no contract change was required.
- (decision) Accepted product decision: AD-20 due/departure enforcement is deferred from ACM-1/ACM-2; User.isActive remains required and the documented dismissed-target projection remains future work.
- (constraint) ACM-1 requires a narrow deploy-time prerequisite that normalizes ROOT_WORK_EMAIL by DEC-UM-007 and ensures exactly one active User; this authorizes no User Management API, CRUD, runtime role management, or other User Management feature.
- (constraint) Inside one ACM-1 transaction, lock the candidate root User and any seed-owned hr-admin attachment, revalidate normalized exact-one active eligibility and attachment identity before writes and before commit, and roll back every conflict with actionable diagnostics.
- (decision) If normalized ROOT_WORK_EMAIL changes after bootstrap, ACM-1 treats it as conflicting bootstrap drift and atomically fails; it never transfers the attachment or creates an additional root attachment.
- (constraint) PolicyPermissions is database-enforced FR-only by a policyType discriminator fixed to FR and a restrictive composite foreign key from (policyId, policyType) to unique Policies(id, type), with reviewed custom PostgreSQL migration SQL.
- (decision) A repeated reporting-chain node denies Reporting only for that affected target; Self and direct PP remain independently evaluated and Colleague follows the normal fallback rule.
- (decision) CAP-7 keeps the 500-target absolute performance gate and representative Reporting, direct PP, Colleague, and mixed graph coverage; the exact depth sequence is operational measurement protocol in testing-strategy.md, not normative product behavior.
- (decision) ACM-9 fails and stops at the first warm-p95 or worst-case value above two seconds or statement timeout; a post-composition slowdown within both absolute limits is recorded but does not fail the MVP, and baseline/final artifacts remain separate.
- (decision) ACM-4 is not already proven: preserved IDs now require scenario coverage review and real PostgreSQL facade validation over Reporting, direct PP, Colleague, and mixed fixtures; any found gap starts its own AD-1 sequence.
- (constraint) ACM-8 production dispatch must correct the stale AccessControlModule composition comment while importing the module, without rebinding User Management or changing application behavior beyond approved composition.
- (constraint) Every Stage-1 scenario dispatch cites binding sources explicitly: Kernel SPEC capability, AD-1, applicable AD-4/FR-AMD-1 or AD-3, and the relevant canonical architecture/testing contract.
- (decision) All existing CAP-1 through CAP-7 and 20 story IDs remain stable; status remains in-review, implementation_status remains not-authorized, and this repair grants no planning or implementation approval.
- (decision) The historical good-spine reviewer artifact is process metadata, not a load-bearing adopted companion; remove it from SPEC companions while preserving the file unchanged as review history.
- (constraint) ACM-1 uses an AccessControlBootstrap root-hr-admin singleton holding normalizedRootEmail, rootUserId, and policyId plus a common transaction advisory lock acquired before state inspection; this covers absent-row concurrency and distinguishes later administrator attachments.
- (constraint) Policies.type is NOT NULL and restricted to FR or AR.
- (decision) Reporting repetition is path-local per requested target, shared ancestors across targets are valid, and the walk ends successfully when it reaches the viewer; only repeats before viewer proof deny Reporting for that target.
- (constraint) ACM9-MVP-v1 fixes five warm-ups, twenty measured facade calls, nearest-rank percentiles, independent fixture/depth gates, EXPLAIN outside latency samples, and append-only PASS/FAIL/INCOMPLETE run artifacts with comparable baseline references.
- (decision) AD-20 deferral applies consistently to the entire Kernel MVP, explicitly ACM-1 through ACM-5; ACM-8 only composes and ACM-9 only measures, while future due behavior retains the AD-1 gate.
- (constraint) ACM9-MVP-v1 final evidence must match the baseline environment-manifest hash covering PostgreSQL configuration/version, runtime, CPU/memory limits, topology, and isolation/load policy; mismatch is INCOMPLETE with no gate or slowdown comparison.
- (event) Self-validation coherence PASS: CAP-1 through CAP-7 retain stable WHAT-level intent and testable success, statuses remain in-review/not-authorized, and accepted product decisions plus reviewer repairs are consistent.
- (event) Self-validation preservation PASS: accepted AD-20, root drift/locking/provenance, repeated-node, CAP-7 protocol boundary, absolute-threshold, User prerequisite, FR-only database, ACM-4 evidence, ACM-9 fixtures/artifacts, ACM-8 comment, and Stage-1 citation decisions land in SPEC.md, stories.yaml, or adopted companions.
- (event) Story schema validation PASS: all 20 existing IDs and order are preserved, IDs are quoted unique and prefix-free, fields/checkpoints are valid, and all six Stage-1 dispatches carry binding-source citation instructions.
- (event) Repair package re-derived for independent review only; no planning approval is claimed and implementation remains not authorized.
- (event) Repair round 2 opened after an independent NEEDS REVISION verdict naming three execution gaps and seventeen findings; prior PASS verdicts are superseded and the package stays in-review with implementation not authorized.
- (decision) Approved repair scope this round is the seven required repairs plus reviewer findings 8, 9, 10, and 12; the editorial condensation pass is declined to avoid churning already-approved wording.
- (decision) This repair may edit the Kernel spec package and the adopted architecture/testing companions; it changes no application code, Prisma schema, migration, seed file, or test.
- (note) Docs check before adding the bootstrap prerequisite: UM epics FR-1/AD-12 and Story 1.1 own seed/import creating the first User and its HR Admin bootstrap, but um-seed-01/02/03 are unauthored; spine AD-4, access-control.md, database-schema.md, and FR-AMD-1 all place the Kernel prerequisite explicitly outside ACM-1; the live seed neither normalizes ROOT_WORK_EMAIL nor checks exact-one active eligibility.
- (capability) CAP-8: the deploy-time root User bootstrap prerequisite guarantees, before ACM-1 runs, exactly one active User whose DEC-UM-007-normalized workEmail equals the normalized ROOT_WORK_EMAIL; absent, blank, unmatched, ambiguous, or inactive identity fails the deploy step with actionable diagnostics.
- (decision) CAP-8 boundary against User Management: CAP-8 owns only the root User row's existence, active state, and normalized identity; FR role creation and attachment stay in CAP-3/ACM-1, and UM Story 1.1 population import plus um-seed-01/02/03 remain deferred UM work that CAP-8 neither implements, duplicates, nor blocks. CAP-8 authorizes no User Management API, CRUD, runtime role management, HTTP surface, or other User Management feature.
- (decision) CAP-8 receives its own AD-1 trio ACM-0-scenarios, ACM-0-red-tests, and ACM-0-production placed immediately before ACM-1-scenarios; the package becomes 23 stories and every previously existing story ID is unchanged.
- (constraint) Exactly one active User means exactly one active User whose normalized workEmail equals the normalized ROOT_WORK_EMAIL; unrelated active employees never affect that count, and zero or multiple normalized matches fail atomically.
- (decision) Reporting cycle supersession approved by the product owner: reaching the viewer proves the viewer sits on the chain but no longer grants Reporting by itself. The walk continues past viewer proof to chain termination, and Reporting is granted only when the whole walked chain for that target terminates without repeating a node; a repeated node anywhere in that chain denies Reporting for that target only, while Self and direct PP remain independently evaluated and Colleague follows the ordinary no-stronger-audience fallback.
- (constraint) Chain termination for the acyclicity proof is the absence of a further usable manager edge, including a missing, soft-deleted, or inactive bridge already treated as no edge under fail-closed resolution; termination at such a node is a clean end, not a repeat.
- (capability) CAP-1 addition: resolveAudiences(viewerId, []) returns an empty map immediately with zero relationship-graph port calls, preserving the inherited Phase-0 empty-bulk short-circuit; ACM-3 carries this empty-target-list case as an explicit direct-facade regression scenario.
- (constraint) ACM-9-baseline conditional gate: only a PASS baseline artifact satisfies the baseline dependency. A FAIL or INCOMPLETE baseline halts dispatch of ACM-8 and ACM-9-final, is not superseded by a rerun that omits the failing shape, and opens separately AD-1-gated remediation before any dependent story resumes.
- (constraint) ACM-4 gap gate: a concrete unmet behavior found by ACM-4-scenarios or ACM-4-red-tests halts ACM-4-production and ACM-5, starts a separately approved AD-1 sequence for the gap, and requires a re-run of Story Breakdown before the package resumes; ACM-4-production may record a no-change disposition only when no gap was found.
- (constraint) ACM-4 evidence authorization: ACM-4 may add and commit reproducible PostgreSQL integration test files under the approved kernel Stage-2 boundary. Throwaway or unreproducible evidence is not acceptable, and ACM-4 still changes no production code, adds no section or FR behavior, and rewrites no ACF-1 history.
- (constraint) testing-strategy.md carries an explicit Kernel MVP exception on the global AD-19/AD-20 stage-1 scenario list, pointing at the scoped AD-20 amendment, so Stage-1 agents do not reintroduce the absent Departure persistence dependency into ACM-1 through ACM-5.
- (decision) FR-AMD-1 status is one unambiguous gate statement: its architecture decisions are approved and final under approval_scope architecture-decision-only, the separate gate still open is the Kernel MVP spec package review, and the amendment authorizes no Stage-1, test, migration, seed, production-code, or later dispatch.
- (constraint) Conflicting seed-owned drift is defined per canonical row and field rather than as one blanket rule: missing owned rows are restored; identity and authorization-bearing fields that differ fail the transaction before writes; descriptive fields and generated ids are preserved; non-bootstrap rows are always preserved. The field-by-field table lives in FR-AMD-1 and the Kernel SPEC cites it.
- (constraint) ACM-9 run start is an atomic artifact reservation written before the first warm-up call, carrying run id, role, protocol version, and manifest hashes at status INCOMPLETE; finalization rewrites only status and results, a finalization failure leaves the reserved INCOMPLETE artifact and exits nonzero, and no prior run is ever overwritten.
- (constraint) ACM9-MVP-v1 fixture and environment manifests use the versioned ACM9-MANIFEST-v1 canonical form: sorted keys, UTF-8 without byte-order mark, no insignificant whitespace, fixed numeric formatting, and explicit nulls, hashed with SHA-256 and recorded as lowercase hex, so equivalent runs cannot become incomparable through serialization differences alone.
- (constraint) ACM-0 Stage-2 boundary: its subject is the deploy-time root User step, so its evidence runs against migrated PostgreSQL without a facade call; repository fakes, User Management provider overrides, and artificial HTTP endpoints stay prohibited and AD-1 is unchanged.
- (constraint) The AD-20 deferral window is restated as ACM-0 through ACM-5 in SPEC.md, the spine, access-control.md, and FR-AMD-1 now that ACM-0 opens the Kernel MVP.
- (event) Self-validation coherence PASS: CAP-1 through CAP-8 each carry WHAT-level intent and testable success, the new constraints all rule something out, non-goals now name User Management Story 1.1's population import, statuses remain in-review/not-authorized, and CAP-1 through CAP-7 kept their IDs while CAP-8 took the next unused one.
- (event) Self-validation preservation PASS: all twelve unique reviewer findings land in SPEC.md, stories.yaml, or an adopted companion — bootstrap stories, fail-closed cycle rule, PASS-only baseline gate, ACM-4 gap gate and evidence authorization, empty-bulk scenario, AD-20 testing-strategy exception, single FR-AMD-1 gate statement, per-field drift table, artifact reservation, normalized exact-one definition, and ACM9-MANIFEST-v1 canonicalization; the six edge-case-hunter findings duplicate adversarial ones already covered.
- (event) Story schema validation PASS: 23 entries parse, all 20 prior IDs are unchanged, the three ACM-0 IDs are quoted, unique, and prefix-free, required fields and both checkpoints are present, no unsupported or status field exists, ACM-0 precedes ACM-1, ACM-9 baseline precedes ACM-8, ACM-8 follows ACM-2/ACM-3/ACM-5, and ACM-9 final follows ACM-8.
- (event) Mechanical validation PASS: one reproducible script ran 200 checks over SPEC.md, stories.yaml, the spine, FR-AMD-1, access-control.md, and testing-strategy.md, confirming seven existing companions, eight capability IDs with intent and success, and a landing site for every repair item; git diff --check reports no whitespace error.
- (event) Repair round 2 complete; the package is ready for independent re-review only. Implementation remains not authorized, no planning approval is claimed, and no application code, Prisma schema, migration, seed, or test file was changed.
- (event) Repair round 3 (P2) opened after an independent review confirmed eighteen findings; all prior PASS verdicts are superseded, the package stays in-review, and implementation remains not authorized.
- (decision) Approved repair scope this round is all eighteen confirmed findings, with four user decisions taken at approval: ACM-0 creates and validates the normalized root User; the named entrypoints and deployment order are accepted; normalized workEmail identity uses canonical storage with the database gap named and deferred; and the ACM-4 contradiction is resolved by an explicit validation-only exception rather than by reclassifying the story.
- (constraint) Repository reality grounding this repair, verified read-only: prisma/schema.prisma User has no soft-delete column; relationships_shape_check plus an ON DELETE RESTRICT endpoint foreign key make a bridge edge with a missing endpoint row structurally impossible; users_workEmail_key is a plain unique index on the raw stored workEmail, and only the create/update DTOs normalize, so the seed stores ROOT_WORK_EMAIL verbatim.
- (decision) CAP-1 supersession: the earlier statement that a missing or soft-deleted bridge is among the chain-termination cases is RETIRED. Soft-deleted User and bridge behavior is removed from the MVP entirely because no persistence representation exists, and no field is invented to support it.
- (constraint) CAP-1 chain-termination taxonomy: an absent manager edge is a clean end; an edge whose endpoint is inactive is unusable and treated as absent for traversal, denying Reporting before viewer proof and granting it after viewer proof since the chain has terminated without a repeat; an edge whose endpoint row is missing is unreachable in supported operation and is a defensive rule rather than a constructible Stage-2 scenario.
- (constraint) CAP-1 identity ordering: viewer identity validation precedes all audience derivation including Self, and Self is exclusive only after both viewer and target are confirmed present and active. An unconfirmed viewer or target yields an empty audience Set, never Self and never the Colleague floor.
- (capability) CAP-8 supersession: ACM-0 CREATES and validates the DEC-UM-007-normalized root User rather than validating an assumed one. A validation-only CAP-8 could never pass on a fresh migrated database with zero users, so the package no longer contains an unnamed external prerequisite. CAP-8 still owns only the single root User row's existence, active state, and normalized identity.
- (constraint) Named production entrypoints: ACM-0 is services/backend/prisma/seed.ts invoked by npm run db:seed; ACM-1 is src/access-control/infrastructure/bootstrap/access-control-bootstrap.ts wrapped by scripts/bootstrap-access-control.ts and invoked by npm run db:bootstrap:access-control. Deployment order is db:deploy, db:seed, db:bootstrap:access-control, start:prod. Stage-2 evidence for both invokes those exact entrypoints; re-implementing their logic inline in a test does not satisfy the story.
- (constraint) CAP-8 normalized identity: storage is canonical because ACM-0 normalizes on write, so users_workEmail_key blocks a duplicate normalized row. Exact-one eligibility counts ALL normalized matches first and checks active state only afterwards, so a count other than one fails as unmatched or ambiguous before isActive is consulted. A normalized match that is not the intended root is never adopted, mutated, or reactivated; concurrent runs converge through the unique violation, a re-read, and re-validation, leaving no partial state.
- (question) Named residual risk carried in the SPEC and deferred-work: normalized workEmail uniqueness is writer-side, not database-enforced, because users_workEmail_key indexes the raw value. Pre-existing non-normalized rows can yield more than one normalized match; ACM-0 fails closed and the unique functional index is separately gated work outside CAP-8's boundary.
- (decision) Dependency supersession: the live chain is approved FR architecture, ACM-0, ACM-1, ACM-2. ACM-0 is a real predecessor of ACM-1 in SPEC constraints, the sprint proposal, epics.md, and sprint-status.yaml.
- (constraint) Conditional dependencies are executable against persisted artifact state rather than free-text ordering: ACM-5 requires acm-4-disposition.yaml with disposition no-gap, and ACM-8 plus ACM-9-final require the ACM-9 baseline artifact with status PASS. stories.yaml permits no custom fields, so each precondition is expressed inside invoke_dev_with as an exact path, field, and required value triple that the persisted validator checks.
- (constraint) AD-1 stage approvals are persisted in the append-only ledger approvals.yaml beside SPEC.md, recording story id, stage, artifact path, commit, author, approver, decision, and timestamp. Author and approver must differ, and a dispatch may not start until the prior stage's record exists and verifies: the commit resolves and the named artifact is present at that revision. A prose assertion that a stage was approved is not an approval.
- (constraint) CAP-3 absent-singleton provenance: with no AccessControlBootstrap row, ACM-1 adopts an existing FR hr-admin policy by natural key after verifying operator, managedBy, and null targets, and adopts an existing hr-admin attachment only when it already belongs to the located root, then writes the singleton. Attachments belonging to anyone else are neither adopted nor transferred and remain non-bootstrap administrator state. A changed configured root with no singleton has no recorded provenance and is adopted; with the singleton present a changed root is conflicting drift. The asymmetry is deliberate.
- (constraint) CAP-3 cross-type collision: FR role-key uniqueness is the PARTIAL index UNIQUE targetRole WHERE type='FR', so an AR policy carrying targetRole='hr-admin' is legal and is a different object. ACM-1 lookup and ACM-2 evaluation always filter type='FR'; the AR row is never adopted, mutated, counted, or reported as drift, is preserved, can hold no grant, and any UserPolicies row attaching to it is never bootstrap state.
- (constraint) CAP-3 invariant coverage: ACM-1 Stage-1 and Stage-2 each cover all thirteen database invariants enumerated in database-schema.md — policy type, row shape, partial FR unique key, the Policies(id,type) support key, Permissions.key uniqueness and immutability, PolicyPermissions pair uniqueness, the policyType discriminator, the restrictive composite foreign key rejecting an AR grant, the restrictive Permissions foreign key, the permission-first index asserted via pg_indexes, UserPolicies integrity, the AccessControlBootstrap constraints, and ON DELETE RESTRICT delete behavior.
- (constraint) CAP-7 reservation supersession: reservation is two-phase and precedes EVERY fallible step, not merely the first warm-up call. The artifact is created with run id, role, protocol version, and status INCOMPLETE before fixture construction, database connection, or manifest hashing, and manifest hashes are written as computed, so a failure while probing or hashing still leaves a reserved auditable artifact with its stop reason.
- (constraint) CAP-7 infrastructure errors: connection failure, query error, and fixture-setup failure finalize the run INCOMPLETE with an error class and message. Such a run is never PASS and never FAIL, because FAIL is reserved for a measured threshold breach or statement timeout.
- (decision) CAP-7 precedence supersession: the earlier rule that an environment-manifest mismatch produces no absolute gate verdict is RETIRED. A recorded warm-p95 or worst-case value above two seconds, or a statement timeout, always finalizes the run FAIL whether or not manifests match, because the gate is absolute rather than comparative. A mismatch sets comparability mismatched and suppresses the slowdown comparison; a mismatched run with no breach finalizes INCOMPLETE. A mismatch can never upgrade a run to PASS and never erases a recorded breach.
- (constraint) CAP-2 halt supersession: the ACM-4 halt trigger widens from a concrete unmet behavior to ANY missing approved scenario coverage or concrete behavior gap, and it halts Stage 2 onward — ACM-4-red-tests, ACM-4-production, and ACM-5 — rather than production alone, opening a separately approved AD-1 sequence and requiring a Story Breakdown re-run.
- (decision) ACM-4's committed-red contradiction is resolved by the named validation-only evidence exception in testing-strategy.md rather than by reclassifying the story: characterization tests over already-shipped behavior that change no production code may be committed green, are never a Stage-2 gate for a production change, and any found gap re-enters the ordinary three-stage sequence with a real committed-red test. All three ACM-4 story IDs are preserved; only titles and descriptions change so red-tests stops misdescribing validation work.
- (decision) Mechanical validation correction: the earlier claim that one reproducible script ran 200 checks is superseded because no validator was persisted. The runnable validator now lives at validate.py beside SPEC.md and is run as: uv run _bmad-output/specs/spec-access-control-kernel-mvp/validate.py. It reports the count it actually runs, and no check count may be claimed without it.
- (decision) Preservation held: all eight CAP IDs and all 23 story IDs are unchanged by this repair, no repair item required a new capability or story ID, both memlogs are append-only, and status remains in-review with implementation_status not-authorized.
- (event) Self-validation coherence PASS: CAP-1 through CAP-8 keep their IDs and each carries WHAT-level intent and testable success; the new constraints each rule something out; non-goals and a concrete success signal remain; statuses remain in-review and not-authorized; and no approved decision was reopened.
- (event) Self-validation preservation PASS: all eighteen confirmed findings have a landing site — cycle semantics in SPEC/spine/access-control.md/proposal/epics, identity-before-Self, bridge taxonomy, soft-delete removal, ACM-0 create-and-validate, named entrypoints and deployment order, canonical workEmail storage with the named gap, the ACM-0 dependency edge, the single FR gate statement, the approvals ledger, executable preconditions, absent-singleton provenance, cross-type collision, the thirteen-invariant checklist, ACM-9 reservation/infra/precedence, the ACM-4 halt widening plus validation-only exception, and the persisted validator.
- (event) Story schema validation PASS: 23 entries parse, all 23 prior IDs are unchanged and in the same order, ids are quoted unique and prefix-free, only supported fields appear, no status field exists, both checkpoints are set on every entry, ACM-0 precedes ACM-1, ACM-9 baseline precedes ACM-8, ACM-8 follows ACM-2/ACM-3/ACM-5, and ACM-9-final follows ACM-8.
- (event) Mechanical validation PASS with a persisted validator: uv run _bmad-output/specs/spec-access-control-kernel-mvp/validate.py ran 839 checks with 0 failures. This supersedes the earlier unverifiable 200-check claim, which had no script behind it.
- (event) P2 repair complete and ready for independent re-review only. No application code, Prisma schema, migration, seed file, or test was changed; approvals.yaml is empty by design; implementation remains not authorized and no BMAD gate was self-approved.
- (event) bmad-review pass over the repaired package: adversarial and edge-case lenses run sequentially (verification-gap skipped as code-only). Nine findings were consequences of this repair and were fixed in place: ACM-9-final still said reserve-before-first-warm-up; CAP-8's no-CRUD wording collided with ACM-0 now creating a row; a PASS baseline under an incomparable protocol version satisfied the ACM-8/ACM-9-final gate; the blank-ROOT_WORK_EMAIL skip-to-fail behavior change was unstated; the seed's own header comment goes stale once ACM-1 creates Policies/UserPolicies; the after-viewer-proof Reporting grant is a widening versus today's adapter and was not flagged as a change; ACM-4's disposition file had no reservation so a crash blocked ACM-5 with no diagnostic; epics.md was out of sync on the CRUD boundary; and the validator's substring limitation was undocumented. Five findings remain OPEN for the human reviewer and were not silently resolved.
- (event) The five open bmad-review findings are recorded in planning-artifacts/architecture/architecture-access-control-foundation-2026-08-29/reviews/review-p2-repair.md alongside the nine fixed ones, so the earlier reference to five open findings is no longer a dangling claim: OPEN-1 Constraints outweighs Capabilities, OPEN-2 AccessControlBootstrap pins the root User permanently, OPEN-3 root row versus UM Story 1.1 import collision, OPEN-4 skipping the bootstrap entrypoint fails closed but silently, OPEN-5 approvals.yaml cannot identify which repository a commit belongs to. None is resolved by this pass.
- (decision) User directed on 2026-08-31 that OPEN-3 and OPEN-5 be closed as defects rather than carried as open questions, and that OPEN-1, OPEN-2, and OPEN-4 remain open for the independent reviewer. OPEN-3: the claim that CAP-8 neither implements nor blocks UM Story 1.1's population import was factually wrong in five live artifacts and is replaced by the DEC-UM-009-grounded statement that the import is not blocked but must reuse the root User id rather than insert a second row for an existing normalized email. OPEN-5: approvals.yaml gains a required repo field of workspace or services/backend with artifact_path relative to that repository root, because the workspace spans two git repositories and a bare commit could not be resolved.
- (event) Package gate APPROVED by the user on 2026-08-31. status becomes approved and kernel_spec_status becomes approved across SPEC, spine, FR-AMD-1, and both sprint proposals. Per FR-AMD-1's single gate statement this authorizes Stage-1 dispatch only, so implementation_status becomes stage-1-authorized rather than authorized: Stage-2 and Stage-3 still require their own per-stage human approvals. approvals.yaml is deliberately left empty because the Stage-1, Stage-2, and Stage-3 artifacts do not exist yet, so there is nothing an approver could have read and no commit any record could resolve against; pre-filling it would fabricate audit history and fail the ledger's own verification rule.
- (decision by user) 2026-08-31 CAP-2 audit disposition: coverage gap found. Approved ACM-4 scenario coverage lacks multi-audience retention, confirmed-active Self exclusivity, Colleague floor against stronger audiences, Reporting plus direct-PP deduplication, FR-permission separation, and one representative mixed PostgreSQL fixture. ACM-4-red-tests, ACM-4-production, ACM-5, and downstream work remain halted; no scenarios, tests, production code, or audit artifact were written by the audit.
- (decision by user) Story Breakdown re-run authorized for the CAP-2 coverage-gap recovery. Preserve every existing ACM story ID; add ACM-4R-scenarios, ACM-4R-tests, ACM-4R-production, and ACM-4R-disposition. The repair starts with six approved scenario contracts, then real migrated-PostgreSQL facade evidence; green characterization is allowed only when it proves already-shipped behavior and cannot authorize production change, while any behavior gap requires a committed-red test and separately approved Stage 3. ACM-5 remains blocked until the existing disposition artifact records no-gap after the repair.
- (event) CAP-2 coverage-gap recovery re-derived: the audit is a SPEC companion; the original ACM-4 Stage-2 and Stage-3 dispatches are explicitly halted; Story Breakdown adds four ACM-4R entries while preserving all 23 earlier IDs and their relative order. validate.py now checks the audit, recovery dependencies, and 27-story schema; it ran 1082 checks with 0 failures. No scenario, test, production, Prisma, migration, seed, or approval-ledger entry was created by this recovery-planning run.
- (event) ACM-4R Stage-1 scenario prose authored at workspace commit eed4351: ACM4R-MA-01 through ACM4R-MA-06 cover multi-audience retention, confirmed-active Self exclusivity, Colleague floor, duplicate-target deduplication, FR separation, and a representative mixed PostgreSQL fixture. The scenarios are pending independent human Stage-1 approval; no Stage-2 test, production code, Prisma schema/migration, seed, or approvals.yaml entry was created.
- (event by user) Independent Stage-1 approval recorded for all six ACM-4R-scenarios artifacts at workspace commit eed43516394775e6245c963e64c182742488ddc8. The next ACM-4R Stage-2 evidence remains blocked on completed ACM-1-production as well as this approval; no test or production dispatch is authorized by the Stage-1 decision alone.
- (event) ACM-3 Stage-1 completion authored: ACM3-II-04 through ACM3-II-14 close every CAP-1 gap the suite README previously listed as unwritten — empty-bulk short-circuit, duplicate-target collapse, both constructible cycle positions relative to viewer proof, a viewer inside the cycle, both chain-termination cases stated separately, path-local visited state with shared ancestors, the direct-PP inactive-endpoint variants, missing (non-existent) viewer and target ids, and infrastructure-error propagation. The scenarios are pending independent human Stage-1 approval; no Stage-2 test, production code, Prisma schema/migration, seed, or approvals.yaml entry was created by this run.
- (constraint) CAP-1 coverage addition: a MISSING viewer or target id — a well-formed id with no `users` row — is a distinct case from `isActive = false` and had no scenario. Neither the resolver nor the adapter looks a user up by id, so absence and "no qualifying relationship" are the same input today and both resolve to the Colleague floor. ACM3-II-14 carries it; Stage 3 therefore owes an existence check on both parties, not only an active-state check.
- (constraint) CAP-1 cycle-position taxonomy grounded in the schema: `relationships_one_direct_per_user` makes an upward walk a linked list, so once it enters a cycle it never leaves and every node reachable after the first repeat was already visited. A viewer reachable at all is therefore always reached before or at the first repeat, and "a repeated node before viewer proof" has exactly one constructible shape — the repeat ends the walk and the viewer is never proven. The two constructible positions are ACM3-II-06 and ACM3-II-07, with ACM3-II-08 covering the viewer's own membership in the cycle.
- (decision) Code-verified behavior baseline for ACM-3 Stage 2, established by executing the shipped reporting and PP queries against the running migrated PostgreSQL inside a rolled-back transaction. Red (behavior changes Stage 3 must make): repeat after viewer proof, viewer inside a cycle, inactive PP endpoint as viewer, inactive target of a live PP assignment, missing viewer, missing target. Green (already-shipped behavior the new scenarios guard as regressions): empty-bulk short-circuit, duplicate-target collapse, repeat before viewer proof, absent-edge clean end, inactive-endpoint clean end after viewer proof, path-local visited state, infrastructure-error propagation.
- (question) OPEN — F-6 is factually wrong and is NOT resolved by this run. `stories.yaml` ACM-3-scenarios and reviews/review-p2-repair.md F-6 both assert that the after-viewer-proof Reporting GRANT on an inactive endpoint is a widening because "the current recursive CTE joins users.isActive in both terms and stops at an inactive node, so it would deny that case today". The two joins do different jobs: the base term gates the target's own active state, the recursive term gates the node the walk is climbing FROM, so the walk emits the inactive node as an ancestor and refuses only to climb PAST it. The final `WHERE ancestor_id = <viewer>` still matches the row produced below the dead node. Verified on the running database with a `target -> viewer -> inactive -> active` fixture: the query returns the target, so today's resolver already grants Reporting. The correct characterization is a restatement, and the Stage-3 risk is REGRESSION, not widening. ACM3-II-10 records the verified behavior and raises the contradiction rather than repeating the instruction; correcting stories.yaml and F-6 is a planning decision left to the approver. SPEC CAP-1 needs no correction — it states the required behavior and never claims it is new.
- (question) OPEN — ACM3-II-13 (infrastructure-error propagation) has no authority in CAP-1's `success` text; its only source is the ACM-3-red-tests dispatch line. It is authored so Stage 2 has an approved scenario to translate, and is written to be rejectable on its own: no other scenario depends on it, and rejecting it means the Stage-2 dispatch drops that item rather than carrying an unapproved test.
- (event) ACM-1 Stage-1 coverage audit run on 2026-08-31 before any Stage-2 dispatch, and persisted at implementation-artifacts/access-control/acm-1-stage1-coverage-audit.md. The nine approved ACM1-FB contracts cover 3 of the 13 CAP-3 invariants fully (1, 2, 6), 3 partially (3, 5, 11), and 7 not at all (4, 7, 8, 9, 10, 12, 13), plus fourteen named CAP-3 behaviors with no contract at all. This is not a new discovery: the suite README already declared the dispatch partial. What is new is measuring it against CAP-3's own rule that Stage 1 and Stage 2 each cover all thirteen, and database-schema.md's statement that partial coverage is not a passing ACM-1.
- (decision by user) 2026-08-31 ACM-1 disposition: authorize the ACM-1R repair rather than a partial Stage 2 or a waiver of the thirteen-invariant gate. The user was offered all three and chose the repair.
- (event) ACM-1-red-tests HALTED in stories.yaml with DO NOT DISPATCH, on the ACM-4-red-tests precedent. The reason is recorded as a contradiction rather than a preference: the entry requires proving all thirteen invariants while translating only approved scenarios, and seven have no approved scenario, so no dispatch can satisfy both halves. Closing that gap by authoring the missing contracts in test form would be a Stage-1 act inside a Stage-2 dispatch, which AD-1 forbids.
- (event) Story Breakdown extended for the CAP-3 repair: ACM-1R-scenarios and ACM-1R-tests added, all 27 earlier IDs preserved in their relative order, total 29. The two new entries sit BETWEEN the halted ACM-1-red-tests and ACM-1-production rather than after it, so the file reads top-to-bottom in the order the stages may actually run; validate.py now asserts that ordering. ACM-1-production is unchanged in subject and scope and names ACM-1R-tests as its prior stage. validate.py ran 1224 checks with 0 failures.
- (decision) ACM-1R-tests produces ONE committed-red suite over the union of ACM1-FB-01..09 and the ACM-1R contracts, not a second partial suite. The thirteen invariants are properties of one migration, so two partial Stage-2 records would leave no single record an approver could read as CAP-3 proven. ACM-3's approved partial Stage 2 is not a counter-precedent: it is a legitimate increment that leaves its story in-progress, and ACM-3 has no Stage-3 approval either.
- (event) ACM-1R Stage-1 scenario prose authored: ACM1R-FB-10 through ACM1R-FB-28, nineteen contracts closing every Missing and Partial item in the audit. ACM1-FB-01..09 were not rewritten, renumbered, or restated. One authoring correction is worth recording because the intuitive answer is wrong: a canonical permission key renamed in place is ABSENT from the seed's view, not DIFFERENT, so the FR-AMD-1 drift table requires restoring the canonical row and preserving the renamed one — renaming it back would be the in-place key update that append-only forbids. ACM1R-FB-16 was rewritten to match the table after first being drafted as fail-before-writes.
- (event) No Stage-1 approval was recorded for ACM-1R-scenarios. The nineteen contracts are authored and pending an independent human approver, author != approver; approvals.yaml is untouched. No Stage-2 test, Prisma schema, migration, seed, or production code was written by this dispatch.
- (event by user) Independent Stage-1 approval recorded for all eleven ACM-3 completion artifacts ACM3-II-04..ACM3-II-14 at workspace commit e42fd2d111ecab7e4cbddb9680c47ecdfbc2edc9, author Claude Code (agent), approver Anna Pikula. Verified per the ledger's own rule before recording: the commit resolves and all eleven artifact paths are present at that revision. This supersedes the earlier line in this log stating that no approvals.yaml entry was created by the ACM-3 Stage-1 completion run. CAP-1 Stage-1 is now complete and approved at fourteen scenarios: ACM3-II-01..03 plus ACM3-II-04..14.
- (decision by user) The Stage-1 approval covers ACM3-II-13 as well, so the open question about its authority is answered in favor of keeping it: Stage 2 may translate its infrastructure-error assertion. The separate F-6 question is NOT answered by this approval and stays open — stories.yaml ACM-3-scenarios and review F-6 still assert a widening that the running database contradicts, and correcting them remains a planning decision.
- (constraint) ACM-3 Stage-2 consequence of the completed Stage 1: the existing committed-red suite test/access-control/acm3-inactive-identity.e2e-spec.ts covers only ACM3-II-01..03, and its Stage-2 approval at services/backend commit 32a10bf7ce5412b7a16f317c9b54a10a994b9e0a stands for those three alone. ACM-3-production may not run against that record, because CAP-1 is not green until the approved fourteen are covered. The next ACM-3 dispatch is a Stage-2 extension of that one file to all fourteen scenarios, committed red and independently approved, before the single ACM-3-production dispatch.
- (event by user) Working tree committed in full at the user's explicit request while a parallel agent was still authoring in it: e42fd2d carries the eleven ACM-3 Stage-1 artifacts and the suite README, and 6c77ad1 carries the in-flight ACM-1R Stage-1 work, the ACM-1 coverage audit, the SPEC/stories/validator updates, sprint status, and this shared append-only log. The ACM-1R artifacts in 6c77ad1 carry no Stage-1 approval and being committed does not confer one.
- (event by user) Independent Stage-1 approval recorded for all nineteen ACM-1R-scenarios artifacts at workspace commit 6c77ad1, approver Anna Pikula, author Claude Code (agent). The commit resolves and all nineteen artifacts were verified present at that revision before the records were written. Approval basis, recorded because ACF-1 set the precedent of stating it honestly: line-by-line reading of all nineteen contracts, including the two flagged judgment calls — ACM1R-FB-16 treating a renamed canonical permission key as ABSENT rather than DIFFERENT per the FR-AMD-1 drift table, and ACM1R-FB-27 placing its injected failure after writes so it can distinguish one real transaction from statements that failed early.
- (event) ACM-1R Stage 2 is now unblocked. ACM-1-red-tests remains halted; the eligible dispatch is ACM-1R-tests, one committed-red suite over the union of ACM1-FB-01..09 and ACM1R-FB-10..28. No Stage-3 approval exists and ACM-1-production remains gated on the ACM-1R Stage-2 record.
- (event) ACM-1R Stage-2 committed-red evidence authored at services/backend 77c1b0f: test/access-control/acm1r-fr-foundation.e2e-spec.ts, one suite over the union of ACM1-FB-01..09 and ACM1R-FB-10..28. Run against real migrated PostgreSQL: 39 of 39 tests FAIL, 0 pass. The suite invokes the real entrypoints npm run db:seed and npm run db:bootstrap:access-control and never re-implements normalization, locking, adoption, or drift logic inline. No Prisma schema, migration, seed, or production file was changed — git status showed only the new test file.
- (decision) Every failure-path test asserts its SPECIFIC diagnostic rather than a bare nonzero exit, and the suite opens with a test asserting the db:bootstrap:access-control npm script exists. Without both, a missing script would make each failure-path test pass for the wrong reason and the red state would be unreadable.
- (decision) The suite fixes the physical table names the ACM-1 migration must create, because the approved scenarios specify them verbatim: "Permissions", "Policies", "PolicyPermissions", "UserPolicies", "AccessControlBootstrap", alongside the existing lowercase "users". ACM-1-production is bound to these.
- (event) ACM-1R-tests is PENDING independent Stage-2 approval; no approvals.yaml entry exists for it and ACM-1-production is not eligible to start.
- (decision by user) 2026-08-31: the fourteen-scenario Stage-2 extension was judged not to match the MVP, and ACM-3-production was directed to proceed on the Stage-2 record that already exists. Recorded as a user decision because it overrides the constraint logged immediately above it, which said ACM-3-production may not run until all fourteen approved scenarios are covered.
- (event) ACM-3-production dispatched at services/backend 9e69682 against the ONE verified prior-stage record available: ACM-3-red-tests stage-2-tests at 32a10bf, which gates ACM3-II-01..03 only. Implemented exactly what that suite asserts and no more — identity before derivation. A new IdentityPort seam (domain/interfaces/identity.port.ts, infrastructure/prisma-identity.adapter.ts) confirms the viewer and every target in one lookup; AudienceResolverService now denies an unconfirmed viewer globally with no graph read, returns an empty Set for an unconfirmed target instead of the Colleague floor, and keeps the empty-bulk short-circuit above the identity gate. Missing and inactive are indistinguishable at the port by design.
- (constraint) ACM-3-production deliberately did NOT implement the other eight approved CAP-1 behaviors — cycle denial before and after viewer proof, viewer inside a cycle, both chain-termination cases as explicit rules, the inactive-PP-endpoint variants, path-local visited state, and infrastructure-error propagation. Each is approved at Stage 1 and has NO approved Stage-2 test, so implementing it would be production code entering the package through no gate at all. CAP-1 is therefore partially implemented and ACM-3 remains in progress; the honest close needs the Stage-2 extension the user deferred.
- (event) Evidence for ACM-3-production: unit 13/13 pass; test/access-control/acm3-inactive-identity.e2e-spec.ts 5/5 pass, turning the approved committed-red suite green; test/access-control/audience-resolution.e2e-spec.ts 10/10 pass, so ACF-1 Phase-0 behavior including ACF-FC-03 empty bulk and ACF-FC-04 cycle-terminates is unregressed. The full e2e run also shows 31 user-management failures and the ACM-1R suite's 39 intended reds; the user-management failures are not attributable to this change — src/user-management and src/app.module.ts contain no reference to AccessControlFacade or AccessControlModule, and the failures are 404s for seeded personas after the ACM-1R suite ran npm run db:seed and npm run db:bootstrap:access-control against the shared database.
- (correction) The entry above overstates the remaining gap and is superseded. Measured against the fourteen approved scenarios, ACM-3-production leaves exactly TWO behaviors unimplemented, both the same rule: ACM3-II-07 a repeat after viewer proof, and ACM3-II-08 a viewer inside the cycle. The recursive CTE is untouched, so a proven viewer still grants Reporting on a cyclic chain — confirmed by the pre-implementation database probe. The others are covered: ACM3-II-11 and ACM3-II-14 are satisfied by the new identity gate, because the port returns only ids that exist AND are active, and ACM3-II-04, 05, 06, 09, 10, 12, 13 were already-shipped behavior this change preserves. That coverage claim is ANALYSIS, not evidence: only ACM3-II-01..03 have an approved Stage-2 test, so nothing but those three is proven by a run.
- (constraint) The residual CAP-1 gap after ACM-3-production is the cycle-acyclicity rule alone. Closing it needs its own Stage 2 over ACM3-II-07 and ACM3-II-08 before any production change to the reporting walk, and until then the product-owner cycle-supersession decision is recorded in the SPEC but not enforced in code.
- (event) No stage-3-production record was written for ACM-3. The production artifact is committed and pending independent human approval; author != approver forbids the agent that wrote it from recording its own approval.
- (event) ACM-1R Stage-2 evidence corrected three times before approval, each found by re-running rather than by inspection. (1) The entrypoint guard test itself failed with a TypeError in test code instead of on the missing script — the one test whose job was to make the red state unambiguous was broken. (2) FB-27 aborted on Jest's 5s default before its own logic ran, so it was red for a harness reason, not for the absent subject; the suite now sets a 120s timeout. (3) Fixture cleanup matched only the current runId, so earlier runs leaked users into the shared development database forever; it now matches the suite prefix.
- (decision by user) 2026-08-31: the ACCESS_CONTROL_BOOTSTRAP_TEST_HOOK environment hook is removed rather than carried into production. FB-27 now kills the process with SIGKILL after OBSERVING a real write — a granted RowExclusiveLock on "Permissions" held by another backend, whose rows are still invisible because the transaction is uncommitted — so PostgreSQL performs the rollback with no cooperation from production code, which is stronger evidence than a cooperative failure. FB-19 is made deterministic by taking the root row lock FIRST so the bootstrap blocks on its own FOR UPDATE, with ordering enforced by PostgreSQL row locking rather than sleeps. ACCESS_CONTROL_BOOTSTRAP_LOCK_TIMEOUT_MS is retained as operational configuration of a deploy-time step, not as a test hook.
- (event by user) Independent Stage-2 approval recorded for ACM-1R-tests at services/backend 506f7f8, approver Anna Pikula, author Claude Code (agent). Commit resolves and the artifact is present at that revision. Final red state: 39 of 39 tests fail, 0 pass, 0 test-code errors, 0 harness timeouts, every failure attributable to the absent schema, migration, and bootstrap entrypoint. An intermediate approval named ef3ed95; it was NOT recorded, because the tests changed afterwards on the user's own hook-removal decision and a ledger entry on superseded evidence would misstate what was approved.
- (event) All four ACM-1R test commits touch exactly one file, the test suite; no production code was modified by this dispatch. Commit 9e69682 by a concurrent session (ACM-3 identity validation) is interleaved in the branch history and is unrelated to this artifact.
- (event) ACM-1-production is now eligible: its prior stage ACM-1R-tests exists and verifies.
- (event) ACM-3 Stage-2 committed-red evidence for the residual cycle gap authored at services/backend f6bbda3: test/access-control/acm3-cycle-acyclicity.e2e-spec.ts, covering ACM3-II-07 and ACM3-II-08 only. Prior stage verified before starting: those two Stage-1 artifacts are present at workspace commit e42fd2d and their approval records stand. Run against real migrated PostgreSQL: 6 of 6 tests FAIL, 0 pass, every failure the intended assertion — `reporting` present where the approved scenario requires it absent, and in the PP case `reporting` is the only surplus member so `pp` is already retained correctly. The commit touches exactly one file; no production code, Prisma schema, migration, or seed was changed.
- (decision) A NEW suite rather than an extension of acm3-inactive-identity.e2e-spec.ts. That file is approved at Stage 2 and now green after 9e69682; adding red tests into it would turn an approved artifact red and make one ledger record cover two different evidence states. The two suites are complementary: identity in the first, acyclicity in the second.
- (decision) The suite races every facade call against a 5s guard. A cyclic graph is the one shape where a wrong implementation does not return a wrong answer but fails to return at all; SET LOCAL statement_timeout = '2s' protects the SQL walk, but a walk moved into application code by ACM-3-production would have no such guard, so non-termination must surface as a fast readable failure rather than a hung suite. ACF-FC-04 set this precedent.
- (event) ACM-3-cycle Stage 2 is PENDING independent human approval. No approvals.yaml entry was written for it, and no production change to the reporting walk is authorized until that record exists and verifies.
- (event by user) Independent Stage-2 approval recorded for the ACM-3 cycle suite at services/backend f6bbda375087b107a332a753c729c4a8dfc1399f, story_id ACM-3-red-tests, author Claude Code (agent), approver Anna Pikula. Verified before recording: the commit resolves in services/backend and test/access-control/acm3-cycle-acyclicity.e2e-spec.ts is present at that revision. This supersedes the PENDING line immediately above it. The earlier ACM-3-red-tests record at 32a10bf is NOT superseded — the two records approve different suites covering different scenarios, and both are the Stage-2 basis for the reporting-walk change.
- (constraint) Stage-2 coverage of the fourteen approved CAP-1 scenarios is now two records covering five scenarios: ACM3-II-01..03 in acm3-inactive-identity.e2e-spec.ts and ACM3-II-07..08 in acm3-cycle-acyclicity.e2e-spec.ts. Nine approved scenarios — ACM3-II-04, 05, 06, 09, 10, 11, 12, 13, 14 — still have NO Stage-2 record. Seven of them assert behavior the identity change at 9e69682 either already satisfies or preserves, and analysis says ACM3-II-11 and ACM3-II-14 are satisfied by the identity gate, but none of that is proven by a run: it is analysis, and it does not become evidence by being written down.
- (event) ACM-3-production is eligible for the reporting-walk change: its prior stage exists and verifies at f6bbda3. Scope is the acyclicity rule alone — make the six red tests green by making viewer proof provisional and denying Reporting on a repeated node, per target, with Self and direct PP evaluated independently. Nothing else in CAP-1 may change under this dispatch.
- (event) ACM-3-production reporting-walk change landed at services/backend 5bdbc75, one file: prisma-relationship-graph.adapter.ts. The recursive CTE now carries an explicit per-row path and is UNION ALL instead of UNION. Reachability is no longer the decision: Reporting is granted only where the viewer appears on a target's path AND no row for that target is marked repeated.
- (decision) UNION was replaced rather than kept, and the reason is worth recording because the old comment argued the opposite. Set deduplication does halt the recursion on a cycle, but halting is not denying — it ends the walk while leaving the viewer's row in the result, which IS the reachability answer CAP-1 retired. Only a per-row path can say which target's walk closed on itself, and the same path makes visited state path-local by construction, so a shared ancestor across two targets is never mistaken for a repeat. The walk stays bounded because relationships_one_direct_per_user gives each person at most one direct row, so each target walks a single path and NOT c.repeated stops it at the first closure.
- (decision) Both chain-termination cases are the same join rather than two branches: an absent edge produces no successor row, and an edge whose endpoint is inactive is removed by the join on users e, so it produces no successor row either. Unusable is therefore literally treated as absent, which is what CAP-1 says, instead of being a second rule that could drift from the first.
- (event) Evidence for ACM-3-production, reporting walk: unit 13/13 pass; acm3-cycle-acyclicity.e2e-spec.ts 6/6 pass, turning the approved committed-red suite green; acm3-inactive-identity.e2e-spec.ts 5/5 still pass; audience-resolution.e2e-spec.ts 10/10 still pass, including ACF-FC-01 whose HTTP assertions run through the facade via a port override and ACF-FC-04 which asserts a cycle terminates with Colleague. 21 of 21 across the three suites.
- (event) The nine approved scenarios with no Stage-2 record were additionally MEASURED against the new walk, by running its exact query over purpose-built fixtures inside a rolled-back transaction: ACM3-II-06 denies, ACM3-II-09 grants on both an absent-edge chain and a direct-manager chain, ACM3-II-10 grants above a proven viewer and denies for a viewer above the dead node, and ACM3-II-12 grants BOTH shared-ancestor targets. All nine outcomes match their approved scenario. This is a measurement, not a Stage-2 record, and it does not substitute for one: it is written down so the earlier note that this coverage rested on analysis is no longer true of the reporting walk.
- (constraint) CAP-1 status after this dispatch: the reporting walk and the identity gate both match every approved scenario, but Stage-2 records still cover only five of fourteen. ACM3-II-04, 05, 06, 09, 10, 11, 12, 13, 14 remain without a committed test, so a future change to either seam has nothing standing to catch a regression. Closing that is a further Stage-2 dispatch, not a production one.
- (event) No stage-3-production record was written for the reporting-walk change. The artifact is committed at 5bdbc75 and pending independent human approval; author != approver.
- (event by user) Independent Stage-3 approval recorded for ACM-3-production at services/backend 5bdbc7580ffcc6a40bad7d2972f58ae7bb90ddef, artifact_path src/access-control/infrastructure/prisma-relationship-graph.adapter.ts, author Claude Code (agent), approver Anna Pikula. Verified before recording: the commit resolves in services/backend and the artifact is present at that revision; its prior stage f6bbda3 was already verified when that Stage-2 record was written. This supersedes the PENDING line for this artifact.
- (constraint) The CAP-1 status recorded immediately before this approval still holds after it: the reporting walk and the identity gate both match every approved scenario by measurement, but Stage-2 records exist for five of fourteen. Approval of production behavior does not retroactively create the missing nine Stage-2 records, and none should be inferred from this entry.
- (event) The green ACM-1R run surfaced two defects in the already-approved Stage-2 artifact, both in the tests, neither in production behavior. FB-11's pg_constraint query never executed (`operator does not exist: name[] = text[]`, an array_agg(name) vs text[] type mismatch); the support key and composite FK were independently confirmed present via direct psql before any test fix. FB-27's timing window let the bootstrap's transaction commit faster than the 100ms poll could observe it, so the test could report a false negative on rollback; the underlying single-transaction property was independently verified with a standalone script (LOCK TABLE "UserPolicies", SIGKILL, and a same-session-invisible-write check) before any test fix, confirming production was already correct.
- (decision by user) 2026-08-31: fix and re-approve the two defective assertions rather than open a new repair cycle or ship at 37/39. FB-11 now casts array_agg(attname::text). FB-27 now holds an EXCLUSIVE lock on "UserPolicies" — the LAST table the bootstrap writes — pinning it inside its own open transaction with the permission writes already done, and asserts BOTH halves of the property: a granted foreign lock on "Permissions" (the write happened) and countOf('Permissions') === 0 in the observing session (it is uncommitted). Neither fix touches production code or weakens any assertion.
- (event) Full e2e suite run before and after ACM-1-production, isolated by git stash: the User Management baseline (auth, profile, career-timeline, relationships) shows 31 pre-existing failures with or without the ACM-1 change — the /auth routes do not exist in src and LocalStack is not running, unrelated to this dispatch. ACM-3's committed-red cycle-acyclicity suite (a concurrent session's f6bbda3) went green independently at 5bdbc75. Zero regressions attributable to ACM-1-production.
- (event) ACM-1R Stage 2 re-approved at services/backend 5b26e04 (39/39 passing) and ACM-1-production Stage 3 approved at services/backend 436f3c6, both approver Anna Pikula, author Claude Code (agent). Commits resolve and both artifacts verified present at their revisions before the records were written.
- (event) ACM-1 / CAP-3 is COMPLETE. Production: prisma/schema.prisma (5 FR models), migration 20260831070000_access_control_functional_roles (140 lines raw SQL, all 13 CAP-3 invariants), access-control-bootstrap.ts, scripts/bootstrap-access-control.ts, npm run db:bootstrap:access-control. All 28 approved contracts (ACM1-FB-01..09, ACM1R-FB-10..28) pass against real migrated PostgreSQL through the real entrypoint. ACM-2 and ACM-4R-tests are now unblocked; ACM-8 remains additionally gated on ACM-2, ACM-3, and ACM-5.
- (decision by user) 2026-08-31 review repair resolves the open F-6 planning contradiction in favor of the code-verified baseline already recorded above: ACM3-II-10's after-viewer-proof GRANT is already-shipped behavior and a regression guard, not a widening. `stories.yaml` now says this directly; CAP-1 behavior is unchanged.
- (correction) The ACM3-II-11 and ACM3-II-14 Stage-2 records are post-hoc regression evidence, not committed-red evidence. Git ancestry proves identity production commit 9e69682 precedes both test commits e04a09c and 366e483. The approval records retain their stable story and stage identifiers but now state this provenance explicitly; no historical red state is inferred.
- (decision) ACM3-II-13 failure injection no longer renames the shared `relationships` table. Its real-PostgreSQL test now holds a transaction-scoped `ACCESS EXCLUSIVE` lock and lets the production adapter's existing 2-second statement timeout reject the call. The relation remains present throughout, and PostgreSQL releases the lock automatically on every transaction or connection termination path.
- (event) 2026-09-12: CAP-6's success criterion is superseded by the tree. services/backend commit 0788f60 rebound ACCESS_CONTROL_PORT to AccessControlFacadeAdapter (user-management.module.ts:215, useExisting) and deleted interim-access-control.adapter.ts, replacing it with infrastructure/access-control-facade.adapter.ts. Verified directly against the working tree and that commit's stat, not from report.
- (correction) The cutover was reported as the UMAC-1 Stage 3 cutover dated 2026-09-01. Both details are wrong on verification. 0788f60 is authored and committed 2026-09-02 11:06 +0100 under the subject 'User management WIP (#7)', carrying a large batch of unrelated user-management work; 2026-09-01 is the date of the UMAC-1 Stage-1 and Stage-2 approvals and of the in-code realignment comment, not of the cutover commit. The code labels the change AD-21, not UMAC-1.
- (event) BLOCKER surfaced before amending: no UMAC-1 Stage-3 approval exists. spec-user-management-access-control-adoption/approvals.yaml records UMAC-1-scenarios (stage-1, 2026-09-01T14:05:00Z) and UMAC-1-red-tests (stage-2, 2026-09-01T15:30:00Z), then jumps to UMAC-10-scenarios; its line 189 still names UMAC-1-production (Stage 3) as the pending next dispatch, adoption SPEC.md:40 calls Stage 3 'the next authorized' dispatch, and project-status-2026-09-01.md:64 still lists 'rebind the port, delete the interim adapter' as unshipped. A repo-wide search for a UMAC-1-production approval record returns none. This SPEC's own ledger constraint applies: a prose assertion that a stage was approved is not an approval.
- (decision by user) 2026-09-12: amend CAP-6 recording the rebind as unratified drift, not as a completed approved cutover. CAP-6's success criterion states the current bound reality as fact and names it as landed outside the AD-1 gate with no Stage-3 record, so the canonical contract does not launder an ungated production change. Rejected alternatives: amending plainly as a completed cutover, and holding the amendment until the ledger is reconciled.
- (decision by user) 2026-09-12: the emitted realignment story covers the five ACM8-KC Stage-1 scenario docs plus the stale header comment at services/backend/src/access-control/access-control.module.ts:18-19, which still asserts the port binds to InterimAccessControlAdapter. Ledger ratification of the missing UMAC-1-production record is deliberately NOT folded into this story; it belongs to the User Management adoption package that owns UMAC-1.
- (note) The ACM-8 Stage-2 e2e artifact needs no realignment work: test/access-control/acm8-kernel-composition.e2e-spec.ts was already inverted in-tree (KC-02 now asserts the port resolves to AccessControlFacadeAdapter by class name; KC-03 now asserts GET /users/:id goes through the real facade, self 200 enveloped / stranger 403) and its header explicitly defers the scenario docs as 'a follow-up realignment for the access-control context'. That deferral is the story emitted here.
- (correction) Two further statements in this SPEC are falsified by the same commit and are corrected in the same derive, since leaving them would make the kernel contract self-contradictory (Spec Law 1-6 coherence). Success signal said 'User Management remains on its interim adapter' — flatly false, the interim adapter no longer exists. Non-goals framed 'production port rebinding' as wholly future work for a later package — it has now partly happened, ungated. Neither correction widens CAP-6's scope; both record state.
- (capability) CAP-6 amended, ID preserved. Intent unchanged: ACM-8's subject is container composition, and ACM-8 itself still adopted nothing in User Management. Success criterion's first half (AppModule imports AccessControlModule, resolves AccessControlFacade, no User Management or endpoint change from ACM-8) stands and is shipped at ACM-8-production d2335907. Second half (ACCESS_CONTROL_PORT remains bound to InterimAccessControlAdapter) is struck as superseded and replaced by a drift record naming 0788f60, the missing Stage-3 approval, and the fact that CAP-6 now asserts nothing about the live port binding — ratification or revert belongs to the adoption package that owns UMAC-1.
- (decision) Realignment story emitted as ACM-8R-scenarios, inserted after ACM-8-production and before ACM-9-final. Subject: the five ACM8-KC Stage-1 scenario docs under docs/test-cases/access-control-kernel/kernel-composition/ (kc-02 and kc-03 substantively inverted, kc-01/04/05 re-checked against the amended CAP-6) plus the false header comment at services/backend/src/access-control/access-control.module.ts:18-19. Stage-1 prose only; both checkpoints true. Id is prefix-free against ACM-8-scenarios, ACM-8-red-tests, and ACM-8-production, and follows the existing ACM-1R / ACM-4R repair-naming convention.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6 and 8): PASS. CAP-6 keeps both intent and success; its intent still describes WHAT (container composition) with no implementation prescription; CAP-6's id is preserved and nothing renumbered; Non-goals remain explicit; the Success signal stays demonstrable. The amendment removed a self-contradiction rather than adding one — three statements across CAP-6, Non-goals, and the Success signal had all been falsified by the same commit. No new assumptions[] or open_questions[] were needed: every claim is verified against the tree or the ledger.
- (event) Self-validate Pass 2 (preservation): PASS. Every load-bearing claim in the invocation landed — the rebind to AccessControlFacadeAdapter, the deleted interim adapter, commit 0788f60, the CAP-6 amendment, and the realignment story. The two claims that did not survive verification (the 2026-09-01 date and the UMAC-1 Stage-3 attribution) are preserved as corrections rather than dropped, so the record shows what was reported alongside what was true. Nothing was dropped as wrapper-only.
- (event) validate.py after the amendment: 1292 checks, 1 failure, verdict FAIL. The single failure is PRE-EXISTING and unrelated to this run: the R10 landing check wants the literal 'An approval is a persisted record' in docs/architecture/testing-strategy.md, and git shows that sentence present at 9735bab and c342138 and removed by b074364. The file is clean in git status and was not touched here. The two failures this run did cause — pinned story count 29 and the pinned STORY_IDS roster — were resolved by adding ACM-8R-scenarios to both pins in validate.py.
- (event by user) 2026-09-12: Anna Pikula ratified the ungated rebind rather than reverting it. Revert was assessed and rejected as infeasible: 0788f60 mixes the UMAC-1 files with Story 1.1 population import and its migration across 59 files, and Epic 4 (49fd0c4) since added section-access.guard.ts, which injects ACCESS_CONTROL_PORT, so un-rebinding would break already-merged work. Seven per-artifact stage-3-production records were appended to spec-user-management-access-control-adoption/approvals.yaml at commit 0788f60, author Dmytro Novyk, approver Anna Pikula, timestamped at the real decision time and not backdated. Each path was verified present at that revision first.
- (correction) CAP-6's drift paragraph is now stale in its own terms: it asserted that no stage-3-production record exists for the rebind. That record exists as of 2026-09-12. CAP-6 is re-derived to cite the late ratification instead, and to keep saying that the rebind was NOT gated when it shipped — ratification records the fact, it does not retroactively make the change gated.
