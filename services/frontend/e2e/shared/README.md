# Shared E2E Test Utilities

This folder contains shared configuration, mocks, and helpers used across test suites.

## Structure

- `test-env.ts` - Test environment configuration (mirrors `src/config/env.ts` for Node.js tests)

Add reusable mocks, fixtures, and helper functions here as the test suite grows.
